"use strict";

/* ============================================================
   PLAYER POOL  —  APPROXIMATE per-game lines, placeholders only.
   Replace via the "Load projections" button before you draft for real.
   [name, pos, team, FGM, FGA, FTM, FTA, 3PM, PTS, REB, AST, STL, BLK, TO]
   ============================================================ */
/* ============================================================
   PUBLIC BUILD

   TEST BUILD: the first 30 current-season projection rows are embedded
   as a default sample. A user-pasted projection pool still overrides them.

   2025-26 historical actuals are embedded below from BoxScore Lab,
   published under CC BY 4.0. Source: https://boxscorelab.com/downloads/
   Data through August 9, 2026.
   ============================================================ */
const RAW = `
R#	PLAYER	ADP	POS	TEAM	GP	MPG	FG%	FT%	3PM	PTS	TREB	AST	STL	BLK	TO	TOTAL
1	Nikola Jokic	1.7	C	DEN	72	35.1	0.573 (10.5/18.3)	0.816 (5.6/6.8)	1.8	28.4	12.7	10.4	1.6	0.7	3.5	15.96
2	Victor Wembanyama	2.9	C	SA	66	30.4	0.502 (9.2/18.3)	0.831 (5.1/6.1)	2.2	25.6	11.8	3.6	1.1	3.2	2.9	14.39
3	Shai Gilgeous-Alexander	3.1	PG	OKC	71	33.5	0.534 (11.1/20.7)	0.889 (8.0/8.9)	1.9	32.1	4.7	6.5	1.6	0.9	2.3	14.17
4	Luka Doncic	4.6	PG	LAL	68	35.4	0.465 (10.6/22.9)	0.781 (7.5/9.6)	4.0	32.7	8.7	8.8	1.7	0.5	4.0	10.94
5	Giannis Antetokounmpo	5.3	PF/C	MIA	69	34.2	0.608 (12.1/19.9)	0.628 (6.9/11.0)	0.3	31.4	11.9	6.5	1.0	1.1	3.3	10.31
6	Cade Cunningham	8.2	PG	DET	68	33.9	0.466 (9.2/19.7)	0.826 (4.6/5.6)	2.0	25.0	5.8	9.8	1.3	0.8	4.1	7.80
7	Anthony Edwards	6.8	SG/SF	MIN	74	35.3	0.487 (9.7/20.0)	0.818 (5.4/6.6)	3.7	28.7	5.6	3.5	1.2	0.7	3.0	7.62
8	Tyrese Maxey	16.3	PG/SG	PHI	70	36.1	0.458 (8.4/18.2)	0.885 (4.6/5.2)	2.8	24.2	3.4	5.8	1.8	0.5	2.2	7.59
9	Jayson Tatum	11.3	SF/PF	BOS	68	33.7	0.464 (9.0/19.4)	0.815 (4.8/5.8)	3.3	26.1	8.8	5.8	1.2	0.5	2.8	7.54
10	Stephen Curry	21.1	PG	GS	60	31.6	0.456 (8.4/18.3)	0.929 (4.3/4.6)	4.4	25.4	3.6	5.2	1.0	0.4	2.9	7.52
11	Kevin Durant	25.9	SF/PF	HOU	70	34.6	0.523 (8.8/16.8)	0.860 (4.8/5.6)	2.3	24.7	5.4	4.3	0.8	1.0	2.9	7.26
12	Anthony Davis	12.8	PF/C	WAS	65	31.1	0.514 (8.6/16.8)	0.767 (4.7/6.1)	0.6	22.5	10.9	3.2	1.1	1.9	2.1	7.23
13	Donovan Mitchell	21.7	PG/SG	CLE	72	33.4	0.464 (9.3/20.1)	0.846 (4.9/5.8)	3.3	26.9	4.7	5.4	1.5	0.3	2.6	6.84
14	Tyrese Haliburton	73.2	PG/SG	IND	63	30.9	0.467 (6.1/13.0)	0.852 (2.4/2.8)	2.8	17.3	3.3	9.6	1.3	0.6	1.5	6.55
15	Jamal Murray	36.2	PG	DEN	73	34.8	0.478 (8.2/17.2)	0.887 (3.9/4.4)	2.8	23.2	4.1	6.5	1.1	0.4	2.1	6.54
16	Karl-Anthony Towns	14.4	C	NY	73	31.3	0.514 (7.6/14.7)	0.844 (4.6/5.4)	1.7	21.3	11.8	2.9	0.9	0.6	2.5	6.47
17	Cooper Flagg	23.2	SF/PF	DAL	72	34.1	0.475 (8.8/18.5)	0.836 (4.4/5.3)	1.3	23.3	7.3	4.9	1.3	1.0	2.2	6.39
18	Jalen Johnson	31.9	PF	ATL	72	35.1	0.482 (8.1/16.8)	0.786 (3.7/4.7)	1.5	21.4	10.2	6.9	1.4	0.6	3.3	6.38
19	Kawhi Leonard	49.7	SF/PF	TOR	65	31.1	0.491 (8.6/17.5)	0.872 (4.4/5.0)	2.2	23.7	5.0	3.0	1.7	0.4	1.9	6.38
20	Trae Young	22.2	PG	WAS	70	33.4	0.436 (7.0/16.0)	0.868 (6.2/7.1)	2.7	22.8	2.9	10.8	1.1	0.2	4.3	6.14
21	Trey Murphy III	73.1	SG/SF	NO	68	35.2	0.463 (7.4/16.0)	0.888 (3.6/4.0)	3.2	21.6	5.5	3.7	1.3	0.5	1.9	5.98
22	Scottie Barnes	29.5	SG/SF	TOR	80	34.2	0.477 (7.5/15.7)	0.784 (3.4/4.3)	1.0	19.4	7.9	6.1	1.5	1.3	2.8	5.96
23	Chet Holmgren	43.0	PF/C	OKC	70	29.7	0.536 (6.3/11.7)	0.780 (3.4/4.4)	1.4	17.3	9.1	1.9	0.7	2.1	1.7	5.88
24	James Harden	18.1	PG/SG	CLE	72	33.4	0.421 (5.9/14.0)	0.878 (5.6/6.4)	2.6	20.0	4.6	8.2	1.1	0.5	3.4	5.81
25	Austin Reaves	51.9	PG/SG	LAL	70	34.4	0.472 (6.9/14.5)	0.874 (5.2/5.9)	2.6	21.5	4.6	5.7	1.1	0.3	2.6	5.59
26	Evan Mobley	33.8	PF/C	CLE	72	32.5	0.552 (7.5/13.7)	0.665 (3.1/4.7)	1.1	19.3	9.6	3.6	0.8	1.8	2.1	5.25
27	Kyrie Irving	90.5	PG	DAL	55	31.8	0.484 (7.5/15.5)	0.899 (3.3/3.7)	2.4	20.7	4.0	4.9	1.1	0.4	1.9	4.91
28	Jaren Jackson Jr.	73.0	PF/C	UTA	67	30.7	0.483 (7.9/16.4)	0.788 (4.0/5.1)	2.0	21.8	5.8	2.1	1.2	1.5	2.2	4.85
29	Amen Thompson	20.2	SG/SF	HOU	76	36.2	0.539 (7.0/13.0)	0.760 (3.7/4.9)	0.4	18.1	7.9	4.2	1.5	1.0	2.2	4.75
30	Dejounte Murray	127.4	PG/SG	NO	65	30.7	0.456 (6.3/13.8)	0.836 (3.2/3.8)	1.6	17.3	6.1	7.1	1.8	0.3	3.4	4.70
`.trim();

const RAW_LAST = `
Player	Pos	Team	G	FGM	FGA	FTM	FTA	3P	TRB	AST	STL	BLK	TOV	PTS
Amen Thompson	G-F	HOU	79	7.0506	13.2152	3.8481	4.9367	0.3165	7.7722	5.3165	1.5063	0.6076	2.4051	18.2658
Kevin Durant	F	HOU	78	9.1795	17.6410	5.2308	5.9872	2.3846	5.4615	4.7692	0.7949	0.9103	3.1538	25.9744
Desmond Bane	G	ORL	82	7.1098	14.6951	3.8293	4.2195	2.0366	4.1220	4.1220	1.0488	0.4512	2.0000	20.0854
Toumani Camara	F	POR	82	4.7805	10.8659	1.1829	1.6707	2.6707	5.1341	2.4512	1.1463	0.4268	1.7561	13.4146
Jabari Smith Jr.	F	HOU	77	5.6883	12.6623	2.1039	2.7143	2.2987	6.8701	1.8571	0.7273	0.9221	1.3636	15.7792
Mikal Bridges	G-F	NYK	82	5.7561	11.7439	0.9878	1.1951	1.9024	3.8049	3.7073	1.3049	0.7561	1.0122	14.4024
Scottie Barnes	G-F	TOR	80	7.1125	14.0375	3.0250	3.7125	0.8500	7.5000	5.9250	1.4250	1.4500	2.6250	18.1000
Tyrese Maxey	G	PHI	70	9.9143	21.4429	5.3143	5.9571	3.1429	4.1429	6.5857	1.8571	0.7857	2.4429	28.2857
Jamal Murray	G	DEN	75	8.7600	18.1333	4.6133	5.2000	3.2667	4.4000	7.1333	0.8800	0.3867	2.2800	25.4000
Derrick White	G	BOS	77	5.6753	14.3896	2.3896	2.6494	2.7143	4.4026	5.3766	1.1429	1.2727	1.7403	16.4545
VJ Edgecombe	G	PHI	75	6.0133	13.7333	1.9733	2.4133	1.9867	5.6400	4.2000	1.4267	0.5200	1.8267	15.9867
Julius Randle	F-C	MIN	79	7.3418	15.2785	5.0380	6.2785	1.3797	6.7342	5.0380	1.0886	0.2278	2.7468	21.1013
Brandon Ingram	F	TOR	77	7.9740	16.7273	3.7922	4.6234	1.7532	5.5844	3.6883	0.7532	0.7143	2.4286	21.4935
Nickeil Alexander-Walker	G	ATL	78	7.0256	15.3205	3.5513	3.9359	3.2179	3.4359	3.6667	1.3077	0.5385	2.0769	20.8205
Jalen Brunson	G	NYK	74	9.3108	19.9324	4.7838	5.6892	2.6351	3.3378	6.7973	0.7703	0.1081	2.3649	26.0405
Payton Pritchard	G	BOS	79	6.4051	13.8101	1.5316	1.7215	2.6835	3.9494	5.1646	0.7342	0.1266	1.3797	17.0253
Kon Knueppel	G-F	CHA	81	6.3580	13.3827	2.4074	2.7901	3.3704	5.3333	3.3827	0.7037	0.2346	1.9877	18.4938
Jalen Johnson	F	ATL	72	8.3333	17.0556	4.1806	5.3056	1.6667	10.2778	7.8611	1.2361	0.4306	3.3889	22.5139
Dyson Daniels	G	ATL	76	5.2895	10.2237	0.9868	1.6053	0.2895	6.8421	5.9079	1.9605	0.4079	1.7763	11.8553
Paolo Banchero	F	ORL	72	7.3472	16.0000	6.3472	8.1944	1.1667	8.3889	5.1806	0.7083	0.5556	3.0833	22.2083
Donte DiVincenzo	G	MIN	82	4.1341	10.1829	0.9512	1.2805	2.9756	4.0976	3.8171	1.2683	0.4146	1.4390	12.1951
Jaylen Brown	G-F	BOS	71	10.3662	21.7324	6.0000	7.5493	1.9718	6.9296	5.1268	1.0141	0.3803	3.6479	28.7042
James Harden	G	CLE	70	6.9571	16.0429	6.6143	7.4857	3.0714	4.8000	7.9571	1.1286	0.4000	3.5000	23.6000
DeMar DeRozan	G-F	SAC	77	6.4805	13.0519	4.8571	5.5974	0.6104	2.9351	4.1039	1.0390	0.2987	1.2468	18.4286
Alperen Sengun	C	HOU	72	8.0972	15.5972	3.6111	5.2222	0.5556	8.8889	6.1806	1.1806	1.0694	3.1528	20.3611
Miles Bridges	F	CHA	77	6.2208	13.5325	2.7013	3.2857	1.9221	5.8442	3.1948	0.5974	0.4416	1.3896	17.0649
Rudy Gobert	C	MIN	76	4.4079	6.4605	2.0921	3.9737	0.0000	11.4737	1.6842	0.7632	1.6316	1.3947	10.9079
Ryan Rollins	G	MIL	74	6.5676	13.9189	1.6892	2.1216	2.4865	4.5676	5.5946	1.4865	0.4189	2.7162	17.3108
Bam Adebayo	F-C	MIA	73	6.9315	15.6849	4.5068	5.7945	1.7397	10.0274	3.1781	1.1781	0.6712	1.6438	20.1096
Cooper Flagg	F	DAL	70	7.9857	17.0571	4.0429	4.8857	1.0286	6.6571	4.5143	1.2000	0.9143	2.3000	21.0429
Donovan Mitchell	G	CLE	70	9.6857	20.0429	5.3143	6.1429	3.2000	4.5143	5.6857	1.4857	0.2857	2.8286	27.8857
Trey Murphy III	F	NOP	66	7.4848	15.9091	3.3030	3.7273	3.2424	5.7121	3.7879	1.5000	0.3636	1.8182	21.5152
Brandin Podziemski	G	GSW	82	4.8049	10.5610	2.3415	2.9390	1.8659	5.1098	3.7073	1.0732	0.1829	1.5976	13.8171
Karl-Anthony Towns	F-C	NYK	75	6.8933	13.7600	4.7600	5.5467	1.5067	11.8533	3.0133	0.8667	0.5333	2.4933	20.0533
Jaden McDaniels	F	MIN	73	5.7123	11.0822	2.0137	2.4110	1.4110	4.2466	2.7260	1.1096	1.0000	1.7945	14.8493
Onyeka Okongwu	F-C	ATL	74	5.5811	11.6216	2.0676	2.7297	1.9459	7.5946	3.1216	1.1081	1.0676	1.7162	15.1757
Luka Dončić	G-F	LAL	64	10.8281	22.7656	7.8594	10.0781	3.9688	7.7344	8.2812	1.6406	0.5312	3.9844	33.4844
Wendell Carter Jr.	F-C	ORL	78	4.1795	8.1667	2.5385	3.2051	0.9231	7.3974	1.9744	0.7692	0.6282	1.2821	11.8205
Collin Gillespie	G	PHX	80	4.4000	10.5250	0.9500	1.0875	2.9000	4.0875	4.6375	1.2375	0.2000	1.5625	12.6500
Bub Carrington	G	WAS	82	3.8293	9.0366	0.9878	1.3537	2.0610	3.3659	4.6463	0.6341	0.2073	2.3293	10.7073
AJ Green	G	MIL	78	3.3590	7.9231	0.6795	0.7949	2.9744	2.7308	1.9487	0.5000	0.1026	0.9615	10.3718
Julian Champagnie	F	SAS	82	3.6707	8.4024	1.3902	1.6463	2.3780	5.7683	1.5488	0.7561	0.4634	0.8415	11.1098
Nikola Jokić	C	DEN	65	9.9077	17.4154	6.1385	7.3846	1.7231	12.8615	10.7231	1.4154	0.8154	3.7385	27.6769
CJ McCollum	G	ATL	76	6.9079	15.1842	2.4079	3.1184	2.5132	3.3158	3.8553	0.8421	0.4605	1.7763	18.7368
Shai Gilgeous-Alexander	G	OKC	68	10.7500	19.4265	7.9412	9.0294	1.6912	4.2941	6.5882	1.3971	0.7647	2.2206	31.1324
Matas Buzelis	F	CHI	77	5.7922	12.5065	2.4286	3.0909	2.2468	5.8182	2.0519	0.7143	1.5065	2.0649	16.2597
Saddiq Bey	G-F	NOP	72	6.0556	13.4306	3.4444	4.0972	2.0972	5.5556	2.5278	0.9444	0.0972	0.9444	17.6528
Max Christie	G	DAL	77	4.1558	9.4156	1.6234	1.8052	2.3377	3.2338	1.9610	0.6104	0.3506	1.2468	12.2727
Immanuel Quickley	G	TOR	70	5.7286	12.9286	2.4286	2.9571	2.5429	4.0286	5.9286	1.3143	0.0714	1.4571	16.4286
De'Aaron Fox	G	SAS	72	7.0694	14.5417	2.5972	3.4167	1.8194	3.7500	6.1806	1.2361	0.2639	2.2778	18.5556
Kris Dunn	G	LAC	82	2.7683	5.8171	0.7561	0.9878	0.9756	3.3049	3.5976	1.5976	0.1707	1.3537	7.2683
OG Anunoby	G-F	NYK	67	5.8060	12.0000	2.7313	3.2985	2.3433	5.2388	2.1642	1.5522	0.7164	1.8358	16.6866
Royce O'Neale	F	PHX	78	3.3718	8.0128	0.3462	0.4872	2.7179	4.7692	2.6667	1.0769	0.4103	1.3205	9.8077
Quentin Grimes	G	PHI	75	4.5333	10.0667	2.6533	3.1600	1.6933	3.6400	3.2533	0.8533	0.3733	1.7733	13.4133
Deni Avdija	F	POR	66	7.4545	16.1212	7.4091	9.2424	1.8939	6.8788	6.6970	0.7727	0.6212	3.8333	24.2121
Naji Marshall	F	DAL	74	5.6622	11.0946	3.0811	4.0541	0.8378	4.7027	3.2568	1.1081	0.0811	1.6081	15.2432
Cade Cunningham	G	DET	64	8.5625	18.5781	4.8438	5.9688	1.9531	5.5469	9.9062	1.4375	0.8438	3.6875	23.9219
Reed Sheppard	G	HOU	82	4.9634	11.5366	0.8415	1.0488	2.7683	2.9024	3.3780	1.4878	0.6585	1.5000	13.5366
Devin Booker	G	PHX	64	8.5312	18.7188	7.0938	8.1250	1.9062	3.8750	6.0312	0.7969	0.2812	3.1406	26.0625
Anthony Edwards	G	MIN	61	9.8689	20.1803	5.7049	7.1639	3.3607	4.9508	3.7049	1.3607	0.8033	2.8525	28.8033
Tim Hardaway Jr.	G-F	DEN	80	4.3500	9.7375	1.9875	2.4500	2.8000	2.5750	1.3500	0.5250	0.1250	0.5375	13.4875
Jaime Jaquez Jr.	G	MIA	75	6.1867	12.2000	2.2133	2.8800	0.7733	5.0000	4.6933	0.6800	0.2800	2.0267	15.3600
Duncan Robinson	F	DET	77	4.1299	9.0649	1.0779	1.4286	2.8571	2.6623	2.0649	0.5714	0.2857	0.7013	12.1948
Jeremiah Fears	G	NOP	82	5.3293	12.2683	2.3659	3.0000	1.2317	3.6585	3.4024	1.1829	0.3659	2.1707	14.2561
Donovan Clingan	C	POR	77	4.6234	8.8831	1.7922	2.6364	1.0909	11.5844	2.1039	0.5714	1.6883	1.2338	12.1299
Kawhi Leonard	F	LAC	65	9.7692	19.3538	5.7077	6.4000	2.6462	6.3538	3.6000	1.8769	0.4154	2.0462	27.8923
Evan Mobley	C	CLE	65	7.2154	13.2154	2.7692	4.5692	0.9538	9.0154	3.6000	0.7385	1.7385	1.8769	18.1538
Andrew Wiggins	F	MIA	68	5.7353	12.0735	1.8676	2.3824	2.0294	4.8382	2.6765	1.1176	1.0147	1.5147	15.3676
Jake LaRavia	F	LAL	82	2.9268	6.3780	1.4146	1.8537	0.9512	3.9756	1.7683	1.2683	0.4634	1.0610	8.2195
Pascal Siakam	F	IND	62	9.0161	18.6290	4.2258	6.0968	1.6935	6.6290	3.8065	1.1129	0.4032	2.2258	23.9516
Cason Wallace	G	OKC	77	3.2727	7.5844	0.7143	0.8831	1.3117	3.0909	2.5974	1.9481	0.3636	0.9481	8.5714
Devin Vassell	G-F	SAS	67	4.9254	11.2687	1.5821	1.9403	2.4776	4.0149	2.4925	0.8507	0.3731	0.8955	13.9104
Stephon Castle	G	SAS	68	5.6618	12.0147	4.1471	5.6471	1.1912	5.3235	7.3824	1.1471	0.2794	3.1912	16.6618
Derik Queen	C	NOP	81	4.3704	9.2346	2.6790	3.3704	0.2840	7.0617	3.6790	1.0370	0.9136	2.3457	11.7037
LaMelo Ball	G	CHA	72	7.0278	17.2778	2.2361	2.4861	3.7778	4.8194	7.1389	1.2083	0.2500	2.8056	20.0694
Naz Reid	F-C	MIN	77	5.1558	11.3117	1.1688	1.5974	2.1039	6.1948	2.2078	0.9870	1.0130	1.6364	13.5844
Davion Mitchell	G	MIA	70	3.7286	7.6143	0.6000	0.9286	1.2857	2.6714	6.4714	0.9714	0.1571	1.5429	9.3429
Bruce Brown	G-F	DEN	82	3.0244	6.3659	1.1707	1.5366	0.6951	3.8780	2.1220	1.0366	0.2439	1.1098	7.9146
Chet Holmgren	F-C	OKC	69	6.2899	11.2899	3.2609	4.1159	1.2754	8.8986	1.6667	0.6232	1.8986	1.5507	17.1159
Josh Hart	G	NYK	66	4.5606	8.9697	1.3636	1.8939	1.5152	7.3636	4.8030	1.1364	0.2576	1.9394	12.0000
LeBron James	F	LAL	60	7.8833	15.3167	3.8833	5.2667	1.2833	6.0667	7.2000	1.2000	0.5833	2.9833	20.9333
Ace Bailey	F	UTA	72	5.4861	12.3889	0.9167	1.2222	1.9028	4.1944	1.8194	0.8333	0.6806	1.4861	13.7917
Harrison Barnes	F	SAS	77	3.4026	7.4675	1.3247	1.5974	1.8052	2.7662	1.9221	0.6494	0.1558	0.7922	9.9351
Jalen Duren	C	DET	70	7.4571	11.4714	4.5857	6.1429	0.0000	10.5286	1.9714	0.8143	0.8429	1.8571	19.5000
Brandon Miller	F	CHA	65	6.9846	16.0615	3.0615	3.4308	3.1385	4.9077	3.3385	1.0154	0.6769	2.5077	20.1692
Maxime Raynaud	C	SAC	74	5.1757	9.0676	1.7838	2.2703	0.3243	7.5135	1.3514	0.5135	0.5270	1.2703	12.4595
Deandre Ayton	C	LAL	72	5.5972	8.3472	1.2639	1.9583	0.0000	7.9861	0.8194	0.5694	0.9722	1.1528	12.4583
Jarace Walker	F	IND	76	4.0395	9.6447	1.8026	2.4079	1.7500	5.1447	2.5263	0.8289	0.3421	1.8289	11.6316
Sam Hauser	F	BOS	78	3.2308	7.7051	0.2179	0.2564	2.5385	3.8462	1.4615	0.4872	0.2949	0.4487	9.2179
Rui Hachimura	F	LAL	68	4.5147	8.7794	0.7353	1.0588	1.7059	3.3235	0.8382	0.5735	0.3088	0.6176	11.4706
Neemias Queta	C	BOS	76	4.2895	6.5658	1.6184	2.3026	0.0132	8.3684	1.6579	0.7895	1.3158	0.9868	10.2105
Nic Claxton	C	BKN	69	4.8261	8.4493	2.0435	3.3188	0.0435	6.9275	3.6667	0.6522	1.1159	1.3913	11.7391
Myles Turner	F-C	MIL	71	4.0000	9.0986	1.8451	2.4930	2.0704	5.3239	1.4507	0.7042	1.6197	1.1690	11.9155
Keldon Johnson	G-F	SAS	82	5.0488	9.7195	1.8780	2.3659	1.2073	5.3659	1.3537	0.5610	0.1341	0.8537	13.1829
Anthony Black	G	ORL	64	5.3906	12.0625	2.6406	3.6094	1.5625	3.8125	3.6875	1.4219	0.7031	2.0625	14.9844
Tristan da Silva	F	ORL	77	3.6234	8.0519	1.0909	1.2338	1.5584	3.6883	1.6104	0.8831	0.3247	0.8961	9.8961
Moussa Diabaté	F	CHA	73	3.1370	4.9726	1.5890	2.4110	0.0137	8.6986	1.9315	0.7671	0.9863	1.0000	7.8767
Ausar Thompson	G-F	DET	73	4.1507	7.9041	1.4932	2.6164	0.0822	5.7260	3.1233	2.0000	0.9452	1.5068	9.8767
Nique Clifford	G	SAC	75	3.3200	7.9467	0.9333	1.2933	1.0267	3.7600	2.3867	0.9067	0.3200	1.5467	8.6000
Ayo Dosunmu	G	MIN	69	5.5942	10.8261	1.8406	2.1014	1.7826	3.4348	3.6087	0.8406	0.2609	1.3913	14.8116
John Collins	F-C	LAC	69	5.2754	9.5507	1.7101	2.2319	1.2899	5.3188	1.0145	0.8696	0.7391	1.3768	13.5507
Draymond Green	F	GSW	68	3.0147	7.2206	0.8676	1.2353	1.4853	5.5147	5.5294	0.8971	0.6176	2.6618	8.3824
Victor Wembanyama	F-C	SAS	64	8.6406	16.8750	5.8125	7.0312	1.9062	11.5000	3.1094	1.0312	3.0781	2.4219	25.0000
Russell Westbrook	G	SAC	64	5.5938	13.1094	1.9844	2.8594	2.0000	5.4375	6.6562	1.2969	0.1875	3.2656	15.1719
Jamal Shead	G	TOR	82	2.2561	6.1463	1.1098	1.4146	1.0244	1.7439	5.4146	0.9268	0.1829	1.3659	6.6463
Pelle Larsson	G	MIA	70	3.9571	7.9857	2.6286	3.3000	0.8857	3.4857	3.3714	0.7000	0.2286	1.4429	11.4286
Luguentz Dort	G	OKC	69	2.9275	7.5942	0.6377	0.8406	1.8551	3.5507	1.2319	0.8986	0.3913	0.7826	8.3478
Sion James	G	CHA	82	1.6829	4.5366	1.0000	1.1951	1.0122	3.5000	1.9878	0.6341	0.2439	0.7927	5.3780
Zion Williamson	F	NOP	62	7.8065	13.0161	5.3710	7.5000	0.0161	5.6935	3.2097	0.9677	0.5484	2.0323	21.0000
Jaylen Wells	F	MEM	69	4.3623	10.1159	1.9420	2.4783	1.8696	3.1884	1.6087	0.8841	0.1304	1.0725	12.5362
Nikola Vučević	C	BOS	64	6.1562	12.4844	1.2031	1.4531	1.5625	8.3906	3.3281	0.6094	0.6406	1.3281	15.0781
Oso Ighodaro	F	PHX	82	2.9146	4.4634	0.6463	1.4268	0.0000	5.0976	2.2561	0.8902	0.6951	1.2317	6.4756
Kyle Kuzma	F	MIL	69	4.9130	9.9855	1.9565	2.6957	1.2029	4.5362	2.6667	0.6667	0.4058	1.7246	12.9855
Kyle Filipowski	C	UTA	77	4.1948	8.5325	2.0649	2.7532	0.9610	7.1688	2.5714	0.9481	0.5065	1.6234	11.4156
Keyonte George	G	UTA	54	7.4444	16.3148	6.2407	7.0000	2.4815	3.6852	6.1481	1.0741	0.2593	3.0741	23.6111
Andrew Nembhard	G-F	IND	57	5.8421	13.2105	3.3860	4.1053	1.8772	2.8070	7.6667	0.8947	0.1404	2.4035	16.9474
Noah Clowney	F-C	BKN	66	3.7879	9.5606	2.7273	3.3939	1.9545	4.1364	1.6364	0.7576	0.6818	1.5455	12.2576
Jaylon Tyson	G-F	CLE	66	5.0000	10.1515	1.1970	1.6212	2.0152	5.1212	2.2273	0.8182	0.4242	1.5000	13.2121
Marcus Smart	G	LAL	62	3.0323	7.6774	1.7097	2.0806	1.5645	2.7742	2.9839	1.4032	0.3710	1.4839	9.3387
Brice Sensabaugh	F	UTA	75	5.2533	11.4267	2.2133	2.6800	2.1600	3.0800	1.9067	0.7200	0.1600	1.6533	14.8800
Austin Reaves	G	LAL	51	7.3137	14.9412	6.3333	7.2745	2.3137	4.7059	5.4902	1.0980	0.3922	2.9608	23.2745
Tre Jones	G	CHI	65	5.2462	9.4923	2.9385	3.4923	0.6308	3.1385	5.3846	1.1692	0.1692	1.4154	14.0615
Sandro Mamukelashvili	F-C	TOR	80	4.1250	7.8875	1.4750	1.9750	1.4375	4.9375	1.8875	0.8000	0.5125	0.8375	11.1625
Tobias Harris	F	DET	63	4.9048	10.4603	2.1587	2.4921	1.3016	5.1270	2.4921	0.9206	0.4127	1.0000	13.2698
Precious Achiuwa	F	SAC	73	4.3288	8.1918	1.0548	1.9041	0.3699	6.7397	1.3836	0.8904	0.6849	0.8904	10.0822
P.J. Washington	F	DAL	56	5.2857	11.7500	2.2321	3.2500	1.3571	7.0357	1.7500	0.9821	1.0714	1.6786	14.1607
Josh Giddey	G	CHI	54	5.9444	13.2778	3.2222	4.2222	1.9074	8.2963	9.1481	1.0185	0.5000	3.6296	17.0185
RJ Barrett	G-F	TOR	57	7.0351	14.3158	3.5088	4.8947	1.6842	5.2632	3.3158	0.7368	0.3333	1.7368	19.2632
Jay Huff	C	IND	82	3.5488	7.4512	1.0000	1.2073	1.4512	3.9512	1.4756	0.5000	1.8659	0.9268	9.5488
Norman Powell	G	MIA	58	7.2586	15.4310	4.5345	5.4828	2.6897	3.4828	2.4655	1.1034	0.2414	1.9138	21.7414
Cam Spencer	G	MEM	72	3.6528	7.7222	1.7500	1.8611	2.0278	2.5417	5.5556	0.7222	0.1528	1.2917	11.0833
Kel'el Ware	C	MIA	77	4.4675	8.4286	0.9221	1.2468	1.1948	9.0130	0.7273	0.7662	1.0909	0.8052	11.0519
Dillon Brooks	G-F	PHX	56	7.4464	17.1250	3.0357	3.6071	2.2679	3.6250	1.7679	1.0179	0.1607	1.7500	20.1964
Dennis Schröder	G	CLE	70	3.6429	8.9857	2.4429	2.9286	1.0714	2.7143	4.8714	0.7857	0.1571	1.7714	10.8000
Jerami Grant	F	POR	57	5.8070	12.8070	4.5965	5.6491	2.3860	3.4737	2.1404	0.7368	0.6316	2.0877	18.5965
Isaac Okoro	G-F	CHI	63	3.3492	7.2857	1.4127	1.7778	1.1587	2.7460	1.5873	0.7302	0.4603	0.7302	9.2698
Michael Porter Jr.	F	BKN	52	8.5385	18.4231	3.7500	4.3654	3.3846	7.0577	3.0385	1.0577	0.2500	2.3462	24.2115
Dominick Barlow	F	PHI	71	3.0282	5.6197	1.3239	1.8451	0.2958	4.7887	1.2254	0.8873	0.7465	0.7887	7.6761
Luke Kennard	G	LAL	78	3.0256	5.6795	0.8077	0.8846	1.5000	2.3333	2.2308	0.7051	0.1026	0.7692	8.3590
Cameron Johnson	F	DEN	54	4.2222	8.7963	1.7407	2.0741	2.0370	3.8148	2.4444	0.7407	0.3889	0.8704	12.2222
Brook Lopez	C	LAC	75	3.0933	7.2267	0.7467	0.9867	1.5200	3.6400	1.2933	0.6000	1.1600	0.8133	8.4533
Will Riley	F	WAS	74	3.6622	8.3514	1.9459	2.4324	1.0541	2.8649	1.9730	0.7297	0.1216	1.2703	10.3243
Cody Williams	F	UTA	67	3.5522	7.5970	1.2537	1.7761	0.4179	2.9552	2.0448	0.7612	0.3881	1.0896	8.7761
Bennedict Mathurin	G-F	LAC	54	5.3889	12.5370	5.4074	6.2222	1.3889	5.4259	2.3889	0.7963	0.2407	2.1852	17.5741
Bobby Portis Jr.	F	MIL	67	5.4627	11.1940	0.7164	1.0149	2.0149	6.3881	1.5672	0.6119	0.1791	0.9851	13.6567
Collin Sexton	G	CHI	68	5.2941	10.9118	3.2059	3.7500	1.6471	2.2794	3.2794	1.1176	0.1471	2.0735	15.4412
Cedric Coward	G	MEM	62	4.9194	10.4516	2.2581	2.6774	1.5000	5.8710	2.7581	0.6452	0.4355	1.6935	13.5968
Peyton Watson	G	DEN	54	5.2778	10.7593	2.5556	3.5000	1.5000	4.8889	2.0556	0.9259	1.1296	1.7222	14.6111
Herbert Jones	F	NOP	56	3.2679	8.5357	0.8929	1.1071	1.4286	3.3929	2.7679	1.5714	0.4821	1.3036	8.8571
Kelly Oubre Jr.	G-F	PHI	50	5.0800	10.8800	2.2200	2.9000	1.7200	5.0400	1.6000	1.3600	0.4800	1.3800	14.1000
Jalen Suggs	G	ORL	57	4.9474	11.3860	1.7544	2.0526	2.1404	3.8596	5.4737	1.8421	0.7018	2.6667	13.7895
Jordan Goodwin	G	PHX	70	3.2714	7.9286	0.5571	0.8000	1.6000	4.8714	2.1571	1.5143	0.2000	0.9714	8.7000
Jrue Holiday	G	POR	53	6.0377	13.3774	1.6604	1.9811	2.5849	4.6226	6.0755	1.0189	0.1132	2.7547	16.3208
Dylan Harper	G	SAS	69	4.7971	9.5072	1.3043	1.7246	0.8841	3.3913	3.8551	0.7826	0.3478	1.4348	11.7826
Ronald Holland II	F	DET	78	3.0128	6.9744	1.5385	1.9103	0.6410	4.0385	1.2179	1.1795	0.3462	1.2308	8.2051
Tari Eason	F	HOU	60	4.0167	9.6667	0.8667	1.1167	1.5500	6.2667	1.5000	1.2000	0.4667	1.3500	10.4500
Kevin Huerter	G-F	DET	69	3.8986	8.6377	0.7971	1.0290	1.4493	3.4783	2.5797	0.9130	0.4783	1.0435	10.0435
Moses Moody	G	GSW	60	4.0833	9.2833	1.4500	1.8833	2.5167	3.3333	1.6333	0.9667	0.5667	0.9500	12.1333
Terance Mann	G-F	BKN	63	2.6667	5.8413	1.0000	1.2698	0.8889	3.1746	3.0159	0.6508	0.2381	1.1270	7.2222
Jarrett Allen	C	CLE	56	6.0179	9.4286	3.3036	4.6607	0.0179	8.5000	1.7857	0.9821	0.8393	1.3393	15.3571
Isaiah Collier	G	UTA	59	4.3220	8.7288	2.5593	3.5424	0.4576	2.5424	7.1695	1.1186	0.3390	2.5085	11.6610
Isaiah Joe	G	OKC	71	3.4789	7.6479	1.5493	1.7324	2.5493	2.5493	1.3239	0.7042	0.1549	0.6338	11.0563
Jock Landale	C	ATL	68	4.1471	8.0588	1.1765	1.8529	1.0882	5.6912	1.6912	0.5147	0.5294	0.8824	10.5588
Zaccharie Risacher	F	ATL	67	3.6418	8.0000	0.8657	1.3433	1.4179	3.8358	1.1343	0.8806	0.5373	0.8657	9.5672
Klay Thompson	G	DAL	69	4.1449	10.5507	0.5217	0.6812	2.9275	2.0725	1.3913	0.4783	0.3188	0.9130	11.7391
Ryan Kalkbrenner	C	CHA	69	3.1739	4.2174	1.2029	1.6812	0.0000	5.4638	0.7826	0.4638	1.4638	0.8986	7.5507
Keon Ellis	G	CLE	72	2.3472	5.3333	0.7083	0.9722	1.2639	1.8750	1.0000	1.1528	0.6389	0.5694	6.6667
Patrick Williams	F	CHI	72	2.4722	6.6389	0.7500	1.0417	1.3194	2.9861	1.4583	0.6806	0.3611	1.0417	7.0139
Ja'Kobe Walter	G	TOR	72	2.5000	5.6111	0.9861	1.2500	1.5278	2.5694	1.1944	1.0139	0.1944	0.5139	7.5139
Ajay Mitchell	G	OKC	57	5.0175	10.3509	2.4737	2.8421	1.0702	3.3333	3.6140	1.2456	0.3158	1.4211	13.5789
Shaedon Sharpe	G	POR	50	7.8400	17.3600	3.1000	3.9400	2.0600	4.3400	2.6400	1.4000	0.1200	2.9400	20.8400
Bilal Coulibaly	G	WAS	56	4.1071	9.6607	2.3571	3.1607	1.1607	4.3393	2.5893	1.3393	0.9643	1.4464	11.7321
Grayson Allen	G	PHX	51	5.2745	13.0784	2.8235	3.2941	3.1176	3.0392	3.8235	1.3529	0.2549	1.6275	16.4902
Brandon Williams	G	DAL	66	4.4091	9.3485	3.6515	4.6061	0.5455	2.8939	3.8788	0.9394	0.3030	1.8939	13.0152
Daniss Jenkins	G	DET	72	3.2222	7.8889	1.8611	2.2361	1.0139	2.3056	3.8611	0.8889	0.2083	1.5972	9.3194
Jaren Jackson Jr.	F-C	UTA	48	7.1250	14.9792	3.3958	4.2292	1.7500	5.6667	1.9583	1.1042	1.4375	2.2292	19.3958
Ivica Zubac	C	IND	48	6.0000	10.0417	2.1042	2.9583	0.0000	10.6042	2.1875	0.3750	0.8333	1.7917	14.1042
Javonte Green	G	DET	82	2.2561	5.0488	1.2805	1.5244	1.1463	2.8049	0.6829	1.2439	0.3049	0.5488	6.9390
Tre Johnson	G	WAS	60	4.5167	10.7833	1.2667	1.4500	1.9167	2.8333	2.0333	0.5500	0.3000	1.5500	12.2167
Lauri Markkanen	F-C	UTA	42	9.1429	19.1667	5.7143	6.3810	2.7381	6.8810	2.1190	1.0000	0.5000	1.5000	26.7381
Khris Middleton	F	DAL	63	3.6032	8.5714	1.8889	2.1587	1.0635	3.6667	2.7937	0.6508	0.1429	1.7143	10.1587
Sidy Cissoko	G	POR	75	1.7733	4.4533	0.7733	1.1733	0.7867	2.2133	1.4667	0.6933	0.3333	0.9333	5.1067
Luke Kornet	F-C	SAS	68	2.5735	4.0000	1.3824	1.6765	0.0000	6.0882	1.8529	0.4706	0.9559	0.4118	6.5294
Baylor Scheierman	G	BOS	77	1.9610	4.3247	0.3636	0.4026	1.2597	3.4935	1.5065	0.5455	0.0779	0.5974	5.5455
Spencer Jones	F	DEN	64	2.0625	4.0938	0.4844	0.7969	0.8906	3.2656	0.7969	0.8125	0.4531	0.4844	5.5000
Mark Williams	C	PHX	60	4.7000	7.3000	2.2500	2.9167	0.0167	7.9500	0.9667	0.9167	0.9167	1.0833	11.6667
Aaron Wiggins	G	OKC	65	3.5538	8.2462	0.8154	1.1077	1.4769	3.0615	1.6769	0.9231	0.4308	1.2462	9.4000
Walter Clayton Jr.	G	MEM	69	2.5652	6.6812	1.5797	1.7536	1.0870	2.0145	4.0435	0.6377	0.2754	1.6812	7.7971
Christian Braun	G	DEN	44	4.5909	8.8409	1.7955	2.2955	0.9773	4.7955	2.7273	0.6818	0.2727	0.9773	11.9545
Gui Santos	F	GSW	68	3.3824	6.7647	1.2794	1.7647	1.1471	3.9118	2.3382	0.8971	0.3235	1.5000	9.1912
Vít Krejčí	G	POR	65	2.9538	6.6000	0.5846	0.7846	1.9692	2.2769	1.5385	0.7231	0.2769	0.6308	8.4615
Kyshawn George	F	WAS	48	5.2708	12.0208	2.1875	2.7292	2.0625	5.1042	4.4583	1.0000	0.8958	2.6042	14.7917
Ben Sheppard	G	IND	65	2.5846	5.9538	0.6000	0.7846	1.2923	3.0000	1.7538	0.6000	0.0923	0.6308	7.0615
Justin Champagnie	G-F	WAS	69	3.3623	6.6957	1.1594	1.4783	0.7681	5.5652	1.2174	0.8551	0.6232	0.6522	8.6522
Gary Trent Jr.	G	MIL	65	2.8154	7.2769	0.6154	0.8000	1.8923	1.0308	1.2154	0.5231	0.0462	0.6308	8.1385
Sam Merrill	G	CLE	52	4.2885	9.3077	1.1346	1.3269	3.0385	2.5577	2.4038	0.6346	0.1346	0.8077	12.7500
Will Richard	G	GSW	69	2.3478	5.0145	0.6667	0.7826	1.0290	2.4928	1.3478	1.1594	0.1304	0.7681	6.3913
Anfernee Simons	G	CHI	55	5.1818	11.7818	1.2545	1.4000	2.6545	2.4545	2.4364	0.4909	0.1455	1.2364	14.2727
Jose Alvarado	G	NYK	69	2.7101	6.5072	0.5072	0.6667	1.4638	2.4783	3.3913	0.9565	0.1159	1.1159	7.3913
Malik Monk	G	SAC	62	4.4677	10.1935	1.5161	1.7258	2.0000	1.8871	2.9839	0.5968	0.3871	1.4677	12.4516
Ryan Dunn	F	PHX	70	2.3429	5.1714	0.3286	0.6714	0.8000	4.2429	1.4571	0.8571	0.4143	0.8000	5.8143
Josh Okogie	G	HOU	78	1.6026	3.7692	0.5256	0.8846	0.7949	2.5641	0.8846	0.8077	0.2051	0.5256	4.5256
Kobe Sanders	G	LAC	68	2.6324	5.6471	0.8382	1.0147	1.1765	2.2500	1.6029	0.7206	0.1471	1.1912	7.2794
Derrick Jones Jr.	F	LAC	50	3.7800	7.5800	1.4200	1.8600	1.1000	3.4600	1.3800	0.9000	1.0000	0.8000	10.0800
Darius Garland	G	LAC	45	6.8667	14.9333	2.3333	2.7111	2.7111	2.3778	6.6889	0.9556	0.1778	2.8889	18.7778
Aaron Nesmith	G-F	IND	45	4.7111	11.3778	2.0444	2.4667	2.3333	4.1556	1.9111	0.5778	0.5111	1.4000	13.8000
Kris Murray	F	POR	57	2.2632	4.8421	0.6842	1.0000	0.5965	3.6140	1.4211	0.8947	0.3860	0.8246	5.8070
Stephen Curry	G	GSW	43	8.6977	18.5814	4.7442	5.1395	4.4186	3.5814	4.7209	1.1395	0.3953	2.8140	26.5581
Jordan Miller	G	LAC	60	3.5333	6.6500	2.2667	2.9167	0.6333	3.0333	2.2667	0.7500	0.1833	1.0500	9.9667
Drake Powell	G-F	BKN	63	2.2857	5.6825	1.0952	1.2222	0.8095	1.7619	1.4444	0.5714	0.2222	0.9524	6.4762
Jericho Sims	C	MIL	67	2.1642	2.7612	0.6567	1.0597	0.0000	5.5224	1.5522	0.2836	0.2537	1.1194	4.9851
Dean Wade	F-C	CLE	59	2.0678	4.7119	0.4576	0.6441	1.2034	4.1864	1.4915	0.7458	0.3898	0.2712	5.7966
Isaiah Stewart	F-C	DET	58	3.8103	6.9310	1.7069	2.2586	0.6897	5.0000	1.1379	0.2759	1.6034	1.1552	10.0172
Egor Dëmin	G	BKN	52	3.4423	8.6346	1.0385	1.2500	2.3846	3.1731	3.3269	0.8077	0.3269	1.6538	10.3077
Alex Sarr	C	WAS	48	6.5833	13.6667	2.1042	3.0417	1.0417	7.3958	2.7083	0.8125	1.9792	1.7083	16.3125
Yves Missi	C	NOP	66	2.4242	4.4545	0.8636	1.5455	0.0000	5.8333	1.2727	0.3030	1.5303	0.8182	5.7121
Nicolas Batum	G-F	LAC	74	1.2973	3.2162	0.1216	0.1486	1.2432	2.4865	0.8649	0.6351	0.3108	0.5000	3.9595
Ziaire Williams	F	BKN	56	3.2857	7.7321	2.1250	2.5000	1.5357	2.3929	1.0714	1.3571	0.3750	1.0536	10.2321
Jordan Clarkson	G	NYK	72	3.2917	7.3056	1.0833	1.3056	0.9722	1.7639	1.3333	0.4167	0.1250	0.8750	8.6389
Jaylin Williams	F	OKC	65	2.2923	5.4154	1.1231	1.4154	1.5077	5.5077	2.4308	0.4769	0.6462	1.0308	7.2154
Kevin Porter Jr.	G-F	MIL	38	6.2895	13.5263	3.6053	4.1053	1.2105	5.2105	7.3684	2.1842	0.5000	2.8947	17.3947
Coby White	G	CHA	50	5.5600	12.4600	3.9400	4.8200	2.3000	3.4000	4.0000	0.5000	0.1000	2.5600	17.3600
Collin Murray-Boyles	F	TOR	57	3.4737	6.0000	1.2456	1.8947	0.2982	5.0000	1.8596	0.9123	0.9298	1.0351	8.4912
Nolan Traore	G	BKN	56	3.2500	8.5536	1.3214	1.6786	1.0893	1.7679	3.8036	0.8036	0.4107	2.3036	8.9107
Adem Bona	F	PHI	71	1.8451	3.0986	1.1268	1.5915	0.0282	4.3239	0.4507	0.4225	1.1549	0.7887	4.8451
Andre Drummond	C	PHI	63	2.5238	5.3492	0.8413	1.3333	0.5079	8.4444	1.2698	0.6032	0.7778	0.9683	6.3968
Pat Spencer	G	GSW	66	2.8182	6.6061	0.6667	0.8636	0.9091	2.3939	3.5152	0.7273	0.0758	1.2879	7.2121
Zach LaVine	G	SAC	39	6.7179	14.0256	3.2051	3.6410	2.5385	2.7692	2.2821	0.7179	0.2564	1.8974	19.1795
Jordan Walsh	G	BOS	68	2.0000	3.9265	0.6471	0.8382	0.7059	4.0000	0.7941	0.7206	0.4559	0.5147	5.3529
Jaxson Hayes	F-C	LAL	66	3.0000	3.9697	1.4848	2.2727	0.0455	4.1061	0.8939	0.4394	0.7879	0.4697	7.5303
Marvin Bagley III	F	DAL	60	4.2333	6.8500	1.5833	2.4000	0.4000	6.1000	1.4167	0.4667	0.7167	1.0167	10.4500
Joel Embiid	F-C	PHI	38	8.9737	18.3421	7.5263	8.8158	1.3947	7.7105	3.9474	0.5526	1.2105	2.8684	26.8684
Santi Aldama	F-C	MEM	43	5.3023	11.0698	1.7674	2.6512	1.6279	6.6744	2.9302	0.8605	0.7209	1.2791	14.0000
Daniel Gafford	F-C	DAL	55	3.7636	5.7455	2.0000	2.9273	0.0000	6.8727	1.0545	0.7636	1.2545	1.1455	9.5273
Danny Wolf	F	BKN	57	3.1228	7.7193	1.4737	1.9123	1.1930	4.9298	2.2281	0.5263	0.5614	1.2807	8.9123
Jimmy Butler III	F	GSW	38	6.3158	12.1579	6.5263	7.5526	0.8421	5.5526	4.8684	1.4474	0.2105	1.5789	20.0000
Mouhamed Gueye	F	ATL	77	1.7013	3.7662	0.5195	0.8052	0.5195	3.5714	0.8961	0.7922	0.5065	0.4286	4.4416
GG Jackson	F	MEM	55	4.6364	9.3455	2.0364	2.7455	1.1636	4.2727	1.4727	0.5636	0.8000	1.5455	12.4727
Bones Hyland	G	MIN	71	2.9859	6.5915	0.9014	1.1549	1.6338	1.7887	2.6479	0.6197	0.2394	0.9859	8.5070
De'Andre Hunter	G-F	SAC	45	4.6222	11.1333	2.7556	3.1778	1.6667	4.1111	2.0000	0.6667	0.1333	1.6667	13.6667
Mitchell Robinson	F-C	NYK	60	2.4833	3.4333	0.7000	1.7167	0.0000	8.7500	0.8667	0.9333	1.1667	0.6833	5.6667
Simone Fontecchio	F	MIA	70	2.7714	6.7286	1.2286	1.4571	1.7571	3.0143	1.4429	0.5286	0.1286	0.7000	8.5286
Landry Shamet	G	NYK	51	3.1176	7.1373	1.0588	1.4902	2.0196	1.8039	1.4118	0.6078	0.1765	0.5882	9.3137
Ryan Nembhard	G	DAL	60	2.7000	6.5000	0.4167	0.5167	0.8000	2.2167	5.2667	0.4167	0.0333	1.4167	6.6167
Day'Ron Sharpe	C	BKN	62	3.4516	5.7419	1.6613	2.4516	0.1452	6.6613	2.3387	1.0806	0.4194	1.7097	8.7097
Jared McCain	G	OKC	67	3.0448	7.1642	0.7612	0.8806	1.4776	2.0448	1.3433	0.5224	0.0746	0.8358	8.3284
Quinten Post	C	GSW	67	2.8806	6.5522	0.5075	0.6418	1.3881	4.0149	1.4030	0.3881	0.5224	0.8358	7.6567
Svi Mykhailiuk	G-F	UTA	50	3.4600	7.2400	0.5000	0.5600	1.9600	2.4800	1.8600	0.5200	0.0600	0.8400	9.3800
Caris LeVert	G	DET	60	2.7167	6.5167	0.9500	1.4000	1.0000	1.9500	2.6500	0.8500	0.6833	1.3667	7.3833
Jakob Poeltl	C	TOR	46	4.7174	6.7391	1.2826	2.1304	0.0000	7.0217	2.0217	0.8696	0.6957	1.2391	10.7174
Craig Porter Jr.	G	CLE	64	1.8125	4.0312	0.3750	0.6250	0.5156	3.4375	3.2031	0.8906	0.5938	0.9219	4.5156
Gary Payton II	G	GSW	73	3.3151	5.6849	0.3562	0.5479	0.5068	3.6301	1.6849	0.9178	0.2877	0.8493	7.4932
Dru Smith	G	MIA	70	1.9286	4.6429	1.2429	1.5000	0.5429	2.4714	2.5714	1.4286	0.3000	0.7857	5.6429
Isaiah Hartenstein	F-C	OKC	47	3.9149	6.2979	1.3617	2.2340	0.0000	9.4255	3.5319	1.0213	0.7660	1.7447	9.1915
Paul George	F	PHI	37	6.0811	13.8649	2.4595	3.0000	2.7027	5.2973	3.5946	1.6757	0.4324	1.7297	17.3243
Jarred Vanderbilt	F	LAL	65	1.7385	3.6923	0.5077	0.8615	0.4462	4.4923	1.2923	0.8462	0.2923	0.8615	4.4308
De'Anthony Melton	G	GSW	49	4.3878	10.7755	2.0408	2.4694	1.4694	3.2245	2.5510	1.5714	0.4082	1.8571	12.2857
Taylor Hendricks	F	MEM	59	2.7627	6.0169	0.8305	1.2203	1.0678	3.7458	0.9322	0.8475	0.5085	0.8475	7.4237
Luka Garza	C	BOS	69	2.9855	5.1739	1.3043	1.6957	0.7971	4.0725	0.9565	0.3913	0.4058	0.6667	8.0725
John Konchar	G	UTA	56	1.6786	3.5536	0.5714	0.7857	0.5000	4.2857	2.1071	1.4821	0.6786	0.6250	4.4286
Ousmane Dieng	F	MIL	57	2.9474	6.9825	0.5614	0.7895	1.1053	3.1579	2.3860	0.4386	0.2982	1.3158	7.5614
Jalen Smith	F-C	CHI	53	3.6792	7.6226	1.3019	1.7547	1.5472	6.6981	1.2264	0.4528	0.7925	1.0000	10.2075
Kentavious Caldwell-Pope	G	MEM	51	2.9412	7.1765	1.4314	1.5686	1.0784	2.4510	2.7255	0.8431	0.1569	1.1961	8.3922
Hugo González	G	BOS	74	1.5811	3.3243	0.1757	0.3514	0.5676	3.3243	0.5405	0.5676	0.2838	0.5135	3.9054
Jusuf Nurkić	C	UTA	41	4.3902	8.7317	1.5122	2.7561	0.6098	10.3659	4.7805	1.2683	0.5366	2.4634	10.9024
Miles McBride	G	NYK	41	4.1707	9.8537	0.9024	1.1463	2.7317	2.4390	2.5854	0.8537	0.2195	0.8049	11.9756
Corey Kispert	F	ATL	58	3.1724	6.7931	1.3276	1.6724	1.5000	2.2759	1.5862	0.2759	0.1552	0.7759	9.1724
Rayan Rupert	G-F	MEM	64	1.9219	4.8906	0.6562	0.8438	0.6875	2.9375	1.0625	0.8594	0.1406	0.8594	5.1875
Gradey Dick	G-F	TOR	76	2.1184	5.0526	1.0132	1.1579	0.7368	1.9342	0.6711	0.5921	0.0921	0.5526	5.9868
Giannis Antetokounmpo	F	MIL	36	10.3611	16.6111	6.4444	9.9167	0.4167	9.7778	5.4444	0.9444	0.6667	3.1944	27.5833
Tyler Herro	G	MIA	33	7.4848	15.6061	3.0000	3.2727	2.5455	4.7879	4.1212	0.6970	0.3636	1.9394	20.5152
Jamir Watkins	F	WAS	50	2.6400	5.9200	1.1400	1.6400	0.9400	3.8800	1.3400	1.0800	0.5000	0.8000	7.3600
Alex Caruso	G	OKC	56	2.2679	5.3571	0.7321	0.9107	0.9464	2.7679	1.9643	1.2857	0.3036	0.8571	6.2143
Franz Wagner	F	ORL	34	7.1765	14.9118	4.7941	5.8235	1.4118	5.2059	3.3235	0.9412	0.3235	1.7059	20.5588
Caleb Love	G	POR	49	3.8776	10.0000	0.7347	1.0000	1.8980	2.3265	2.5102	0.5510	0.0816	1.2653	10.3878
Robert Williams III	F-C	POR	59	3.0000	4.2373	0.5763	0.9661	0.1525	7.0339	1.0169	0.5932	1.4746	0.8305	6.7288
Guerschon Yabusele	F	CHI	67	1.8657	4.6567	0.8060	1.0746	1.0299	3.4925	0.9403	0.3881	0.2388	0.6119	5.5672
Aaron Gordon	F	DEN	36	5.5000	11.0556	3.4722	4.5278	1.6944	5.8056	2.6667	0.5833	0.2778	1.0556	16.1667
Nae'Qwan Tomlin	F	CLE	64	2.3438	4.9062	0.7344	0.9531	0.4219	2.9062	0.8281	0.6406	0.5312	0.4531	5.8438
Mike Conley	G	MIN	54	1.3333	3.9815	0.8333	0.9259	1.0370	1.7222	2.9074	0.6481	0.2593	0.6296	4.5370
Olivier-Maxence Prosper	F	MEM	53	3.6226	6.6038	1.6226	2.1509	1.1321	3.5283	0.9623	0.7547	0.2453	0.8868	10.0000
Tyus Jones	G	DEN	67	1.1343	3.2537	0.1045	0.1343	0.5970	1.1343	2.3731	0.5522	0.0448	0.3731	2.9701
Justin Edwards	F	PHI	64	2.2344	5.0000	0.5156	0.6094	1.0469	1.4688	1.2500	0.8438	0.1719	0.5625	6.0312
Ochai Agbaji	G	BKN	62	1.9677	4.5161	0.5806	0.6935	0.5484	2.2742	0.7581	0.3871	0.2742	0.5000	5.0645
Goga Bitadze	F-C	ORL	64	2.3750	3.5156	1.0781	1.5156	0.0312	5.0000	1.3125	0.5781	0.9531	0.6719	5.8594
Rob Dillingham	G	CHI	65	2.5077	6.3385	0.6308	0.8462	0.6462	2.0154	2.2000	0.7231	0.0923	1.5385	6.2923
Al Horford	F-C	GSW	45	3.0667	7.2000	0.4889	0.5778	1.6444	4.8667	2.5778	0.6667	1.1333	0.9333	8.2667
T.J. McConnell	G	IND	56	4.3214	8.0357	0.4464	0.5179	0.2857	2.2143	5.1250	1.0000	0.1786	1.1429	9.3750
Kobe Brown	F	IND	61	2.0820	4.4426	0.7705	0.9672	0.8525	3.0328	1.3115	0.3770	0.1967	0.8689	5.7869
Anthony Gill	F	WAS	55	2.4545	3.9091	0.6182	0.8364	0.3091	2.8727	1.2545	0.5636	0.3818	0.6545	5.8364
Kasparas Jakučionis	G	MIA	53	1.9245	4.4906	1.0943	1.2453	1.2453	2.6038	2.5849	0.6226	0.1321	0.9434	6.1887
Jalen Williams	G-F	OKC	33	6.5455	13.5152	3.2727	3.9091	0.6970	4.6364	5.4848	1.1818	0.2727	1.9091	17.0606
Jordan Poole	G	NOP	39	4.2308	11.3846	2.5128	2.9231	2.4615	1.9744	3.0513	0.5641	0.3846	1.7692	13.4359
Clint Capela	C	HOU	75	1.5733	3.0267	0.6000	1.0400	0.0133	4.6000	0.6667	0.4800	0.8400	0.5467	3.7600
Ben Saraf	G	BKN	44	2.7273	6.8864	1.6591	2.0000	0.4318	2.0909	3.2955	0.8636	0.1818	2.2500	7.5455
Micah Peavy	G-F	NOP	61	1.7377	4.5082	0.3607	0.4754	0.4590	1.9180	1.0000	0.7049	0.1475	0.3279	4.2951
Karlo Matković	F-C	NOP	62	2.2903	3.7903	0.4839	0.6613	0.6129	3.6935	0.8065	0.4032	0.8710	0.5484	5.6774
Micah Potter	C	IND	47	3.2128	6.2340	1.8085	2.1489	1.5106	4.9574	1.4681	0.4681	0.3191	1.0000	9.7447
Isaiah Jackson	F	LAC	55	2.7455	4.3091	1.2364	1.9091	0.0000	5.2727	0.9455	0.6545	0.8909	1.0545	6.7273
Josh Green	G	CHA	58	1.4310	3.1207	0.4310	0.4828	1.0000	1.7586	0.8103	0.6034	0.1034	0.6034	4.2931
Dylan Cardwell	C	SAC	44	2.3864	4.0682	0.5909	1.1591	0.0455	7.5455	1.4091	0.6818	1.5227	0.8864	5.4091
Dwight Powell	F-C	DAL	63	1.0317	1.6032	1.2381	1.7778	0.0317	4.0794	1.0794	0.5397	0.3333	0.6508	3.3333
Paul Reed	F	DET	65	3.2154	5.2154	1.1231	1.6923	0.2000	4.5231	1.2308	0.8769	0.9231	0.8923	7.7538
Jaden Hardy	G	WAS	57	3.1930	7.5263	0.9649	1.3158	1.8246	1.5088	1.0175	0.2807	0.1053	1.0877	9.1754
Quenton Jackson	G	IND	49	3.1429	6.6939	2.0000	2.4286	0.8571	2.3265	2.8571	0.6327	0.1224	1.2653	9.1429
Jaylen Clark	G	MIN	68	1.4559	3.3529	0.6471	0.9853	0.4853	1.7647	0.6324	0.6912	0.1324	0.3382	4.0441
Cam Thomas	UTIL	MIL	42	4.5000	10.9762	3.2619	4.0238	1.2381	1.6905	2.5714	0.2143	0.0952	1.7619	13.5000
Bryce McGowens	G	NOP	42	2.7143	5.6429	1.7619	2.2619	0.9048	2.0952	1.4762	0.6429	0.1905	0.4048	8.0952
Gabe Vincent	G	ATL	53	1.4340	4.0755	0.3774	0.4340	1.1132	1.0189	1.4340	0.5094	0.0566	0.5094	4.3585
Jonas Valanciunas	C	DEN	65	3.4923	6.0000	1.6154	2.0923	0.1231	5.0615	1.2000	0.2154	0.5692	1.1231	8.7231
Jevon Carter	G	ORL	53	2.3208	5.7925	0.2830	0.3585	1.4717	1.6792	1.6415	0.7547	0.1887	0.6792	6.3962
Trendon Watford	G-F	PHI	53	2.6038	5.0566	1.1321	1.4528	0.1887	3.2830	2.4906	0.3019	0.4151	1.2075	6.5283
Julian Strawther	G	DEN	57	2.6140	5.5965	1.0000	1.2281	1.0175	2.0000	1.0702	0.4386	0.1053	0.6316	7.2456
Jalen Wilson	F	BKN	54	2.0370	5.1481	1.2778	1.7778	1.0000	2.1111	0.9259	0.4074	0.0370	0.7037	6.3519
Caleb Martin	F	DAL	58	1.4655	3.2586	0.6379	1.0517	0.3448	2.4655	1.3966	0.6897	0.2586	0.6897	3.9138
Kenrich Williams	G-F	OKC	56	2.5000	5.2857	0.5893	0.9286	0.8929	3.3393	1.4107	0.5536	0.0893	0.7679	6.4821
Kyle Anderson	G-F	MIN	43	2.4884	4.6977	1.1163	1.5814	0.0930	3.4884	2.9302	1.0465	0.6047	0.9070	6.1860
Daeqwon Plowden	G-F	SAC	32	3.7500	8.7188	1.3125	1.5312	1.9375	3.0312	1.2812	0.7188	0.2188	0.8125	10.7500
Josh Minott	F	BKN	49	2.5714	5.1429	0.9796	1.2449	1.3061	3.2245	0.9388	0.8980	0.4898	0.8776	7.4286
Jonathan Kuminga	F	ATL	36	4.3611	9.4167	2.4722	3.4167	0.9722	5.6111	2.3056	0.6111	0.3056	1.8611	12.1667
Javon Small	G	MEM	41	3.2683	7.0244	1.6585	2.0000	1.4634	3.1220	3.6829	0.8293	0.2195	1.3659	9.6585
Jalen Green	G	PHX	32	6.7500	16.0000	2.0312	2.7188	2.2188	3.6250	2.8438	1.0625	0.2500	2.2500	17.7500
Vince Williams Jr.	UTIL	UTA	40	2.5750	7.3000	0.9750	1.2500	1.3750	3.8750	4.1500	0.7250	0.4000	1.7500	7.5000
Buddy Hield	G	ATL	51	2.8039	6.4118	0.5882	0.7255	1.4510	2.2745	1.3922	0.7647	0.1765	0.9412	7.6471
Carter Bryant	F	SAS	71	1.5211	3.7324	0.4225	0.5915	0.7324	2.5070	0.6620	0.2254	0.3239	0.4648	4.1972
Nikola Jović	F	MIA	47	2.4468	6.6809	1.4681	2.1489	0.9574	3.2553	2.1702	0.5532	0.3617	1.3830	7.3191
Jalen Pickett	G	DEN	50	1.9600	4.6400	0.3000	0.3800	0.9800	2.2800	2.3000	0.2600	0.1000	0.6800	5.2000
Keegan Murray	F	SAC	23	5.4783	13.0435	1.6522	2.1304	1.3478	5.7391	1.6522	1.0435	1.5652	1.1739	13.9565
Aaron Holiday	G	HOU	57	1.8421	4.4211	0.6140	0.7193	1.1754	1.0175	1.0526	0.4737	0.0877	0.6667	5.4737
Tyrese Martin	F	PHI	46	2.3696	6.0217	0.4130	0.6087	1.1522	2.5217	1.6957	0.5435	0.1304	0.8696	6.3043
Kristaps Porziņģis	F-C	GSW	32	5.4062	12.1250	4.1562	4.9375	1.6875	5.2188	2.5312	0.5625	1.1875	1.2812	16.6562
Jabari Walker	F	PHI	64	1.5156	3.3281	0.7812	1.0625	0.5000	3.0000	0.5312	0.3594	0.2031	0.4219	4.3125
Noah Penda	G-F	ORL	59	1.3898	3.4407	0.5593	0.8136	0.5085	3.1695	1.1864	0.5085	0.3051	0.7119	3.8475
Scoot Henderson	G	POR	30	4.7667	11.4000	2.8000	3.3333	1.9000	2.6667	3.7333	0.9000	0.3000	2.4333	14.2333
Pete Nance	F	MIL	47	2.1277	4.1277	0.0851	0.2340	1.0638	2.6596	1.0213	0.2979	0.2979	0.5532	5.4043
Thomas Bryant	F-C	CLE	60	2.2500	4.4500	0.8833	1.1000	0.7833	3.4167	0.5500	0.3000	0.4333	0.4667	6.1667
Steven Adams	C	HOU	32	2.1875	4.3438	1.4688	2.5312	0.0000	8.6250	1.5000	0.6875	0.6250	1.0938	5.8438
Lonzo Ball	UTIL	CLE	35	1.5143	5.0286	0.4000	0.6000	1.1429	4.0000	3.8857	1.2857	0.2571	1.4857	4.5714
Tyler Kolek	G	NYK	62	1.7419	4.0000	0.2258	0.3226	0.7097	1.5806	2.7419	0.3548	0.0645	0.8871	4.4194
Leonard Miller	F	CHI	46	3.1522	5.6957	0.8478	1.1522	0.6957	3.9348	0.8696	0.3696	0.3261	0.7826	7.8478
Grant Williams	F	CHA	36	2.1667	5.0833	1.2222	1.5000	1.4444	3.9444	1.5556	0.4722	0.5000	0.8611	7.0000
Nick Richards	C	CHI	48	2.1458	4.1875	1.3542	2.1042	0.1042	5.0625	0.3333	0.2083	0.6667	1.2292	5.7500
Sharife Cooper	G	WAS	41	3.0976	6.0244	1.2195	1.4634	0.7073	2.0976	3.0244	0.3902	0.1220	1.4878	8.1220
Devin Carter	G	SAC	38	3.1842	7.6842	1.7632	2.4737	0.7895	3.2895	2.6579	0.8947	0.2368	1.4211	8.9211
Jett Howard	G	ORL	55	1.9091	4.5636	0.6727	0.7091	0.9818	1.5818	0.8182	0.1818	0.2000	0.2727	5.4727
Jordan Hawkins	G	NOP	51	1.8431	5.0392	0.4510	0.5294	0.9216	1.7255	0.8235	0.2549	0.1961	0.6667	5.0588
Rasheer Fleming	F	PHX	55	1.5818	3.9091	0.3455	0.6182	0.8182	2.2545	0.2909	0.4000	0.3818	0.5273	4.3273
Jahmai Mashack	G	MEM	31	2.4839	6.2903	0.6129	1.1290	0.6129	2.6129	2.2258	1.1935	0.4516	1.5806	6.1935
Tristan Vukcevic	F	WAS	49	3.1020	6.4694	1.7755	2.2653	1.0204	3.0000	1.1224	0.4694	0.6735	1.2653	9.0000
Jaden Ivey	UTIL	CHI	37	3.0811	6.9189	1.0270	1.2703	1.3514	2.4595	1.8378	0.6216	0.3784	1.1081	8.5405
Tre Mann	G	CHA	53	2.0566	5.7170	0.4340	0.5094	0.9434	1.7358	1.6226	0.4717	0.0943	1.0189	5.4906
Gary Harris	G	MIL	48	0.9583	2.1667	0.1667	0.1875	0.5833	1.2708	1.0625	0.5625	0.1875	0.3958	2.6667
Ethan Thompson	G	IND	32	2.3125	6.1250	1.1562	1.4688	1.1875	2.1562	1.7812	0.6250	0.2812	0.7812	6.9688
Jamaree Bouyea	G	PHX	46	2.3478	5.1304	0.5217	0.7609	0.5000	1.7826	1.7826	0.6304	0.3478	0.6522	5.7174
Johnny Furphy	G	IND	35	2.0000	4.2571	0.4857	1.0000	0.6571	4.3714	1.1714	0.5714	0.2000	0.8286	5.1429
Mohamed Diawara	F	NYK	69	1.3043	3.0870	0.2609	0.3478	0.6957	1.3623	0.7681	0.2464	0.0870	0.4348	3.5652
Zeke Nnaji	F-C	DEN	52	1.3654	2.9038	0.6538	0.8462	0.2692	2.6154	0.5577	0.3269	0.4615	0.5192	3.6538
Anthony Davis	F-C	WAS	20	8.4500	16.7000	2.9500	4.0500	0.5000	11.0500	2.8000	1.1000	1.6500	2.0500	20.3500
Dorian Finney-Smith	F	HOU	37	1.1892	3.5676	0.2162	0.2432	0.7297	2.5135	0.9730	0.4324	0.1622	0.5946	3.3243
Kam Jones	G	IND	37	1.8378	4.5676	0.1081	0.2162	0.5946	1.6216	3.2162	0.4865	0.0000	0.9730	4.3784
Kevin Love	F-C	UTA	37	2.1351	5.3784	1.0000	1.1892	1.4324	5.8108	1.8108	0.3784	0.2432	0.8108	6.7027
Taurean Prince	F	MIL	26	3.3077	7.3462	0.2308	0.2308	2.3462	3.1154	1.7692	0.6154	0.2308	1.1923	9.1923
Jase Richardson	G	ORL	54	1.6481	3.4815	0.6111	0.8519	0.5185	1.1667	1.0741	0.3519	0.0370	0.4630	4.4259
Tidjane Salaün	F	CHA	37	2.1622	4.2973	0.7027	1.0811	0.9730	4.0270	0.7297	0.4324	0.1892	0.7027	6.0000
Ja Morant	G	MEM	20	6.6000	16.1000	5.2500	5.8500	1.0000	3.2500	8.0500	1.0000	0.3000	3.5500	19.4500
Domantas Sabonis	F-C	SAC	19	6.3158	11.6316	2.9474	4.0526	0.2632	11.3684	4.1053	0.9474	0.2105	2.6842	15.8421
Dalton Knecht	F	LAL	54	1.6111	3.5370	0.2963	0.4074	0.7037	1.3889	0.3889	0.2037	0.2037	0.4259	4.2222
Drew Eubanks	F-C	SAC	42	2.2143	3.7143	0.7619	1.3571	0.0238	3.0238	0.4524	0.3571	0.6190	0.7143	5.2143
Dalen Terry	F	PHI	48	1.4167	3.2292	0.3333	0.6250	0.5000	1.7917	1.3750	0.6042	0.3125	0.5417	3.6667
Tyrese Proctor	G	CLE	50	1.8600	4.5000	0.8000	0.9000	0.9200	1.2800	1.4800	0.5000	0.0000	0.8000	5.4400
Elijah Harkless	G	UTA	26	2.0769	6.1923	1.8846	2.4615	0.8077	2.0385	2.8846	1.1923	0.2308	1.0000	6.8462
Terrence Shannon Jr	G-F	MIN	43	1.7907	3.9767	1.3023	1.6279	0.7209	1.1395	0.8837	0.3256	0.0233	0.5581	5.6047
Keaton Wallace	G	ATL	53	1.2830	3.2264	0.2264	0.3396	0.7170	1.0566	1.7736	0.4528	0.0943	0.5283	3.5094
Moussa Cisse	C	DAL	38	1.7368	3.0263	1.0000	2.1316	0.0000	5.6842	0.2368	0.5000	1.1842	0.7895	4.4737
Cole Anthony	UTIL	MIL	35	2.8000	6.6000	0.4571	0.7429	0.6286	2.4857	3.4571	0.5714	0.3143	1.8571	6.6857
Jayson Tatum	G-F	BOS	16	7.3750	17.9375	4.0625	4.9375	2.9375	10.0000	5.3125	1.3750	0.1875	2.4375	21.7500
Jonathan Isaac	F	ORL	52	0.8846	2.0962	0.6731	1.1154	0.1346	2.5385	0.4231	0.3846	0.5962	0.3462	2.5769
Jamison Battle	F	TOR	61	1.2131	2.3770	0.1475	0.2295	0.5738	1.4590	0.3934	0.1148	0.0492	0.3115	3.1475
Asa Newell	F	ATL	44	2.0682	3.8409	0.3636	0.6591	0.6591	2.1818	0.5909	0.3636	0.3409	0.5227	5.1591
Ariel Hukporti	C	NYK	54	0.8333	1.4815	0.4815	0.6111	0.0185	2.9444	0.5370	0.1667	0.4815	0.5926	2.1667
Trayce Jackson-Davis	F	TOR	53	1.3396	2.3396	0.7170	1.3962	0.0189	2.7358	0.7358	0.3208	0.4528	0.4340	3.4151
D'Angelo Russell	G	WAS	26	3.6154	8.9231	1.6538	2.3077	1.2692	2.3462	4.0000	0.4615	0.0769	1.8846	10.1538
Taelon Peter	G	IND	38	1.5789	4.1579	0.2632	0.4211	1.0789	1.5526	1.0526	0.6842	0.1053	0.7105	4.5000
Bez Mbeng	G	UTA	15	3.3333	6.6667	1.0667	1.6667	0.4000	3.8000	4.1333	2.2667	0.3333	1.4667	8.1333
Amir Coffey	G-F	PHX	46	1.1957	2.4565	0.4783	0.6087	0.3696	1.2391	0.5870	0.1957	0.1087	0.1957	3.2391
Branden Carlson	C	OKC	42	2.3333	4.4286	0.3571	0.6190	0.7381	2.9524	0.6905	0.1905	0.6429	0.3810	5.7619
Jamal Cain	F	ORL	40	1.8500	3.8000	0.9750	1.1500	0.7000	1.9500	0.7250	0.2750	0.1000	0.4750	5.3750
Cam Christie	G	LAC	55	0.9636	2.5273	0.6909	0.9091	0.2182	1.4182	0.5636	0.2909	0.0545	0.4000	2.8364
Matisse Thybulle	G-F	POR	30	1.9333	4.4667	0.7000	0.8333	1.2333	1.9667	0.9000	2.0000	0.5333	0.9333	5.8000
Jeremy Sochan	F	NYK	44	1.4545	2.9318	0.4318	0.5909	0.2500	2.4318	0.9091	0.3864	0.1591	0.4773	3.5909
Maxi Kleber	F	LAL	43	0.7674	1.6977	0.3256	0.6047	0.1395	1.9535	0.6279	0.4186	0.2558	0.3953	2.0000
Marcus Sasser	G	DET	38	1.8158	4.6579	0.3947	0.4737	1.1579	1.0263	1.9737	0.5263	0.1053	0.8158	5.1842
AJ Johnson	G	DAL	48	1.1875	3.6667	0.7083	0.8333	0.2500	1.1458	0.9792	0.2292	0.0625	0.6250	3.3333
Bogdan Bogdanović	G	LAC	23	2.5652	6.6087	0.8696	1.0870	1.4348	2.5652	2.2174	0.4348	0.1304	1.1739	7.4348
Tony Bradley	F-C	ATL	41	1.5610	2.8537	0.7805	1.0488	0.0488	2.8293	0.5366	0.1463	0.1707	0.4390	3.9512
Larry Nance Jr.	F-C	CLE	35	1.4857	3.5429	0.1714	0.3714	0.6000	2.6571	0.9714	0.6000	0.1714	0.5714	3.7429
Oscar Tshiebwe	F-C	UTA	27	3.4074	5.3704	1.0000	1.8148	0.0000	6.6296	1.1852	0.5926	0.1852	0.7778	7.8148
Doug McDermott	F	SAC	29	1.8621	4.7931	0.2759	0.3103	1.6552	1.2759	0.7586	0.2069	0.1379	0.4483	5.6552
Leaky Black	F	WAS	15	2.6667	6.8667	0.4667	0.5333	1.2667	5.0000	1.4667	1.2000	0.5333	0.9333	7.0667
Julian Phillips	F	MIN	48	1.0625	2.5208	0.3750	0.4792	0.4167	1.0208	0.2083	0.4583	0.1458	0.2292	2.9167
Moritz Wagner	F-C	ORL	36	2.3056	5.4167	1.6389	2.0000	0.6111	3.1944	0.7500	0.4167	0.0556	0.5278	6.8611
Obi Toppin	F	IND	24	4.1667	8.2917	1.7500	1.9167	1.5417	4.4167	2.2917	0.5000	0.0417	1.1667	11.6250
Yanic Konan Niederhäuser	C	LAC	41	1.5610	2.4390	1.1463	1.5122	0.0244	2.8537	0.2927	0.1220	0.7317	0.5610	4.2927
Khaman Maluach	C	PHX	46	1.2174	2.2826	0.4783	0.6739	0.1087	2.8913	0.1304	0.1087	0.7174	0.5435	3.0217
Myron Gardner	F	MIA	45	1.3111	2.7333	0.4222	0.5778	0.5778	2.7111	0.9778	0.4444	0.1778	0.3778	3.6222
Andre Jackson Jr.	G	MIL	48	0.8125	2.4792	0.4167	0.6667	0.3125	1.5000	0.8958	0.3958	0.0833	0.6458	2.3542
Killian Hayes	G	SAC	23	1.8261	6.0000	1.1304	1.2174	0.7391	2.3478	3.5217	0.9130	0.1739	1.2174	5.5217
Jae'Sean Tate	F	HOU	46	1.2174	2.3696	0.1522	0.2391	0.2174	1.5870	0.5435	0.2174	0.0652	0.3478	2.8043
Julian Reese	F	WAS	13	4.8462	9.1538	2.1538	3.3846	0.0000	10.5385	1.8462	1.3846	0.6154	2.4615	11.8462
Dejounte Murray	G	NOP	14	6.2857	13.0000	2.7857	3.2143	1.3571	5.3571	6.3571	1.6429	0.2143	3.3571	16.7143
Trae Young	G	WAS	15	5.4000	11.8000	5.3333	6.4667	1.8000	2.0000	8.0000	0.8667	0.1333	2.6000	17.9333
Tyson Etienne	G	BKN	24	2.4167	6.0417	1.2500	1.5000	1.7917	1.1250	1.6667	0.4583	0.0000	0.6667	7.8750
Bronny James	G	LAL	42	1.1190	2.7381	0.1429	0.1667	0.5238	0.5476	1.2143	0.4524	0.0952	0.5952	2.9048
Nick Smith Jr.	G	LAL	30	2.3333	5.3667	0.3667	0.5000	1.1333	0.7667	1.0333	0.3000	0.0667	0.7000	6.1667
Cameron Payne	UTIL	PHI	22	2.5455	6.7727	0.8636	1.0000	1.4545	2.0000	2.6364	1.0909	0.3182	0.5000	7.4091
Liam McNeeley	F	CHA	31	1.2258	3.0645	1.0323	1.2581	0.7742	2.3548	0.7742	0.1613	0.0968	0.4194	4.2581
Blake Wesley	G	POR	31	1.8065	4.0000	0.8387	1.5161	0.3226	1.2581	2.0000	0.5161	0.1613	1.0000	4.7742
Jeremiah Robinson-Earl	UTIL	DAL	22	1.6818	4.5455	0.7727	0.8182	0.4545	4.6818	0.6818	0.4091	0.0455	0.5455	4.5909
Kelly Olynyk	F-C	SAS	42	1.1190	2.2857	0.6429	0.9048	0.2857	1.8095	1.1667	0.3571	0.0714	0.5714	3.1667
Malachi Smith	G	BKN	15	3.2000	6.6000	0.5333	0.5333	1.3333	3.4000	3.2667	0.8000	0.2667	0.9333	8.2667
Christian Koloko	C	ATL	27	1.1481	2.7407	0.3704	0.5556	0.0370	3.0000	0.6667	0.5185	0.8519	0.5926	2.7037
Kennedy Chandler	UTIL	UTA	11	5.5455	12.4545	2.5455	3.9091	1.3636	3.3636	6.7273	1.0000	0.1818	2.0909	15.0000
Cam Whitmore	F	WAS	21	3.6667	8.0476	1.0952	1.4762	0.7619	2.7619	0.7143	0.7143	0.3810	0.8571	9.1905
E.J. Liddell	F	BKN	26	2.0769	4.2692	0.8077	1.0000	0.6923	2.6538	0.9231	0.2308	0.3846	0.5769	5.6538
Brooks Barnhizer	G	OKC	40	0.6750	1.7750	0.1750	0.2500	0.1750	1.9500	0.5500	0.2750	0.1250	0.2500	1.7000
Chaney Johnson	G-F	BKN	17	3.0000	5.5294	1.6471	2.0588	0.5294	4.6471	2.1176	0.8824	0.5294	1.1765	8.1765
Isaiah Livers	F	PHX	36	0.6111	1.7778	0.2500	0.3333	0.3333	1.6944	0.5833	0.3611	0.2222	0.3333	1.8056
Lachlan Olbrich	C	CHI	37	0.9730	2.0811	0.4054	1.0811	0.0541	3.0270	1.1351	0.3243	0.2162	0.4324	2.4054
Ty Jerome	G-F	MEM	15	6.8000	14.3333	3.2667	3.7333	2.8000	2.8000	5.6667	1.0667	0.2667	1.8000	19.6667
Ron Harper Jr.	G-F	BOS	29	1.5862	3.7931	0.1034	0.1379	0.9655	1.6552	0.7586	0.3448	0.3103	0.2759	4.2414
Joan Beringer	F	MIN	40	1.6250	2.4500	0.6500	0.9250	0.0000	2.3000	0.3000	0.1750	0.6500	0.2750	3.9000
Jalen Slawson	F	IND	13	2.6154	5.9231	0.9231	1.3077	1.1538	4.3846	2.7692	1.5385	1.0769	1.3077	7.3077
Kevon Looney	F	NOP	21	1.1905	2.8571	0.3333	0.4762	0.0952	5.6190	1.6190	0.4286	0.4762	0.4286	2.8095
Tyler Burton	UTIL	MEM	12	3.5000	9.3333	1.9167	2.2500	1.8333	4.1667	1.0000	0.9167	0.3333	0.9167	10.7500
Yang Hansen	C	POR	43	0.7209	2.3256	0.6512	0.7907	0.1163	1.5349	0.5116	0.0698	0.2326	0.7442	2.2093
Pat Connaughton	G	CHA	42	0.9048	2.0238	0.3095	0.4762	0.5000	1.4524	0.4286	0.2619	0.0238	0.2619	2.6190
LJ Cryer	G	GSW	18	2.6111	6.5000	0.9444	1.0556	2.0556	1.5556	1.0000	0.1667	0.0000	0.7778	8.2222
Max Strus	G-F	CLE	12	3.9167	8.8333	0.5833	0.7500	2.7500	5.4167	2.0000	0.2500	0.0000	0.7500	11.1667
Blake Hinson	F	UTA	14	4.2143	8.2143	0.8571	1.1429	2.6429	2.3571	1.1429	0.3571	0.1429	0.3571	11.9286
Lindy Waters III	F	SAS	40	0.8250	2.2000	0.0500	0.0750	0.6750	0.7250	0.4750	0.1500	0.0250	0.1750	2.3750
Zach Edey	C	MEM	11	5.6364	8.9091	2.2727	2.9091	0.0909	11.0909	1.0909	0.6364	1.9091	2.3636	13.6364
Jordan McLaughlin	G	SAS	44	0.7500	1.7955	0.1364	0.1591	0.3864	0.7273	0.8636	0.4545	0.0455	0.3409	2.0227
Keshad Johnson	F	MIA	32	1.5938	3.2500	0.6562	0.9062	0.4062	1.9375	0.2500	0.4688	0.2500	0.4062	4.2500
Malevy Leons	F	GSW	25	1.2800	2.8800	0.6000	0.7600	0.1600	2.1200	0.9200	0.5600	0.3600	0.5200	3.3200
Malaki Branham	UTIL	WAS	28	1.5357	3.2500	1.0000	1.2143	0.5000	1.5714	0.8214	0.3571	0.1071	0.5357	4.5714
Cormac Ryan	G	MIL	11	4.8182	9.2727	2.1818	2.3636	2.4545	2.5455	1.7273	1.0000	0.2727	1.5455	14.2727
Chaz Lanier	G	DET	34	0.8235	2.6176	0.1471	0.1765	0.5588	0.6765	0.5000	0.2059	0.0000	0.0882	2.3529
Duop Reath	UTIL	POR	32	1.0312	2.3125	0.1562	0.1875	0.7188	1.1875	0.3125	0.1562	0.2500	0.2188	2.9375
KJ Simpson	G	DEN	20	1.7000	4.9500	0.5500	0.9000	0.6000	1.8500	2.1500	0.8000	0.0000	1.2500	4.5500
John Poulakidas	G	DAL	13	3.0000	7.0000	0.4615	0.5385	2.3846	2.3077	0.8462	0.5385	0.3077	0.3077	8.8462
Jonathan Mogbo	F	TOR	40	0.6500	1.1000	0.2250	0.3500	0.0000	1.7000	0.6250	0.2750	0.2000	0.3500	1.5250
Adama Bal	UTIL	MEM	8	3.5000	8.5000	1.1250	1.2500	2.2500	3.1250	2.3750	1.1250	0.1250	0.7500	10.3750
Nate Williams	G	GSW	14	3.0714	6.2857	0.9286	1.0714	0.9286	2.1429	1.0000	0.3571	0.0000	1.0714	8.0000
Drew Timme	F	LAL	27	1.4074	2.4444	0.1852	0.3333	0.4074	1.2222	0.8519	0.2222	0.0000	0.4815	3.4074
DeJon Jarreau	UTIL	MEM	11	3.0000	7.5455	1.6364	2.0909	0.3636	4.6364	3.2727	0.9091	0.7273	2.3636	8.0000
Chris Paul	UTIL	LAC	16	1.1250	3.5000	0.0625	0.1250	0.6250	1.7500	3.3125	0.6875	0.0000	1.0000	2.9375
A.J. Lawson	G	TOR	24	1.4167	3.2500	0.5833	0.7500	0.7917	1.7917	0.2917	0.5000	0.1667	0.2500	4.2083
Lucas Williamson	UTIL	MEM	7	3.5714	10.7143	1.1429	1.1429	2.1429	5.4286	2.5714	1.5714	0.1429	1.4286	10.4286
JD Davison	G	HOU	28	0.8571	2.2857	0.4643	0.6786	0.2857	1.2143	1.2500	0.2143	0.2143	0.2500	2.4643
Toby Okani	UTIL	MEM	6	4.1667	11.6667	0.1667	1.3333	1.5000	3.5000	1.0000	0.6667	0.5000	0.5000	10.0000
PJ Hall	C	CHA	19	1.5789	3.1053	1.2105	1.5263	0.1579	3.9474	0.5263	0.1579	0.5263	0.8421	4.5263
Scotty Pippen Jr.	G	MEM	10	4.3000	9.6000	1.8000	2.3000	1.0000	2.2000	4.7000	1.9000	0.4000	2.7000	11.4000
DaRon Holmes II	F	DEN	25	1.2400	2.4400	0.4400	0.5600	0.8000	1.4000	0.6400	0.0400	0.2400	0.4800	3.7200
Yuki Kawamura	G	CHI	18	0.9444	2.8889	0.9444	1.0556	0.6111	1.8333	2.6111	0.5000	0.0000	0.7778	3.4444
RayJ Dennis	G	ATL	17	1.5294	4.8824	0.4706	0.5294	0.8235	1.5294	1.9412	0.2353	0.2353	1.0000	4.3529
DeAndre Jordan	C	NOP	12	1.7500	2.6667	0.9167	1.4167	0.0000	6.3333	0.9167	0.3333	0.8333	0.7500	4.4167
MarJon Beauchamp	F	PHI	14	2.3571	5.4286	0.9286	1.3571	1.1429	2.2857	1.0714	0.6429	0.2857	0.9286	6.7857
Garrison Mathews	UTIL	IND	15	1.4000	3.4667	1.2667	1.5333	1.1333	1.1333	0.6667	0.4000	0.2000	0.2000	5.2000
Nigel Hayes-Davis	UTIL	PHX	27	0.5556	1.7037	0.0741	0.1481	0.0741	1.2222	0.2593	0.2963	0.0741	0.2963	1.2593
Chris Youngblood	G	POR	34	0.6471	1.8529	0.2647	0.2941	0.5294	0.8235	0.3529	0.1176	0.0882	0.0588	2.0882
Xavier Tillman	F	CHA	30	0.6667	1.5333	0.0333	0.1333	0.1000	1.4333	0.4333	0.3000	0.1667	0.2667	1.4667
Zach Collins	F-C	CHI	10	3.7000	6.4000	1.4000	2.0000	0.9000	5.6000	1.5000	0.2000	0.4000	1.0000	9.7000
Dariq Whitehead	UTIL	MEM	6	6.1667	15.5000	0.8333	1.1667	3.1667	4.0000	1.5000	0.6667	0.0000	1.6667	16.3333
Trevon Scott	UTIL	BKN	6	3.1667	8.3333	0.6667	1.0000	1.0000	5.1667	1.6667	1.3333	0.8333	0.6667	8.0000
Jeff Green	F	HOU	30	0.8333	2.2667	0.1667	0.1667	0.4000	0.8333	0.2667	0.0667	0.0667	0.1333	2.2333
Mason Plumlee	F-C	SAS	20	0.5500	0.7000	0.4500	0.7000	0.0000	2.6500	1.0000	0.5000	0.0000	0.6000	1.5500
Andersson Garcia	UTIL	UTA	5	1.8000	5.8000	1.4000	1.8000	0.2000	8.4000	2.8000	1.6000	0.8000	1.4000	5.2000
Tyler Smith	F	DAL	12	1.7500	4.1667	0.3333	0.5000	0.8333	2.7500	0.4167	0.5000	0.0000	0.5000	4.6667
Hunter Tyson	UTIL	DEN	21	0.6667	2.4762	0.5238	0.6190	0.3333	1.7143	0.8095	0.0952	0.0476	0.2381	2.1905
Nikola Topić	G	OKC	10	2.2000	5.1000	0.2000	0.5000	0.6000	1.9000	4.4000	0.5000	0.0000	2.5000	5.2000
Kevin McCullar Jr.	G	NYK	21	0.9524	2.2381	0.0952	0.2381	0.3810	1.3333	0.9524	0.4286	0.0000	0.5714	2.3810
Walker Kessler	C	UTA	5	5.2000	7.4000	2.8000	4.0000	1.2000	10.8000	3.0000	1.4000	1.8000	3.2000	14.4000
Charles Bassey	F-C	GSW	13	2.3846	3.6154	1.0769	1.5385	0.0000	4.3846	0.5385	0.3077	0.8462	0.4615	5.8462
Joe Ingles	G-F	MIN	27	0.5926	1.0000	0.0741	0.0741	0.2593	0.7037	1.2593	0.3333	0.0741	0.5185	1.5185
Adou Thiero	G	LAL	25	0.6400	1.2400	0.5600	0.8800	0.0400	1.1200	0.4000	0.2800	0.0800	0.3600	1.8800
Thanasis Antetokounmpo	F	MIL	34	0.4412	0.9412	0.5000	0.8824	0.0000	0.8824	0.4412	0.3824	0.1765	0.2941	1.3824
Amari Williams	F-C	BOS	22	0.4545	0.9091	0.4545	0.6364	0.0000	1.8182	0.4545	0.1364	0.4545	0.5000	1.3636
Alijah Martin	G	TOR	23	0.6957	2.1739	0.6522	0.8261	0.1739	0.9130	0.5217	0.3478	0.1739	0.3478	2.2174
Bismack Biyombo	C	SAS	25	0.3600	0.6000	0.2000	0.2800	0.0000	1.0400	0.2000	0.1600	0.0800	0.3200	0.9200
Tolu Smith	F	DET	15	1.2000	2.4000	1.3333	2.0000	0.0000	3.2667	0.8667	0.1333	0.4000	0.4667	3.7333
Pacôme Dadiet	F	NYK	29	0.5517	1.6552	0.3103	0.3793	0.2414	0.9310	0.4138	0.1034	0.0345	0.1034	1.6552
Mac McClung	G	CHI	11	2.0909	5.3636	1.2727	1.6364	0.6364	0.9091	0.9091	1.0000	0.2727	0.8182	6.0909
Miles Kelly	UTIL	DAL	14	1.1429	3.1429	0.0714	0.0714	0.7143	1.7143	0.8571	0.2857	0.0714	0.4286	3.0714
Seth Curry	G	GSW	10	2.4000	4.9000	1.1000	1.2000	1.2000	1.2000	1.0000	0.3000	0.0000	0.2000	7.1000
Bradley Beal	G	LAC	6	3.0000	8.0000	1.0000	1.3333	1.1667	0.8333	1.6667	0.5000	0.0000	1.5000	8.1667
Kobe Bufkin	UTIL	LAL	16	0.9375	3.1250	0.6875	0.7500	0.3125	0.8125	0.6250	0.1250	0.1875	0.3125	2.8750
Kyle Lowry	G	PHI	14	0.2857	1.7857	0.3571	0.4286	0.2857	0.6429	0.7857	0.1429	0.1429	0.2857	1.2143
Dereck Lively II	C	DAL	7	1.5714	2.5714	1.1429	1.4286	0.0000	5.2857	1.8571	0.5714	1.5714	1.4286	4.2857
Trey Alexander	G	NOP	9	2.1111	4.1111	0.3333	0.5556	0.6667	1.2222	1.0000	0.6667	0.2222	0.3333	5.2222
Omer Yurtseven	UTIL	GSW	9	1.2222	2.8889	1.3333	2.0000	0.0000	3.3333	0.8889	0.2222	0.1111	1.0000	3.7778
Luke Travers	UTIL	CLE	12	0.7500	3.0000	0.7500	0.7500	0.0833	2.0000	0.7500	0.5833	0.3333	0.2500	2.3333
Patrick Baldwin Jr.	F	SAC	9	1.2222	3.0000	0.5556	0.7778	0.6667	2.5556	0.5556	0.3333	0.5556	0.5556	3.6667
Alondes Williams	UTIL	WAS	4	4.0000	6.5000	1.7500	2.0000	1.2500	6.2500	3.0000	0.7500	0.7500	1.2500	11.0000
Taj Gibson	F	MEM	10	1.2000	2.6000	0.7000	0.8000	0.3000	2.7000	0.6000	0.2000	0.1000	0.1000	3.4000
Chris Boucher	UTIL	BOS	9	0.8889	2.7778	0.3333	0.4444	0.2222	2.0000	0.3333	0.5556	0.7778	0.1111	2.3333
Javonte Cooke	UTIL	POR	19	0.4737	2.1579	0.1053	0.1053	0.1579	1.0000	0.3684	0.3158	0.0000	0.4737	1.2105
Isaiah Crawford	F	HOU	14	0.7143	1.7143	0.3571	0.4286	0.2143	1.1429	0.3571	0.1429	0.2143	0.2857	2.0000
Haywood Highsmith	F	PHX	7	1.7143	3.2857	0.8571	1.0000	1.1429	1.8571	1.0000	0.5714	0.0000	0.0000	5.4286
TyTy Washington Jr.	G	LAC	16	0.5625	1.1875	0.0000	0.0000	0.1875	0.3750	1.0625	0.3750	0.1875	0.1875	1.3125
Curtis Jones	G	DEN	10	1.1000	2.7000	0.1000	0.2000	0.6000	1.1000	1.0000	0.4000	0.0000	0.2000	2.9000
Johnny Juzang	UTIL	MIN	21	0.7619	1.8095	0.2381	0.3810	0.2857	0.8095	0.2857	0.0952	0.0476	0.2381	2.0476
Tristan Enaruna	F	CLE	9	1.5556	3.1111	0.6667	0.8889	0.3333	1.5556	0.7778	0.5556	0.0000	0.3333	4.1111
Koby Brea	G	PHX	12	1.2500	3.0000	0.1667	0.1667	1.0833	0.6667	0.8333	0.0833	0.0000	0.3333	3.7500
Josh Oduro	C	NOP	3	3.6667	5.6667	1.0000	2.0000	0.0000	7.6667	1.3333	0.0000	0.0000	0.6667	8.3333
Trey Jemison III	C	NYK	13	0.4615	0.7692	0.0769	0.0769	0.0000	1.3846	0.3846	0.0769	0.1538	0.7692	1.0000
Caleb Houstan	UTIL	ATL	18	0.7778	1.4444	0.1667	0.1667	0.6111	0.6111	0.2222	0.1111	0.0556	0.2222	2.3333
Keshon Gilbert	G	ATL	4	1.2500	3.0000	1.7500	1.7500	0.0000	1.2500	3.0000	0.7500	1.0000	2.0000	4.2500
Eric Gordon	UTIL	PHI	6	2.0000	3.5000	0.1667	0.3333	1.3333	0.3333	0.5000	0.6667	0.1667	0.5000	5.5000
David Roddy	F	DEN	5	3.0000	6.0000	1.4000	1.4000	0.6000	4.0000	0.8000	0.8000	0.2000	0.4000	8.0000
Garrett Temple	G-F	TOR	22	0.1818	0.6818	0.2727	0.4091	0.1364	0.4091	0.4091	0.1818	0.0909	0.3182	0.7727
Bobi Klintman	UTIL	DET	12	0.7500	2.2500	0.0000	0.2500	0.3333	1.5833	0.5000	0.2500	0.0000	0.5000	1.8333
David Jones Garcia	G	SAS	11	1.1818	2.2727	0.2727	0.5455	0.2727	1.1818	1.6364	0.5455	0.0909	0.4545	2.9091
Antonio Reeves	G	CHA	10	1.0000	2.0000	0.0000	0.0000	0.7000	0.8000	0.3000	0.1000	0.0000	0.1000	2.7000
Kadary Richmond	UTIL	WAS	3	3.3333	5.3333	1.0000	1.0000	0.6667	3.3333	2.6667	2.6667	0.3333	0.6667	8.3333
Monte Morris	UTIL	IND	6	1.1667	3.3333	0.3333	0.6667	0.3333	1.1667	1.5000	0.1667	0.1667	0.3333	3.0000
Drew Peterson	UTIL	CHA	6	0.3333	2.6667	0.1667	0.3333	0.0000	1.5000	0.3333	0.5000	0.1667	0.0000	0.8333
Payton Sandfort	F	OKC	4	3.2500	6.5000	0.5000	1.0000	1.7500	2.5000	0.0000	0.2500	0.0000	0.2500	8.7500
Dalano Banton	F	BOS	6	0.6667	2.0000	0.6667	0.6667	0.0000	0.8333	1.8333	0.3333	0.8333	1.3333	2.0000
DaQuan Jeffries	UTIL	SAC	3	3.6667	6.3333	1.6667	1.6667	1.3333	1.6667	0.3333	0.6667	0.0000	0.0000	10.3333
Wendell Moore Jr.	G	DET	6	0.6667	1.1667	0.3333	0.3333	0.0000	1.0000	0.6667	0.3333	0.3333	0.1667	1.6667
James Wiseman	UTIL	IND	4	1.5000	2.5000	0.2500	0.5000	0.0000	2.0000	0.7500	0.0000	0.2500	1.2500	3.2500
Jahmir Young	G	MIA	14	0.7143	1.7857	0.1429	0.1429	0.2143	0.2857	0.6429	0.0714	0.0000	0.2143	1.7857
Riley Minix	F	CLE	9	1.5556	2.5556	0.3333	0.3333	0.4444	0.6667	0.5556	0.2222	0.1111	0.3333	3.8889
Cody Martin	UTIL	IND	4	0.5000	2.5000	0.7500	1.5000	0.0000	3.5000	0.5000	1.0000	0.5000	1.0000	1.7500
Johni Broome	F	PHI	11	0.3636	2.1818	0.1818	0.5455	0.0000	1.4545	0.3636	0.2727	0.1818	0.2727	0.9091
Lawson Lovering	UTIL	MEM	2	3.0000	5.0000	1.0000	2.5000	0.0000	7.5000	1.5000	1.5000	0.5000	1.0000	7.0000
Chris Mañon	G	LAL	9	0.2222	0.6667	0.3333	0.4444	0.0000	1.1111	0.3333	0.5556	0.2222	0.1111	0.7778
Mouhamadou Gueye	F	CHI	2	3.0000	5.5000	1.5000	2.0000	0.5000	3.0000	3.0000	1.0000	0.5000	1.5000	8.0000
Mo Bamba	UTIL	UTA	4	1.2500	2.7500	0.0000	0.0000	0.0000	5.5000	0.2500	0.0000	0.7500	0.5000	2.5000
Isaiah Stevens	G	SAC	3	1.0000	2.3333	1.3333	1.3333	0.0000	1.0000	3.3333	1.6667	0.0000	0.6667	3.3333
Zyon Pullin	G	MIN	5	1.8000	2.6000	0.8000	0.8000	0.2000	0.6000	1.8000	0.2000	0.0000	0.6000	4.6000
Hunter Dickinson	C	NOP	5	1.0000	2.8000	0.4000	0.4000	0.0000	1.0000	0.4000	0.2000	0.4000	0.6000	2.4000
John Tonje	G	BOS	6	0.6667	2.3333	0.6667	0.6667	0.5000	1.0000	0.3333	0.3333	0.0000	0.0000	2.5000
Dario Saric	UTIL	SAC	5	0.2000	1.2000	0.4000	0.4000	0.2000	1.2000	0.4000	0.0000	0.0000	0.4000	1.0000
CJ Huntley	F	PHX	4	1.5000	2.7500	0.0000	0.0000	0.0000	1.2500	0.5000	0.2500	0.0000	0.0000	3.0000
Dillon Jones	F	NYK	7	0.2857	1.2857	0.5714	0.5714	0.1429	1.0000	0.5714	0.4286	0.0000	0.0000	1.2857
Olivier Sarr	C	CLE	4	1.2500	1.7500	0.5000	1.2500	0.5000	2.7500	1.2500	0.5000	0.7500	0.5000	3.5000
Skal Labissiere	UTIL	WAS	3	2.0000	3.3333	0.0000	0.0000	0.3333	3.0000	1.0000	0.6667	0.3333	0.3333	4.3333
Enrique Freeman	F	MIN	4	1.0000	2.0000	0.7500	1.0000	0.5000	2.5000	0.5000	0.5000	0.7500	0.5000	3.2500
Rocco Zikarsky	C	MIN	5	1.0000	1.6000	0.8000	0.8000	0.0000	2.8000	0.4000	0.0000	1.0000	0.2000	2.8000
Max Shulga	G	BOS	11	0.1818	0.7273	0.1818	0.1818	0.0909	0.5455	0.1818	0.0909	0.0000	0.2727	0.6364
Markelle Fultz	UTIL	TOR	5	0.4000	2.2000	0.0000	0.0000	0.0000	0.2000	1.6000	0.4000	0.0000	0.8000	0.8000
Isaac Jones	F	DET	7	0.4286	1.0000	0.4286	0.5714	0.0000	0.7143	0.1429	0.0000	0.1429	0.1429	1.2857
Grant Nelson	UTIL	BKN	4	1.2500	2.2500	1.7500	2.5000	0.0000	1.5000	1.2500	0.2500	1.2500	1.0000	4.2500
Emanuel Miller	F	SAS	5	1.2000	2.6000	0.2000	0.4000	0.4000	0.6000	0.8000	0.4000	0.0000	0.0000	3.0000
Jahmyl Telfort	UTIL	LAC	8	0.0000	0.5000	0.1250	0.5000	0.0000	0.3750	0.1250	0.1250	0.0000	0.1250	0.1250
Sean Pedulla	G	LAC	7	0.7143	2.1429	0.0000	0.0000	0.4286	0.4286	0.7143	0.0000	0.0000	0.1429	1.8571
Harrison Ingram	F	SAS	7	0.7143	0.8571	0.0000	0.0000	0.1429	0.4286	0.1429	0.0000	0.0000	0.1429	1.5714
Mark Sears	UTIL	MIL	7	0.8571	1.8571	0.8571	1.1429	0.5714	0.2857	0.2857	0.0000	0.0000	0.5714	3.1429
Hunter Sallis	UTIL	PHI	7	0.4286	0.7143	0.1429	0.2857	0.0000	0.1429	0.5714	0.0000	0.1429	0.7143	1.0000
Orlando Robinson	UTIL	ORL	4	0.7500	1.2500	0.0000	0.0000	0.2500	1.0000	0.7500	0.2500	0.0000	0.0000	1.7500
Hayden Gray	G	UTA	1	2.0000	3.0000	2.0000	2.0000	0.0000	0.0000	1.0000	1.0000	1.0000	1.0000	6.0000
Vladislav Goldin	C	MIA	9	0.3333	0.4444	0.1111	0.1111	0.0000	1.0000	0.3333	0.0000	0.3333	0.3333	0.7778
Norchad Omier	F	LAC	6	1.1667	1.6667	0.5000	0.5000	0.0000	1.1667	0.3333	0.1667	0.0000	0.0000	2.8333
Alex Morales	G	ORL	4	0.7500	1.7500	0.5000	0.5000	0.0000	0.7500	1.0000	0.0000	0.0000	1.0000	2.0000
Jayson Kent	F	POR	5	0.8000	1.4000	0.2000	0.2000	0.2000	1.0000	0.0000	0.2000	0.2000	0.0000	2.0000
Colin Castleton	C	ORL	4	0.5000	1.2500	0.2500	0.2500	0.0000	2.0000	0.2500	0.2500	0.0000	0.5000	1.2500
Alex Antetokounmpo	F	MIL	6	0.5000	1.3333	1.8333	2.5000	0.3333	1.0000	0.1667	0.0000	0.1667	0.1667	3.1667
Brandon Clarke	F	MEM	2	1.5000	4.5000	1.0000	2.0000	0.0000	3.0000	0.5000	1.0000	0.0000	0.0000	4.0000
Chris Livingston	UTIL	CLE	3	1.3333	2.3333	0.3333	0.3333	0.0000	1.0000	0.3333	0.3333	0.0000	0.0000	3.0000
Jacob Toppin	UTIL	ATL	5	0.8000	1.2000	0.0000	0.0000	0.0000	0.2000	0.2000	0.0000	0.2000	0.0000	1.6000
N'Faly Dante	UTIL	ATL	4	0.2500	1.0000	0.2500	0.2500	0.0000	1.7500	0.0000	0.5000	0.0000	0.2500	0.7500
Buddy Boeheim	UTIL	OKC	4	0.5000	1.5000	0.0000	0.0000	0.5000	0.0000	0.0000	0.0000	0.0000	0.0000	1.5000
Trevor Keels	G	MIA	8	0.3750	1.5000	0.0000	0.0000	0.2500	0.2500	0.0000	0.1250	0.0000	0.0000	1.0000
Chucky Hepburn	G	TOR	2	0.0000	3.0000	0.0000	0.0000	0.0000	0.5000	1.0000	0.5000	0.0000	0.5000	0.0000
Tristen Newton	G	HOU	1	4.0000	9.0000	2.0000	3.0000	2.0000	3.0000	0.0000	1.0000	0.0000	0.0000	12.0000
Tosan Evbuomwan	F	CHA	5	0.0000	0.2000	0.0000	0.0000	0.0000	0.4000	0.0000	0.0000	0.0000	0.0000	0.0000
Colby Jones	UTIL	DET	1	1.0000	3.0000	0.0000	0.0000	0.0000	4.0000	2.0000	0.0000	0.0000	0.0000	2.0000
Stanley Umude	UTIL	SAS	2	0.5000	1.0000	0.0000	0.0000	0.0000	0.0000	0.5000	0.5000	0.0000	0.0000	1.0000
Noa Essengue	F	CHI	2	0.0000	1.5000	0.0000	0.0000	0.0000	0.0000	0.0000	0.5000	0.0000	0.0000	0.0000
Trentyn Flowers	UTIL	CHI	2	1.0000	1.5000	0.0000	0.0000	0.0000	0.5000	0.5000	0.0000	0.0000	0.5000	2.0000
Darius Brown II	UTIL	CLE	1	0.0000	1.0000	0.0000	0.0000	0.0000	1.0000	0.0000	0.0000	0.0000	0.0000	0.0000
`.trim();

/* ============================================================
   CONFIG
   ============================================================ */
const CATS = [
  {k:"fg",  label:"FG%", pct:true},
  {k:"ft",  label:"FT%", pct:true},
  {k:"tpm", label:"3PM"},
  {k:"pts", label:"PTS"},
  {k:"reb", label:"REB"},
  {k:"ast", label:"AST"},
  {k:"stl", label:"STL"},
  {k:"blk", label:"BLK"},
  {k:"to",  label:"TO",  neg:true}
];

let cfg = {
  teams:12, slot:5, size:13, aggr:5, k:7, gpw:0.5, scarcity:1.2,
  /*  Per-category weight, to match your league's scoring. 1 = counts normally,
      0 = not scored at all (8-cat leagues drop turnovers entirely). This is a
      LEAGUE SETTING, not a strategy — punting is separate and per-roster.    */
  /*  Turnovers ship at 0.75 rather than 1.0. Every rostered team finishes
      underwater in TO, so a category-league turnover loss is partly priced in
      already, and full weight pushed high-usage guards further down the board
      than the market will ever let you exploit. Set it back to 1 in Setup if
      your league scores all nine equally — that's the stricter reading.      */
  catW: {fg:1, ft:1, tpm:1, pts:1, reb:1, ast:1, stl:1, blk:1, to:0.75},
  names: []            // optional league team names, indexed by draft slot
};
const teamName = i => (cfg.names[i] && cfg.names[i].trim()) || ("Team " + (i+1));
const shortName = i => { const n = teamName(i); return n.length > 9 ? n.slice(0,8) + "\u2026" : n; };
const cw = k => cfg.catW[k] === undefined ? 1 : cfg.catW[k];
const DEFAULT_SHAPE = 0.5;   // balanced starting point for category shaping
let locks = {};
let shape = DEFAULT_SHAPE;   // 0 = pure value, 1 = full category shaping
let ui = {gapHidden:"", sugHidden:new Set()};                 // cat -> "punt" | "chase" (absent = auto)
let pool = [];
let picks = [];          // {playerId, teamIdx, overall}
let sortKey = "fit", sortDir = -1;
let posFilter = "ALL";
let hoverId = null;      // previewed player; persists until another row is hovered
let recMessageCleared = false; // CLEAR hides the comparison callout until another player is hovered
let rosterInspectId = null;   // click a drafted roster player to expand his projected per-game line
let selectedId = null;     // player currently being inspected
let armedDraftId = null;   // ONLY a first row/Enter confirmation arms a player for drafting
let boardIds = [];         // ids in the order currently rendered, for arrow keys

/*  Starred players are keyed by name, not pool index, so a watchlist survives
    re-importing projections — the ids shift, the names don't.                */
const STAR_KEY = "draftboard.stars.v1";
let starred = new Set();
try{ starred = new Set(JSON.parse(localStorage.getItem(STAR_KEY) || "[]")); }catch(e){}
function saveStars(){
  try{ localStorage.setItem(STAR_KEY, JSON.stringify([...starred])); }catch(e){}
}
const isStarred = p => starred.has(nameKey(p.name));
function toggleStar(p){
  const k = nameKey(p.name);
  if(starred.has(k)) starred.delete(k); else starred.add(k);
  // Starring is a separate action, not the second half of a draft confirmation.
  armedDraftId = null;
  saveStars(); render();
}
let flashPick = null;    // overall index of the pick to highlight briefly
let ledgerTeam = null;   // null = my team
let ledgerMode = "z";    // "z" or "tot"
let ledgerCollapsed = false;

/* ============================================================
   PARSING + Z-SCORES
   ============================================================ */
/*  Header-driven column mapping. Reads whatever header row the source gives
    us and maps it by name, so ESPN (PTS last, FGM/FGA combined), Hashtag
    (paren percentage cells) and plain CSV all land in the same shape.
    Falls back to anchor detection when there's no usable header.          */
const HDR = {
  rank:["r#","rk","rank","#","no","pos rank"],
  name:["player","name","players","player name"],
  adp:["adp","avg pick","average pick"],
  pos: ["pos","position","positions"],
  team:["team","tm"],
  gp:  ["gp","g","games","games played"],
  fg:  ["fg%","fg","fgm/fga","fg made/att","fgm/a","fgm-fga"],
  fgm: ["fgm","fg made"], fga:["fga","fg att"],
  ft:  ["ft%","ft","ftm/fta","ft made/att","ftm/a","ftm-fta"],
  ftm: ["ftm","ft made"], fta:["fta","ft att"],
  tpm: ["3pm","3ptm","3s","threes","3p","3pm/3pa","3pt","3ptm/3pta","3pm-3pa"],
  pts: ["pts","points","pt"],
  reb: ["reb","treb","rebs","rebounds","tr","trb"],
  ast: ["ast","asts","assists"],
  stl: ["stl","steals","st"],
  blk: ["blk","blocks","bs"],
  to:  ["to","tov","turnovers","turnover","tos"],
  pid: ["player-additional","player_additional","playeradditional","player id","slug"]
};

const PAREN = /^([\d.]+)\s*%?\s*\(\s*([\d.]+)\s*[\/\-]\s*([\d.]+)\s*\)$/;
const MADEATT = /^([\d.]+)\s*[\/\-]\s*([\d.]+)$/;
const POSRE = /^(PG|SG|SF|PF|C|G|F)([\/,\-](PG|SG|SF|PF|C|G|F))*$/i;

const norm = s => String(s).toLowerCase().trim().replace(/\*+$/,"").replace(/\s+/g," ");

/*  Accent folding. NFD splits most accented letters into base + combining mark,
    which we then drop — but a handful of letters have no decomposition and must
    be mapped by hand, or they vanish and the name never matches.
    Jokić -> jokic, Dončić -> doncic, Šengün -> sengun, Đoković -> dokovic.     */
const FOLD = {"đ":"d","ø":"o","ł":"l","ß":"ss","æ":"ae","œ":"oe","ı":"i","ð":"d","þ":"th","ħ":"h","ŋ":"n"};
function fold(s){
  return String(s).normalize("NFD")
    .replace(/[\u0300-\u036f]/g, "")
    .toLowerCase()
    .replace(/[đøłßæœıðþħŋ]/g, ch => FOLD[ch] || ch);
}

// Tokens with punctuation and generational suffixes removed.
function nameParts(s){
  return fold(s)
    .replace(/[^a-z ]/g, " ")
    .replace(/\b(jr|sr|ii|iii|iv)\b/g, " ")
    .replace(/\s+/g, " ").trim()
    .split(" ").filter(Boolean);
}
const nameKey = s => nameParts(s).join("");
// Fallback key: surname + first initial, for Nic/Nicolas, Cam/Cameron, Herb/Herbert.
function lastKey(s){
  const t = nameParts(s);
  return t.length > 1 ? t[t.length-1] + "|" + t[0][0] : (t[0] || "");
}

/*  Sites render a full name and an abbreviated one in the same cell, and ESPN
    tacks team + position on the end. Strip both, but never to nothing.      */
/*  Team abbreviations differ by source (NO/NOP, PHO/PHX, GS/GSW...). Matching a
    real list rather than "any 2-3 capitals" matters: the naive version turns
    "Trey Murphy IIINO SG/SF" into "Trey Murphy II" by eating an I.            */
const TEAMS = ["ATL","BKN","BRK","BOS","CHA","CHO","CHI","CLE","DAL","DEN","DET","GS","GSW",
  "HOU","IND","LAC","LAL","MEM","MIA","MIL","MIN","NO","NOP","NOH","NY","NYK","OKC","ORL",
  "PHI","PHO","PHX","POR","SA","SAS","SAC","TOR","UTA","UTAH","WAS","WSH"]
  .sort((a,b)=>b.length-a.length);
const POSALT = "(?:PG|SG|SF|PF|C)";
const TEAMPOS = new RegExp(
  `\\s*(?:${TEAMS.join("|")})\\s+${POSALT}(?:\\s*[,\\/]\\s*${POSALT})*\\s*$`);

function cleanName(s){
  let n = String(s).trim();
  const noTeam = n.replace(TEAMPOS, "").trim();
  if(noTeam.length >= 3) n = noTeam;
  const noAbbrev = n.replace(/\s*[A-Z]\.[A-Za-z'’\-. ]+$/, "").trim();
  if(noAbbrev.length >= 3) n = noAbbrev;
  return n.trim();
}

function splitRow(line){
  let l = line.trim().replace(/^\|/,"").replace(/\|$/,"");
  if(l.includes("\t"))  return l.split("\t").map(s=>s.trim());
  if(l.includes("|"))   return l.split("|").map(s=>s.trim());

  // Quoted CSV: honour quotes, and don't split inside "(10.5/18.3)".
  if(l.includes('"')){
    const out = []; let cur = "", q = false;
    for(let i = 0; i < l.length; i++){
      const ch = l[i];
      if(ch === '"'){
        if(q && l[i+1] === '"'){ cur += '"'; i++; }
        else q = !q;
      }
      else if(ch === "," && !q){ out.push(cur.trim()); cur = ""; }
      else cur += ch;
    }
    out.push(cur.trim());
    return out;
  }

  if(/\s{2,}/.test(l)) return l.split(/\s{2,}/).map(s=>s.trim());
  return l.split(/,(?![^(]*\))/).map(s=>s.trim());
}

function mapHeader(cells){
  const map = {};
  cells.forEach((cell,i)=>{
    const n = norm(cell);
    for(const key in HDR){
      if(map[key] !== undefined) continue;
      if(HDR[key].includes(n)){ map[key] = i; break; }
    }
  });

  /*  Basketball Reference calls the makes column plain "FG" / "FT" and puts
      attempts in "FGA" / "FTA". Elsewhere "FG" means a rate or a combined
      cell. If attempts have their own column, "FG" must be makes.          */
  ["fg","ft"].forEach(k=>{
    const att = k + "a", made = k + "m";
    if(map[att] !== undefined && map[made] === undefined && map[k] !== undefined){
      map[made] = map[k];
      delete map[k];
    }
  });

  const usable = map.name !== undefined &&
                 (map.pts !== undefined || map.reb !== undefined || map.ast !== undefined);
  return usable ? map : null;
}

/*  Traded players appear once per team plus a combined "2TM"/"3TM" row.
    The combined row has the most games, so keeping max-GP per name gives
    the full-season line rather than a partial stint.                      */
function dedupe(rows){
  const best = new Map();
  rows.forEach(r=>{
    const k = nameKey(r.name);
    const prev = best.get(k);
    if(!prev || (r.gp||0) > (prev.gp||0)) best.set(k, r);
  });
  const out = [...best.values()];
  out.forEach((r,i)=> r.id = i);
  return out;
}

function num(s){
  const v = parseFloat(String(s).replace(/[^\d.\-]/g,""));
  return isNaN(v) ? 0 : v;
}

// Pull made/attempted out of a cell in any of the three shapes we see.
function volume(cell){
  if(cell === undefined) return null;
  const s = String(cell).trim();
  let m = s.match(PAREN);   if(m) return {made:num(m[2]), att:num(m[3])};
  m = s.match(MADEATT);     if(m) return {made:num(m[1]), att:num(m[2])};
  return null;
}

function parsePool(text){
  const lines = String(text).trim().split(/\r?\n/).map(s=>s.trim()).filter(Boolean);
  const out = [], skipped = [];
  let map = null;

  for(const line of lines){
    if(/^[-|\s:]+$/.test(line)) continue;
    const c = splitRow(line).filter((x,i,arr)=> !(x === "" && i === arr.length-1));
    if(c.length < 6) continue;

    // Header rows repeat every dozen lines on some sites — re-read, never parse.
    const asHeader = mapHeader(c);
    if(asHeader){ map = asHeader; continue; }

    let rec = null;

    /* ---- mapped columns ---- */
    if(map){
      const at = k => map[k] !== undefined ? c[map[k]] : undefined;
      const fgv = volume(at("fg")) || (map.fgm !== undefined
                    ? {made:num(at("fgm")), att:num(at("fga"))} : null);
      const ftv = volume(at("ft")) || (map.ftm !== undefined
                    ? {made:num(at("ftm")), att:num(at("fta"))} : null);
      if(!fgv || !fgv.att) { skipped.push(line); continue; }

      const posCell = at("pos");
      rec = {
        name: cleanName(at("name") || ""),
        pos:  posCell && POSRE.test(posCell) ? posCell.toUpperCase().split(/[\/,\-]/) : ["UTIL"],
        team: at("team") || "—",
        gp:   map.gp !== undefined ? Math.round(num(at("gp"))) : null,
        adp:  map.adp !== undefined ? num(at("adp")) : null,
        srcRank: map.rank !== undefined ? num(at("rank")) : null,
        fgm:fgv.made, fga:fgv.att,
        ftm:ftv?ftv.made:0, fta:ftv?ftv.att:0,
        tpm:num(volume(at("tpm"))?volume(at("tpm")).made:at("tpm")),
        pts:num(at("pts")), reb:num(at("reb")), ast:num(at("ast")),
        stl:num(at("stl")), blk:num(at("blk")), to:num(at("to")),
        pid:  map.pid !== undefined ? String(at("pid")||"").trim() : ""
      };
    }

    /* ---- no header: anchor on paren percentage cells ---- */
    else {
      const pIdx = c.map((x,i)=>PAREN.test(x)?i:-1).filter(i=>i>=0);
      if(pIdx.length >= 2){
        const fg = c[pIdx[0]].match(PAREN), ft = c[pIdx[1]].match(PAREN);
        const tail = c.slice(pIdx[1]+1).filter(x=>/^-?[\d.]+$/.test(x)).map(num);
        if(tail.length < 7){ skipped.push(line); continue; }
        const posI = c.findIndex((x,i)=> i < pIdx[0] && POSRE.test(x));
        const head = c.slice(0, pIdx[0]);
        const mid  = c.slice(posI+1, pIdx[0]).filter(x=>/^[\d.]+$/.test(x)).map(num);
        const nums = head.filter(x=>/^[\d.]+$/.test(x)).map(num);
        rec = {
          name: cleanName(head.find(x=>/[A-Za-z]{3,}/.test(x) && !POSRE.test(x)) || ""),
          pos:  posI>=0 ? c[posI].toUpperCase().split(/[\/,\-]/) : ["UTIL"],
          team: posI>=0 ? (c[posI+1]||"—") : "—",
          gp:   mid.length ? Math.round(mid[0]) : null,
          srcRank: nums.length ? nums[0] : null,
          adp:  nums.length > 1 ? nums[1] : null,
          fgm:num(fg[2]), fga:num(fg[3]), ftm:num(ft[2]), fta:num(ft[3]),
          tpm:tail[0], pts:tail[1], reb:tail[2], ast:tail[3],
          stl:tail[4], blk:tail[5], to:tail[6]
        };
      }
      /* ---- plain positional fallback ---- */
      else if(c.length >= 14 && /^[\d.]+$/.test(c[3])){
        rec = {
          name:cleanName(c[0]), pos:c[1].toUpperCase().split(/[\/,]/), team:c[2],
          fgm:num(c[3]), fga:num(c[4]), ftm:num(c[5]), fta:num(c[6]),
          tpm:num(c[7]), pts:num(c[8]), reb:num(c[9]), ast:num(c[10]),
          stl:num(c[11]), blk:num(c[12]), to:num(c[13]),
          adp: c[14]!==undefined?num(c[14]):null,
          gp:  c[15]!==undefined?Math.round(num(c[15])):null,
          srcRank:null
        };
      }
      else { skipped.push(line); continue; }
    }

    if(!rec || !rec.name || rec.name.length < 2 || rec.fga <= 0){ skipped.push(line); continue; }
    rec.pos = rec.pos.map(s=>s.trim()).filter(Boolean);
    if(!rec.pos.length) rec.pos = ["UTIL"];
    rec.id = out.length;
    out.push(rec);
  }

  parsePool.skipped = skipped;
  return out;
}

/*  Match an imported set onto the pool. Two tiers: exact folded name, then
    surname + first initial — but only when that key is unique on BOTH sides,
    so "Keon Johnson" never silently absorbs "Kevin Johnson".               */
function attachLast(rows){
  const exact = new Map(), loose = new Map(), dupSrc = new Set();
  rows.forEach(r=>{
    exact.set(nameKey(r.name), r);
    const lk = lastKey(r.name);
    if(loose.has(lk)) dupSrc.add(lk); else loose.set(lk, r);
  });

  const poolCount = new Map();
  pool.forEach(p=>{ const lk = lastKey(p.name); poolCount.set(lk, (poolCount.get(lk)||0)+1); });

  let hit = 0, fuzzy = 0; const missed = [];
  pool.forEach(p=>{
    let m = exact.get(nameKey(p.name));
    if(!m){
      const lk = lastKey(p.name);
      if(!dupSrc.has(lk) && poolCount.get(lk) === 1){
        const candidate = loose.get(lk);
        if(candidate){
          const a = nameParts(p.name)[0] || "";
          const b = nameParts(candidate.name)[0] || "";
          if(a.startsWith(b) || b.startsWith(a)){
            m = candidate;
            fuzzy++;
          }
        }
      }
    }
    p.last = m || null;
    if(m && m.pid && !p.pid) p.pid = m.pid;   // photo id rides along with the actuals
    if(m) hit++; else missed.push(p.name);
  });

  attachLast.fuzzy = fuzzy;
  attachLast.missed = missed;

  /*  Score the matched actuals as their own population. Colouring last season's
      numbers by this season's projected z would be a lie — a player whose role
      changed would show the wrong sign. Only matched rows are scored, so the
      comparison population is the same 190-odd players either way.           */
  const used = pool.map(p=>p.last).filter(Boolean);
  if(used.length) scoreBoth(used);

  return hit;
}

/* ============================================================
   THE ENGINE
   ============================================================ */


/* Fixed replacement-level line for Yahoo players missing from the imported
   projection pool. This keeps one obscure/rookie pick from either breaking the
   sync or counting as literally zero production. */
const YAHOO_FALLBACK_LINE = Object.freeze({
  fgPct:0.45, fga:9.0,
  ftPct:0.75, fta:2.0,
  tpm:1.0, pts:12.0, reb:4.0, ast:4.0, stl:0.5, blk:0.5, to:1.5
});

function yahooPlaceholderPlayer(pk){
  if(!pk || !pk.outOfPool) return null;
  const pos = Array.isArray(pk.unknownPos) && pk.unknownPos.length ? pk.unknownPos : ["UTIL"];
  const l = YAHOO_FALLBACK_LINE;
  const p = {
    id:`__yahoo_unknown_${pk.overall}_${nameKey(pk.unknownName || "unknown")}`,
    name:pk.unknownName || "Yahoo player (out of projection pool)",
    team:pk.unknownTeam || "—", pos, gp:0, adp:null, valRank:null,
    fgm:l.fgPct*l.fga, fga:l.fga, ftm:l.ftPct*l.fta, fta:l.fta,
    tpm:l.tpm, pts:l.pts, reb:l.reb, ast:l.ast, stl:l.stl, blk:l.blk, to:l.to,
    z:{}, zpg:{}, total:0, totalPg:0, last:null,
    yahooPlaceholder:true, yahooFallbackLine:true
  };
  scorePlayerAgainstPool(p, pool);
  scorePlayerAgainstPool(p, pool, {gpw:0, field:"zpg", totalField:"totalPg"});
  return p;
}
function playerForPick(pk){
  if(!pk) return null;
  return (pk.playerId !== null && pk.playerId !== undefined ? pool.find(x=>x.id===pk.playerId) : null) || yahooPlaceholderPlayer(pk);
}
function myRoster(){
  return picks.filter(p=>p.teamIdx === myTeamIdx()).map(playerForPick).filter(Boolean);
}
function takenIds(){ return new Set(picks.map(p=>p.playerId).filter(id=>id !== null && id !== undefined)); }


// Team's summed z per category.


/*  Leverage weights.
    Win prob in a category ≈ sigmoid(teamZ / k). The marginal value of
    adding z there is the derivative: s*(1-s). Peaks at even, dies at
    locked-up or hopeless — which is what makes it punt on its own.  */


/*  Roster gaps are reported, never scored.

    Nudging a player's fit because he plays a position you're missing is how a
    36th-ranked centre ends up recommended over a 6th-ranked guard. Positional
    need is real, but it's a constraint you satisfy late with a replacement-level
    body — not a reason to pass on a better player in round four. So this returns
    information for the UI and contributes nothing to any score.               */


// P(gone before my next turn), from ADP vs picks remaining.






/* ============================================================
   RENDER
   ============================================================ */
const $ = s => document.querySelector(s);

function fmt(n, d=1){ return (n>=0?"":"") + n.toFixed(d); }
/*  A z of -0.03 is noise, not a weakness. Flipping hard from blue to red at
    exactly zero makes league-average look like a liability: Jalen Johnson's
    .482 FG% against a pool average of .483 rendered as a red mark.           */
const NEUTRAL = 0.22;
function zColor(z){
  if(Math.abs(z) < NEUTRAL) return "var(--dimmer)";
  return z >= 0 ? "var(--cool)" : "var(--hot)";
}
function zOpacity(z){
  return Math.abs(z) < NEUTRAL ? 0.55 : Math.min(1, 0.5 + Math.abs(z)/2.2);
}
const zText = z => Math.abs(z) < 0.05 ? "0.0" : z.toFixed(1);

function renderClock(state){
  const done = picks.length >= cfg.teams * cfg.size;
  const overall = picks.length;
  const onIdx = teamOnClock(overall);
  const mine = onIdx === myTeamIdx();
  const rd = Math.floor(overall / cfg.teams) + 1;
  const inRd = (overall % cfg.teams) + 1;

  /*  When it's your pick, "next turn" means the one AFTER this one — that's the
      number you're actually reasoning about when deciding who'll still be there. */
  const nxt = state.nxt;
  const until = nxt === null ? null : (mine ? state.gap : state.gap + 1);
  const untilText = until === null ? "—"
    : until === 0 ? "Back-to-back"
    : until === 1 ? "1 pick"
    : until + " picks";
  const nLbl = nxt === null ? "Next turn"
    : `Next turn · ${Math.floor(nxt/cfg.teams)+1}.${String((nxt%cfg.teams)+1).padStart(2,"0")}`;

  $("#clock").innerHTML = done
    ? `<div class="clock-cell onclock"><span class="k">Draft</span><span class="v">Complete</span></div>
       <div class="clock-gap"></div>
       <button class="clock-btn" id="undo">↶ Undo Pick</button>
       <button class="clock-btn" id="menubtn" title="Menu">☰</button>`
    : `
    <div class="clock-cell"><span class="k">Pick</span><span class="v">${overall+1}</span></div>
    <div class="clock-cell"><span class="k">Round</span><span class="v">${rd}.${String(inRd).padStart(2,"0")}</span></div>
    <div class="clock-cell ${mine?"you":"onclock"}" style="min-width:150px">
      <span class="k">${mine?"You're up":"On the clock"}</span>
      <span class="v" style="${!mine && teamName(onIdx).length>9 ? "font-size:15px" : ""}">${mine ? "YOUR PICK" : teamName(onIdx)}</span>
    </div>
    <div class="clock-cell"><span class="k">${nLbl}</span><span class="v" style="${until===0?"font-size:15px":""}">${untilText}</span></div>
    <div class="clock-gap"></div>
    <button class="clock-btn" id="undo">↶ Undo Pick</button>
    <button class="clock-btn" id="menubtn" title="Menu">☰</button>`;

  /*  Only the clock's own buttons need rewiring here — it's rebuilt every render,
      so a one-time binding would be dropped the first time a pick landed. The
      menu lives outside the clock and is wired once at boot.                 */
  const ub = $("#undo");
  ub.onclick = undo;
  ub.disabled = !picks.length;
  ub.style.opacity = picks.length ? 1 : .55;
  ub.title = picks.length ? `Undo pick ${picks.length} (${playerForPick(picks[picks.length-1])?.name || ""})` : "Nothing to undo";
  $("#menubtn").onclick = e=>{ e.stopPropagation(); $("#menu").classList.toggle("on"); };
}

// Menu actions — bound once.
function closeMenu(){ $("#menu").classList.remove("on"); }
document.addEventListener("click", closeMenu);
$("#menu").addEventListener("click", e=> e.stopPropagation());
$("#b_set").onclick  = ()=>{ closeMenu(); openSet(); };
$("#b_imp").onclick  = ()=>{ closeMenu(); $("#impmask").classList.add("on"); };
$("#b_help").onclick = ()=>{ closeMenu(); $("#helpmask").classList.add("on"); };
$("#theme").onclick  = ()=>{
  document.body.dataset.theme = document.body.dataset.theme === "court" ? "arena" : "court";
  closeMenu(); render();
};
$("#reset").onclick = ()=>{
  closeMenu();
  if(!picks.length || confirm(`Clear all ${picks.length} logged picks? Your loaded projections stay.`)){
    picks = []; locks = {}; hoverId = null; selectedId = null; armedDraftId = null; ledgerTeam = null; clearState(); $("#q").value = ""; render();
  }
};

/*  Player photo, with a monogram underneath.

    The id here is Basketball Reference's player code, carried over from the
    actuals import — NOT our internal pool index, which would have pulled up
    whichever face happened to sit at that number. Rookies and anyone who missed
    last season have no code, and hotlinked images can fail for reasons we can't
    see from here, so the monogram is always rendered behind and the <img> simply
    removes itself if it doesn't load.                                        */
function initials(name){
  const parts = name.replace(/[^A-Za-z\s.'-]/g,"").split(/\s+/).filter(Boolean);
  if(!parts.length) return "?";
  const a = parts[0][0] || "";
  const b = parts.length > 1 ? parts[parts.length-1][0] : "";
  return (a + b).toUpperCase();
}
function hueOf(name){
  let h = 0;
  for(let i = 0; i < name.length; i++) h = (h * 31 + name.charCodeAt(i)) % 360;
  return h;
}
/*  Photos need a real NBA person id. Basketball Reference blocks hotlinking, and
    the BoxScore Lab export carries no id column at all, so there is nothing in
    the data to build a URL from. Instead the map is imported once and cached;
    nba.com serves its headshot CDN without referrer checks.                  */
const PHOTO_KEY = "draftboard.photos.v2";
const PHOTO_TRY = "draftboard.phototry.v1";

/*  A photo record is {s, i}: which service, and that service's id. Two services
    because they're reached differently — Sleeper publishes a player index that
    browsers may read cross-origin, so it can be fetched automatically; NBA's
    index cannot, so those ids arrive via the manual paste.                    */
let photoIds = {};
try{ photoIds = JSON.parse(localStorage.getItem(PHOTO_KEY) || "{}"); }catch(e){ photoIds = {}; }

function savePhotoIds(){
  try{ localStorage.setItem(PHOTO_KEY, JSON.stringify(photoIds)); }catch(e){}
}

function photoUrl(rec){
  if(!rec) return null;
  return rec.s === "s"
    ? `https://sleepercdn.com/content/nba/players/${rec.i}.jpg`
    : `https://cdn.nba.com/headshots/nba/latest/1040x760/${rec.i}.png`;
}

/*  Pull the whole player index and keep only name -> id. Runs once, then lives
    in localStorage. Any failure here is non-fatal: monograms already work, so
    this never blocks a draft.                                                */
async function fetchPhotoIds(){
  const res = await fetch("https://api.sleeper.app/v1/players/nba", {cache:"force-cache"});
  if(!res.ok) throw new Error("HTTP " + res.status);
  const data = await res.json();
  let n = 0;
  Object.keys(data).forEach(id=>{
    const p = data[id];
    const full = p && (p.full_name || [p.first_name, p.last_name].filter(Boolean).join(" "));
    if(!full || !/[a-z]/i.test(full)) return;
    const rec = {s:"s", i:String(p.player_id || id)};
    photoIds[nameKey(full)] = rec;
    const lk = "~" + lastKey(full);
    if(!photoIds[lk]) photoIds[lk] = rec;
    n++;
  });
  savePhotoIds();
  return n;
}

async function autoPhotos(silent){
  const st = $("#photostat");
  if(st && !silent){ st.textContent = "fetching\u2026"; st.style.color = "var(--dim)"; }
  try{
    await fetchPhotoIds();
    try{ localStorage.setItem(PHOTO_TRY, "ok"); }catch(e){}
    syncPhotoStat();
    render();
    return true;
  }catch(e){
    try{ localStorage.setItem(PHOTO_TRY, String(Date.now())); }catch(e2){}
    if(st && !silent){
      st.textContent = "automatic fetch failed \u2014 use the paste method below";
      st.style.color = "var(--hot)";
    }
    return false;
  }
}

function importPhotoIds(text){
  const map = {};
  String(text).split(/\r?\n/).forEach(line=>{
    const cells = line.split(/\t|,/).map(x=>x.trim()).filter(Boolean);
    if(cells.length < 2) return;
    const id = cells.find(x=>/^\d{3,9}$/.test(x));
    const name = cells.find(x=>/[A-Za-z]{2,}/.test(x) && !/^\d+$/.test(x));
    if(id && name){ const rec = {s:"n", i:id}; map[nameKey(name)] = rec; map["~" + lastKey(name)] = rec; }
  });
  const n = Object.keys(map).length;
  if(n){ photoIds = Object.assign({}, photoIds, map); savePhotoIds(); }
  return n;
}

function photoIdFor(p){
  const r = photoIds[nameKey(p.name)] || photoIds["~" + lastKey(p.name)] || null;
  if(!r) return null;
  return typeof r === "string" ? {s:"n", i:r} : r;   // tolerate the old flat format
}

function photoHTML(p){
  const mono = `<span class="mono-badge" style="--h:${hueOf(p.name)}">${initials(p.name)}</span>`;
  const url = photoUrl(photoIdFor(p));
  const img = url
    ? `<img src="${url}" alt="" loading="lazy"
           onload="this.parentNode.classList.add('has-photo')"
           onerror="this.remove()">`
    : "";
  return `<div class="rec-photo">${mono}${img}</div>`;
}

function renderRec(state){
  const {avail, w, riskGap, conviction} = state;
  if(!avail.length){ $("#rec").innerHTML = `<div class="empty">No players left in the pool.</div>`; return; }

  const eligible = avail.filter(p=>p.rosterFit !== false);
  if(!eligible.length){
    $("#rec").innerHTML = `<div class="empty"><b>No legal roster fit available.</b><br>Every remaining player is incompatible with your open roster slots.</div>`;
    return;
  }
  const recPool = eligible;
  const best = [...recPool].sort((a,b)=>fitFor(b)-fitFor(a))[0];
  const bestVal = [...recPool].sort((a,b)=>b.total-a.total)[0];
  const onIdx = teamOnClock(picks.length);
  const mine = onIdx === myTeamIdx();

  const contrib = CATS.map(c=>({c, v: w[c.k] * cw(c.k) * best.z[c.k]}))
                      .sort((a,b)=>b.v-a.v).filter(x=>x.v > 0.25).slice(0,4);

  // Only meaningful on your own clock — after you pick, it clears itself.
  const cost = bestVal.total - best.total;
  const differs = mine && bestVal.id !== best.id && cost > 0.4;

  /*  The panel follows your selection, falling back to the recommendation.
      Clicking a row used to draft instantly, which meant you could never look
      at anyone but the top name — and one stray click cost you a pick.       */
  const sel = selectedId !== null ? avail.find(p => p.id === selectedId) : null;
  if(selectedId !== null && !sel){
    selectedId = null;                                      // he just got drafted
    armedDraftId = null;
  }
  const prev = hoverId !== null && hoverId !== selectedId ? avail.find(p => p.id === hoverId) : null;
  if(hoverId !== null && !avail.find(p => p.id === hoverId)) hoverId = null;
  const shown = prev || sel || best;
  const isRec = shown.id === best.id;
  const previewing = !!prev;

  const shownContrib = CATS.map(c=>({c, v: w[c.k] * cw(c.k) * shown.z[c.k]}))
                           .sort((a,b)=>b.v-a.v).filter(x=>x.v > 0.25).slice(0,4);

  const fg1 = fitFor(best), fg2 = fitFor(shown);
  const runnerUp = [...recPool].sort((a,b)=>fitFor(b)-fitFor(a))[1] || null;
  const fitGap = (isFinite(fg1)?fg1:0) - (isFinite(fg2)?fg2:0);
  /*  One shape for all three states: a label, exactly two lines of text, and a
      button. Anything conditional here changes the panel height, and the ledger,
      punt radar and roster all shuffle underneath it every time you hover a
      different player.                                                        */
  const surname = p => p.name.split(" ").slice(-1)[0];
  let cmpK, cmpBody, cmpBtn, cmpId;
  if(!isRec){
    cmpK = fitGap > 0.05 ? `Board rates ${surname(best)} higher by ${fitGap.toFixed(1)}` : `Equal to the board's pick`;
    cmpBody = `Recommendation is <b>${best.name}</b> (fit ${isFinite(fg1)?fg1.toFixed(1):"—"}${best.valRank?`, #${best.valRank}`:``})
               versus <b>${shown.name}</b> at ${isFinite(fg2)?fg2.toFixed(1):"—"}.`;
    cmpBtn = `Back to ${surname(best)}`; cmpId = "r_back";
  } else if(differs){
    cmpK = `Costs you ${cost.toFixed(1)} of raw value`;
    cmpBody = `Highest value on the board is <b>${bestVal.name}</b> (${bestVal.total.toFixed(1)}${bestVal.valRank?`, #${bestVal.valRank}`:``}).
               ${conviction < 0.45
                 ? `Category fit is only ${Math.round(conviction*100)}% engaged, so trust the value.`
                 : `The board prefers ${surname(best)} on category fit${best.scarce - bestVal.scarce > 0.3 ? ` and availability` : ``}.`}`;
    cmpBtn = `Inspect ${surname(bestVal)}`; cmpId = "r_val";
  } else {
    cmpK = `Top of the board`;
    cmpBody = `Nothing available scores higher for your roster.${runnerUp
      ? ` Clear of <b>${runnerUp.name}</b> by ${(fg1 - fitFor(runnerUp)).toFixed(1)}.` : ``}`;
    cmpBtn = runnerUp ? `Compare ${surname(runnerUp)}` : `—`; cmpId = "r_next";
  }
  /* CLEAR returns the recommendation card to a quiet default state instead of
      leaving a large empty hole. Hovering/selecting another player restores the
      normal comparison card because those interactions clear recMessageCleared. */
  const reasonLabels = shownContrib.slice(0,3).map(x=>x.c.label);
  const reasonText = reasonLabels.length >= 3
    ? `${reasonLabels[0]}, ${reasonLabels[1]} and ${reasonLabels[2]}`
    : reasonLabels.length === 2
      ? `${reasonLabels[0]} and ${reasonLabels[1]}`
      : reasonLabels[0] || `your strongest categories`;
  const availabilityText = shown.scarce > 0.3
    ? ` He is also unlikely to make it back to your next pick.`
    : shown.scarce < -0.3
      ? ` He may still be available later, so you have some flexibility.`
      : ``;
  const defaultCompare = `
    <div class="tradeoff rec-default">
      <div class="k">Why ${surname(shown)}?</div>
      <div class="tt">Best available fit for your roster. Adds <b>${reasonText}</b>${availabilityText}</div>
    </div>`;

  const compare = recMessageCleared
    ? defaultCompare
    : `
    <div class="tradeoff">
      <div class="k">${cmpK}</div>
      <div class="tt">${cmpBody}</div>
      <button class="mini" id="${cmpId}"${cmpId==="r_next" && !runnerUp ? " disabled" : ""}>${cmpBtn}</button>
    </div>`;

  $("#rec").innerHTML = `
    <div class="rec-eyebrow">${previewing ? "Previewing" : !isRec ? "Selected" : mine ? "Take him" : "Best available · not your pick"}${
      fitMode() ? ` <span style="color:var(--dimmer)">· fit from last season</span>` : ``}</div>
    <div class="rec-top">
      <div class="rec-info">
        <div class="rec-name">${shown.name}</div>
        <div class="rec-sub">${shown.pos.join(" / ")} · ${shown.team}${shown.valRank?` · Value Rank #${shown.valRank}`:``} · <span title="Add up the nine category numbers to the right and you get this. It's the player in a vacuum — total production, before anything about your roster. Switch the view to Z-scores to see the columns add up.">Total ${shown.total.toFixed(1)}</span></div>
        <div class="rec-why">
          ${shownContrib.map(x=>`<span class="chip up">${x.c.label} +${shown.z[x.c.k].toFixed(1)}</span>`).join("")}
          ${shown.scarce > 0.3 ? `<span class="chip risk">Gone before your next turn</span>` : ``}
          ${shown.scarce < -0.3 && riskGap > 0 ? `<span class="chip">Would still be there in ${riskGap} pick${riskGap===1?"":"s"}</span>` : ``}
        </div>
      </div>
      ${photoHTML(shown)}
    </div>
    <div class="rec-actions">
      <button class="btn primary" id="r_draft">Draft ${shown.name.split(" ").slice(-1)[0]} to ${mine ? "my team" : teamName(onIdx)}</button>
      <button class="btn" id="r_clear">Clear</button>
    </div>
    `;
  $("#r_draft").onclick = ()=> draft(shown.id);
  const rb = $("#r_back");  if(rb) rb.onclick = ()=>{ recMessageCleared = false; selectedId = null; hoverId = null; armedDraftId = null; render(); };
  const rc = $("#r_clear"); if(rc) rc.onclick = ()=>{ selectedId = null; hoverId = null; armedDraftId = null; render(); };
  const rv = $("#r_val");   if(rv) rv.onclick = ()=>{ recMessageCleared = false; selectedId = bestVal.id; hoverId = bestVal.id; armedDraftId = null; render(); };
  const rn = $("#r_next");  if(rn && runnerUp) rn.onclick = ()=>{ recMessageCleared = false; selectedId = runnerUp.id; hoverId = runnerUp.id; armedDraftId = null; render(); };
}

/*  Every team's roster in a single pass. Cheap enough to recompute each render:
    one walk over the pick log plus a map lookup, not a scan per team.        */
function allRosters(){
  const out = Array.from({length: cfg.teams}, ()=>[]);
  picks.forEach(pk=>{
    const p = playerForPick(pk);
    if(p && pk.teamIdx >= 0 && pk.teamIdx < cfg.teams) out[pk.teamIdx].push(p);
  });
  return out;
}

/*  Par, in real units: what one roster spot is worth if you drafted an average
    rostered player. Gives the totals bars something to measure against even
    when only your own team has picks — comparing to the league mean alone
    would leave every bar flat in round one.                                 */
function leagueRates(){
  const rostered = [...pool].sort((a,b)=>b.total-a.total).slice(0, cfg.teams * cfg.size);
  const s = teamTotals(rostered);
  const n = rostered.length || 1;
  const per = {};
  ["tpm","pts","reb","ast","stl","blk","to"].forEach(k=> per[k] = s[k]/n);
  per.fg = s.fga ? s.fgm/s.fga : 0;
  per.ft = s.fta ? s.ftm/s.fta : 0;
  return per;
}

// Reference value for a category given how many players are on the roster.
function catRef(per, k, size){
  return (k === "fg" || k === "ft") ? per[k] : per[k] * size;
}

function fmtTotal(k, v){
  if(v === null) return "\u2014";
  if(k === "fg" || k === "ft") return v.toFixed(3).replace(/^0/,"");
  return v.toFixed(1);
}

const possessive = n => /s$/i.test(n) ? n + "\u2019" : n + "\u2019s";

function renderLedger(state){
  const rosters = allRosters();
  const viewing = (ledgerTeam === null || ledgerTeam >= cfg.teams) ? myTeamIdx() : ledgerTeam;
  const isMine = viewing === myTeamIdx();

  const roster = isMine ? state.roster : rosters[viewing];
  const tz = isMine ? state.tz : teamZ(roster);
  const w = isMine ? state.w : leverage(tz, state.conviction);
  const n = Math.max(roster.length, 1);

  // Standings per category, so you can see who's actually winning each one.
  const allZ = rosters.map((r,i)=> i === viewing ? tz : teamZ(r));
  const live = allZ.filter((_,i)=> rosters[i].length > 0).length;

  // Ghost preview only makes sense on your own board.
  // Hover wins while the cursor is over a row; otherwise the selection stays pinned.
  const previewId = hoverId !== null ? hoverId : selectedId;
  const hov = (isMine && previewId !== null) ? pool.find(p=>p.id===previewId) : null;

  $("#ledgerteam").innerHTML = rosters.map((r,i)=>
    `<option value="${i}" ${i===viewing?"selected":""}>${i===myTeamIdx()?"My team":teamName(i)}${r.length?` (${r.length})`:""}</option>`).join("");

  const totals = teamTotals(roster);
  const per = leagueRates();
  const hovTotals = hov ? teamTotals(roster.concat([hov])) : null;
  const allTotals = rosters.map((r,i)=> i === viewing ? totals : teamTotals(r));
  const asTotals = ledgerMode === "tot";

  $("#ledger").innerHTML = CATS.map(c=>{
    const lev = w[c.k] > 1.05;
    const flip = c.neg ? -1 : 1;          // fewer turnovers is better
    let barW, ghostW, valTxt, valZ, rank;
    let nextTxt = null, better = 0;

    /* Bar geometry represents the roster's CATEGORY PROFILE and therefore never
       changes when you toggle the number display between Z and Totals. Only the
       labels/ranks change. The hover ghost uses the same fixed profile scale. */
    const curProfile = roster.length ? tz[c.k] / n : 0;
    const nextProfile = hov ? (tz[c.k] + hov.z[c.k]) / (roster.length + 1) : null;
    const profileScale = v => Math.max(-1, Math.min(1, v / 2)) * 50;
    barW = profileScale(curProfile);
    ghostW = nextProfile === null ? null : profileScale(nextProfile);
    valZ = curProfile;

    if(asTotals){
      const v = catTotal(totals, c.k);
      valTxt = roster.length ? fmtTotal(c.k, v) : "\u2014";

      if(hovTotals){
        const hv = catTotal(hovTotals, c.k);
        nextTxt = fmtTotal(c.k, hv);
        // In real units, fewer turnovers is the improvement.
        better = (hv !== null && v !== null) ? (hv - v) * flip : 0;
      }

      rank = roster.length
        ? 1 + allTotals.filter((t,i)=>{
            if(!rosters[i].length || i === viewing) return false;
            const o = catTotal(t, c.k), m = catTotal(totals, c.k);
            return o !== null && m !== null && (o - m) * flip > 0;
          }).length
        : null;
    } else {
      valTxt = roster.length ? fmt(curProfile, 2) : "\u2014";
      if(nextProfile !== null){
        nextTxt = fmt(nextProfile, 2);
        better = nextProfile - curProfile; // z is sign-corrected, so higher is always better
      }
      rank = roster.length ? 1 + allZ.filter((t,i)=> rosters[i].length > 0 && t[c.k] > tz[c.k]).length : null;
    }

    const bar = `<div class="lbar ${barW>=0?"pos":"neg"}" style="${barW>=0
      ? `left:50%;width:${barW}%`
      : `right:50%;width:${-barW}%`}"></div>`;

    const ghost = ghostW === null ? "" : `<div class="lghost" style="${ghostW>=0
      ? `left:50%;width:${ghostW}%`
      : `right:50%;width:${-ghostW}%`}"></div>`;

    const lock = isMine ? (locks[c.k] || "") : "";
    return `<div class="lrow ${lev?"lev":""} ${lock}">
      <span class="lcat" data-c="${c.k}" title="${isMine ? "Click through: auto \u2192 punt \u2192 chase \u2192 hard chase" : "Switch back to your team to set punts"}">${c.label}${cw(c.k)!==1?`<em class="cwx">\u00d7${cw(c.k)}</em>`:""}</span>
      <span class="ltrack"><span class="lmid"></span>${bar}${ghost}</span>
      <span class="lval">
        <span style="color:${roster.length?zColor(valZ):"var(--dimmer)"}">${valTxt}</span>
        ${nextTxt !== null ? `<em class="lnext ${better>0?"up":better<0?"dn":""}">\u2192 ${nextTxt}</em>` : ``}
      </span>
      <span class="lrank ${rank===1?"first":""}" title="${rank?`${rank} of ${live} drafted teams`:""}">${rank?`${rank}/${live}`:"\u2014"}</span>
    </div>`;
  }).join("");

  if(isMine){
    [...$("#ledger").querySelectorAll(".lcat")].forEach(el=>{
      el.onclick = ()=>{
        const k = el.dataset.c;
        const next = locks[k] === undefined ? "punt" : locks[k] === "punt" ? "chase"
                   : locks[k] === "chase" ? "chase2" : undefined;
        setLock(k, next);
      };
    });
  }

  /* Head-to-head uses actual projected production, never summed z-scores. */
  if(!isMine && roster.length && state.roster.length){
    const {rows,won,tied,lost,tot,verdict} = compareTeams(state.roster, roster, CATS, cw);
    $("#h2h").innerHTML = `
      <div class="h2h-head">
        <span class="k">You vs ${teamName(viewing)}</span>
        <span class="score ${verdict}">${won}–${lost}${tied?`–${tied}`:""}</span>
      </div>
      <div class="h2h-grid">
        ${rows.map(r=>`<span class="h2hc ${r.tie?"t":r.win?"w":"l"}">
          ${r.c.label}<em>${r.display}</em></span>`).join("")}
      </div>
      <div class="h2h-note">${verdict === "win" ? `You lead ${won} of ${tot} categories on current projected totals.`
        : verdict === "lose" ? `They lead ${lost} of ${tot}. Closest gaps are the ones worth attacking.`
        : `The matchup is even on current projected totals.`}
        Counting stats are team total vs team total; FG% and FT% are volume-weighted percentage vs percentage.</div>`;
    $("#h2h").style.display = "";
  } else {
    $("#h2h").style.display = "none";
  }

  $("#ledgerkey").innerHTML = asTotals
    ? `Combined projected per-game production of the roster \u2014 counting stats summed, percentages recombined by volume. Bar lengths always show the same category profile as Z mode, so toggling Z/Totals changes the numbers, not the shape of your roster. The right-hand figure is the rank among drafted teams.`
    : ``;

  $("#ledgernote").textContent = isMine
    ? (roster.length ? "Hover a player to preview" : "Draft someone to begin")
    : `${roster.length} drafted \u00b7 Read-only`;
}

// One stat cell. Per-game modes show the real number but keep the z-score
// colouring, so you can read impact and raw output at the same time.
function statCell(p, c, mode){
  if(mode === "z"){
    const z = p.z[c.k];
    return `<td class="mono" style="color:${zColor(z)};opacity:${zOpacity(z)}">${zText(z)}</td>`;
  }
  const src = mode === "last" ? p.last : p;
  let v = null, z = null;
  if(src){
    if(c.k === "fg") v = src.fga ? (src.fgm/src.fga).toFixed(3).replace(/^0/,"") : null;
    else if(c.k === "ft") v = src.fta ? (src.ftm/src.fta).toFixed(3).replace(/^0/,"") : null;
    else if(typeof src[c.k] === "number") v = src[c.k].toFixed(1);
    const zs = src.zpg || src.z;
    if(zs && typeof zs[c.k] === "number") z = zs[c.k];
  }
  if(v === null) return `<td class="mono" style="color:var(--dimmer)">—</td>`;
  if(z === null) return `<td class="mono" style="color:var(--dim)">${v}</td>`;
  return `<td class="mono" style="color:${zColor(z)};opacity:${zOpacity(z)}">${v}</td>`;
}

/*  Which fit to display. In "Last season per game" the Fit column answers the
    same question against last year's real production instead of a projection.
    The ledger deliberately stays on projections — it describes the roster you
    are actually building, not a hypothetical replay of last season.         */
function fitMode(){ return $("#mode").value === "last"; }
function fitFor(p){
  if(!fitMode()) return p.fitAdj;
  return (p.fitLast === null || p.fitLast === undefined) ? -Infinity : p.fitLast;
}

// Sort value for a column, so the first click always puts "best" on top.
function sortVal(p, key, mode){
  if(key === "fit")   return fitFor(p);
  if(key === "total") return p.total;
  if(key === "rank")  return p.valRank == null ? -Infinity : -p.valRank;
  if(key === "adp") {
    const adp = Number(p.adp);
    return (!Number.isFinite(adp) || adp <= 0) ? -Infinity : -adp; // blanks/invalid ADP always count as missing
  }
  if(mode === "z")    return p.z[key];
  const src = mode === "last" ? p.last : p;
  if(!src) return -Infinity;
  if(key === "fg") return src.fga ? src.fgm/src.fga : -Infinity;
  if(key === "ft") return src.fta ? src.ftm/src.fta : -Infinity;
  const raw = src[key];
  if(typeof raw !== "number") return -Infinity;
  return key === "to" ? -raw : raw;      // fewer turnovers is better
}

/*  Shared by the main board and the starred list so the two stay identical in
    layout and behaviour — same columns, same widths, same interactions.      */
function playerRow(p, opts){
  const o = opts || {};
  const rk = p.valRank ?? null;
  const gone = o.takenBy !== undefined && o.takenBy !== null;
  const mode = $("#mode").value;
  const star = `<span class="star${isStarred(p)?" on":""}" data-star="${p.id}"
     title="${isStarred(p)?"Remove from starred":"Star this player"}">${isStarred(p)?"\u2605":"\u2606"}</span>`;

  const nofit = !gone && p.rosterFit === false;
  return `<tr data-id="${p.id}" class="${gone?"gone ":""}${nofit?"nofit ":""}${!gone&&p.id===selectedId?"sel ":""}${o.top?"top":""}"${nofit?' title="No legal roster slot available for this player"':''}>
      <td class="l">
        <div class="pname">${star}${p.name}${
          o.flagRisk?'<span class="risk-dot" title="Likely gone before your next turn"></span>':''}${
          !gone && p.id===armedDraftId?'<span class="again">click again to draft</span>':''}</div>
        <div class="ppos">${gone
          ? `<span class="takenby">Drafted by ${o.takenBy}</span>`
          : `${p.pos.join("/")} \u00b7 ${p.team}${p.gp?` \u00b7 ${p.gp} GP`:""}`}</div>
      </td>
      <td class="fit mono" style="color:${(isFinite(fitFor(p))&&fitFor(p)>=0)?"var(--chalk)":"var(--dimmer)"}">${
        isFinite(fitFor(p)) ? fitFor(p).toFixed(1) : "\u2014"}</td>
      <td class="mono" style="color:${rk?"var(--chalk)":"var(--dimmer)"}">${rk ?? "\u2014"}</td>
      <td class="mono" style="color:var(--dim)">${Number.isFinite(Number(p.adp)) && Number(p.adp) > 0 ? Number(p.adp).toFixed(0) : "\u2014"}</td>
      <td class="mono" style="color:var(--dim)">${p.total.toFixed(1)}</td>
      ${CATS.map(c=>statCell(p,c,mode)).join("")}
    </tr>`;
}

/*  Clicking the star must not also select or draft the row underneath it.    */
function wireRows(container, opts){
  const o = opts || {};
  [...container.querySelectorAll("tr[data-id]")].forEach(tr=>{
    const id = +tr.dataset.id;
    const p = pool.find(x=>x.id === id);
    const st = tr.querySelector(".star");
    if(st) st.onclick = e=>{ e.stopPropagation(); toggleStar(p); };
    if(tr.classList.contains("gone") || tr.classList.contains("nofit")) return;   // unavailable: display only
    tr.onclick = ()=>{
      // Draft only when THIS player was explicitly armed by a previous row click.
      // Being selected by the recommendation panel, keyboard navigation, etc. is
      // not enough to make a single row click commit a pick.
      if(armedDraftId === id){
        armedDraftId = null;
        draft(id);
      } else {
        recMessageCleared = false;
        selectedId = id;
        hoverId = id;
        armedDraftId = id;
        render();
      }
    };
    tr.onmouseenter = ()=>{
      recMessageCleared = false;
      if(hoverId === id) return;
      hoverId = id;
      const s2 = evaluate();
      renderRec(s2); renderLedger(s2); markFocus();
    };
  });
}

function renderStars(state){
  const byId = new Map(pool.map(p=>[p.id, p]));
  const takenBy = new Map();
  picks.forEach(pk=> takenBy.set(pk.playerId, pk.teamIdx));

  const list = pool.filter(isStarred);
  $("#starcount").textContent = list.length ? `${list.length}` : "";
  $("#starwrap").style.display = list.length ? "" : "none";
  if($("#starbody")) $("#starbody").style.display = starsOpen ? "" : "none";
  if(!list.length){ $("#starboard").innerHTML = ""; return; }

  const key = sortKey;
  const sorted = [...list].sort((a,b)=>{
    const ag = takenBy.has(a.id), bg = takenBy.has(b.id);
    if(ag !== bg) return ag ? 1 : -1;          // drafted ones sink to the bottom
    const av = sortVal(a,key,$("#mode").value), bv = sortVal(b,key,$("#mode").value);
    const an = !isFinite(av), bn = !isFinite(bv);
    if(an || bn) return an && bn ? 0 : (an ? 1 : -1);
    return (bv - av) * (sortDir === -1 ? 1 : -1);
  });

  $("#starboard").innerHTML = sorted.map(p=>{
    const t = takenBy.has(p.id) ? teamName(takenBy.get(p.id)) : null;
    return playerRow(p, {takenBy: t});
  }).join("");
  wireRows($("#starboard"));
}

function renderBoard(state){
  const q = fold($("#q").value.trim());
  const mode = $("#mode").value;
  let list = state.avail;
  if(posFilter !== "ALL") list = list.filter(p=>p.pos.includes(posFilter));
  /*  Match team and position as well as name — "DEN" or "C" is often what you
      actually want mid-draft when you're hunting a specific need.           */
  if(q) list = list.filter(p =>
    fold(p.name).includes(q) ||
    fold(p.team).includes(q) ||
    p.pos.some(x => fold(x) === q));

  const key = sortKey;
  list = [...list].sort((a,b)=>{
    if(key === "fit" && (a.rosterFit === false || b.rosterFit === false) && a.rosterFit !== b.rosterFit)
      return a.rosterFit === false ? 1 : -1;
    if(key === "name") return a.name.localeCompare(b.name) * (sortDir === -1 ? 1 : -1);
    const av = sortVal(a,key,mode), bv = sortVal(b,key,mode);
    /*  A missing value is not a low value. Left as -Infinity it sorts as "worst",
        which floats blanks to the top the moment you reverse the direction — and
        two blanks subtract to NaN, which leaves the comparator undefined. Blanks
        go last in both directions, always.                                     */
    const ab = !isFinite(av), bb = !isFinite(bv);
    if(ab || bb) return ab && bb ? 0 : (ab ? 1 : -1);
    return (bv - av) * (sortDir === -1 ? 1 : -1);
  }).slice(0, 80);

  boardIds = list.map(p=>p.id);
  $("#board").innerHTML = list.map((p,i)=> playerRow(p, {
    /*  Only warn about availability for players you're plausibly taking. A
        "gone soon" dot on someone ranked 40th is noise — it's true (the market
        rates him higher than the projections do) but it isn't a decision you
        actually face.                                                        */
    flagRisk: p.risk > 0.6 && i < 15 && key === "fit",
    top: i === 0 && key === "fit"
  })).join("") || `<tr><td class="l" colspan="14" style="padding:24px;color:var(--dimmer)">No players match. Clear the search or change the position filter.</td></tr>`;

  // First click inspects; clicking the already-selected player commits.
  // Hover previews and STAYS put — see wireRows.
  wireRows($("#board"));
}

/*  Move the "previewing" outline without re-rendering the whole board — hover
    fires constantly and rebuilding 200 rows per row-crossing is wasteful.   */
function markFocus(){
  const rows = document.querySelectorAll("#board tr[data-id], #starboard tr[data-id]");
  rows.forEach(tr=>{
    const on = +tr.dataset.id === hoverId && hoverId !== selectedId;
    tr.classList.toggle("focus", on);
  });
}

/*  Applying a punt silently reshuffles the whole board, which is unnerving if
    you can't see what moved. Snapshot the ranking before and after, then show
    the biggest movers plus an undo.                                          */
let lastChange = null;

function rankSnapshot(){
  const st = evaluate();
  const m = new Map();
  [...st.avail].sort((a,b)=>b.fitAdj-a.fitAdj).forEach((p,i)=> m.set(p.id, {rank:i+1, name:p.name}));
  return m;
}

function setLock(k, val){
  const before = rankSnapshot();
  const prev = {...locks};
  if(val === undefined) delete locks[k]; else locks[k] = val;
  const after = rankSnapshot();

  const movers = [];
  after.forEach((a, id)=>{
    const b = before.get(id);
    if(!b) return;
    if(b.rank <= 20 || a.rank <= 20) movers.push({name:a.name, from:b.rank, to:a.rank, d:b.rank-a.rank});
  });
  movers.sort((x,y)=>Math.abs(y.d)-Math.abs(x.d));

  const label = CATS.find(c=>c.k===k).label;
  lastChange = {
    text: val === undefined ? `Cleared ${label}` : val === "punt" ? `Punted ${label}`
        : val === "chase2" ? `Hard chasing ${label}` : `Chasing ${label}`,
    prev,
    up: movers.filter(m=>m.d > 0).slice(0,3),
    down: movers.filter(m=>m.d < 0).slice(0,3)
  };
  render();
}

function undoLock(){
  if(!lastChange) return;
  locks = {...lastChange.prev};
  lastChange = null;
  render();
}

/*  Explain, in plain terms, what the slider is doing right now — including
    which categories the weighting currently favours and ignores.            */
function shapeBlurb(state){
  const pct = Math.round(state.conviction * 100);
  const punted = CATS.filter(c=>locks[c.k]==="punt").map(c=>c.label);
  if(state.conviction === 0)
    return "Overall Value = the best projected player available. Team Needs = the player who best fills holes in your current category profile.";
  const ranked = CATS.filter(c=>!locks[c.k]).sort((a,b)=>state.w[b.k]-state.w[a.k]);
  const hi = ranked.slice(0,2).map(c=>c.label).join(" and ");
  const base = `Overall Value = best projected player available. Team Needs = fills the holes in your current category profile. At ${pct}%, the board ${pct < 35 ? "mostly favours overall value" : pct < 70 ? "balances both" : "leans heavily toward team needs"}.`;
  const detail = hi ? ` Right now it is giving extra weight to ${hi}.` : "";
  const puntNote = punted.length ? ` ${punted.join(", ")} ${punted.length===1?"is":"are"} punted and ignored by Fit.` : "";
  return base + detail + puntNote;
}



function renderSuggest(state){
  const el = $("#suggest");
  const punted = CATS.filter(c=>locks[c.k]==="punt");
  const chased = CATS.filter(c=>locks[c.k]==="chase"||locks[c.k]==="chase2");
  const active = [...punted, ...chased];

  // Every category, always visible — click to cycle auto -> punt -> chase.
  const picker = `
    <div class="active">
      <span class="pickerlbl">Set by hand</span>
      ${CATS.map(c=>{
        const st = locks[c.k] || "auto";
        return `<span class="lockchip ${st}" data-c="${c.k}" title="${
          st==="punt" ? "Punted — counts for nothing. Click to chase instead."
          : st==="chase2" ? "Hard chase — weighted well past natural maximum. Click to clear."
          : st==="chase" ? "Chasing — weighted at maximum. Click to return to auto."
          : "Auto. Click to punt."}">${c.label}${st==="punt"?" ✕":st==="chase"?" ▲":st==="chase2"?" ▲▲":""}</span>`;
      }).join("")}
      ${active.length ? `<span class="lockchip clear" data-all="1">Clear all</span>` : ``}
    </div>`;

  // What the last change actually did to the board.
  const diff = !lastChange ? "" : `
    <div class="sug diff">
      <div class="k">${lastChange.text}</div>
      ${lastChange.up.length ? `<div><span class="up">▲ Rose</span> — ${lastChange.up.map(m=>m.name).join(", ")}</div>` : ``}
      ${lastChange.down.length ? `<div><span class="dn">▼ Fell</span> — ${lastChange.down.map(m=>m.name).join(", ")}</div>` : ``}
      ${!lastChange.up.length && !lastChange.down.length ? `<div style="color:var(--dimmer)">Board order barely moved.</div>` : ``}
      <button class="mini" id="undolock">Undo</button>
      <button class="dismiss" id="diffx" title="Dismiss">&times;</button>
    </div>`;

  const chaseSug = suggestChase(state);
  const chaseBox = chaseSug ? `<div class="sug ${chaseSug.punts===3?"warn":""}">
      <div class="k">${chaseSug.punts===3?"Protect your five-category path":"Consider chasing " + chaseSug.cat.label}</div>
      ${chaseSug.punts===3
        ? `You already have <b>3 punts</b>. Do not let a fourth category slip away. <b>${chaseSug.cat.label}</b> is your weakest remaining category, so hard-chase it now.`
        : `You already have <b>2 punts</b>, and <b>${chaseSug.cat.label}</b> is weak enough to put a fourth category at risk later. Consider chasing it now.`}
      <button class="mini" data-chase="${chaseSug.cat.k}">Chase ${chaseSug.cat.label}</button>
    </div>` : "";

  let body;
  if(punted.length >= 4){
    body = `<div class="sug warn">
      <div class="k">Four punts is too many</div>
      You need to win five of nine categories. Punting ${punted.length} leaves
      ${9-punted.length} to fight over, so you'd have to sweep almost all of them every week.</div>`;
  } else if(punted.length >= 2){
    body = `<div class="sug quiet">
      <b style="color:var(--chalk)">Punt limit reached.</b> You already have ${punted.length} punts, so nineCat will not recommend punting any additional categories.
      From here, recommendations focus on protecting and chasing the seven categories you still need to compete in.
    </div>`;
  } else {
    const all = suggestPunts(state);
    const sug = all.filter(x=>!ui.sugHidden.has(x.cat.k));
    if(!sug.length){
      body = state.roster.length < 2
        ? `<div class="sug quiet">Draft a couple of players and this will suggest categories worth abandoning. You can punt anything by hand above at any time.</div>`
        : `<div class="sug quiet">No obvious punts${all.length?` beyond the ${all.length} you dismissed`:``}. Your roster is still balanced enough to compete across the board${active.length?`, beyond what you've set by hand`:``}.
           ${ui.sugHidden.size ? `<a href="#" id="unhide">Show ${ui.sugHidden.size} dismissed</a>` : ``}</div>`;
    } else {
      body = sug.map(s=>`
        <div class="sug">
          <div class="k">Consider punting ${s.cat.label}</div>
          Drafting best-available finishes you <b>${Math.abs(s.gap).toFixed(1)} behind</b> a typical team here.
          ${s.reach < 0
            ? `Even chasing it all draft doesn't reach par — it's not winnable.`
            : `Chasing it back to par costs about <b>${s.cost.toFixed(0)}</b> of value spent on players you'd otherwise pass.`}
          <button class="mini" data-c="${s.cat.k}">Punt ${s.cat.label}</button>
          <button class="dismiss" data-hide="${s.cat.k}" title="Dismiss">&times;</button>
        </div>`).join("") +
        `<div class="sug quiet">Suggestions only — nothing is applied until you click.
         ${ui.sugHidden.size ? `<a href="#" id="unhide">Show ${ui.sugHidden.size} dismissed</a>` : ``}</div>`;
    }
  }

  el.innerHTML = picker + diff + chaseBox + body;

  [...el.querySelectorAll(".sug button[data-c]")].forEach(b=>{
    b.onclick = ()=> setLock(b.dataset.c, "punt");
  });
  [...el.querySelectorAll(".sug button[data-chase]")].forEach(b=>{
    b.onclick = ()=> setLock(b.dataset.chase, "chase2");
  });
  [...el.querySelectorAll(".lockchip[data-c]")].forEach(b=>{
    b.onclick = ()=>{
      const k = b.dataset.c;
      setLock(k, locks[k] === undefined ? "punt" : locks[k] === "punt" ? "chase"
               : locks[k] === "chase" ? "chase2" : undefined);
    };
  });
  const clr = el.querySelector('.lockchip[data-all]');
  if(clr) clr.onclick = ()=>{ locks = {}; lastChange = null; render(); };
  const u = el.querySelector("#undolock");
  if(u) u.onclick = undoLock;
  const dx = el.querySelector("#diffx");
  if(dx) dx.onclick = ()=>{ lastChange = null; render(); };
  [...el.querySelectorAll("button[data-hide]")].forEach(b=>{
    b.onclick = ()=>{ ui.sugHidden.add(b.dataset.hide); render(); };
  });
  const uh = el.querySelector("#unhide");
  if(uh) uh.onclick = e=>{ e.preventDefault(); ui.sugHidden.clear(); render(); };
}

/*  Roster slots, assigned properly.

    The panel used to drop the nth player drafted into the nth slot, which
    happily parked an SF/PF in the centre slot and told you the roster was fine.
    This is a bipartite matching: players on one side, slots on the other, an
    edge where the player is actually eligible. Kuhn's algorithm finds a maximum
    matching, so if a legal arrangement exists it gets found — and anyone left
    over genuinely doesn't fit.                                               */


function renderLog(){
  $("#logcount").textContent = picks.length ? `${picks.length} pick${picks.length===1?"":"s"}` : "";
  const recent = [...picks].reverse();
  $("#log").innerHTML = recent.length ? recent.map(pk=>{
    const p = playerForPick(pk);
    const mine = pk.teamIdx === myTeamIdx();
    const rd = Math.floor(pk.overall / cfg.teams) + 1;
    return `<div class="slot${pk.overall === flashPick ? " flash" : ""}">
      <span class="idx mono">${rd}.${String((pk.overall % cfg.teams)+1).padStart(2,"0")}</span>
      <span class="nm" style="${mine?"color:var(--ok);font-weight:600":""}">${p?p.name:"?"}${pk.outOfPool?` <span style="color:var(--dimmer);font-size:10px">· OUT OF POOL · 12/4/4 FALLBACK</span>`:""}</span>
      <span class="mono" style="color:var(--dimmer);font-size:10px">${mine?"YOU":shortName(pk.teamIdx)}</span>
      <button class="px" data-o="${pk.overall}" title="Remove this pick">×</button>
    </div>`;
  }).join("") : `<div class="empty" style="padding:14px 0">No picks yet. Click any player on the board to log him to whoever's on the clock.</div>`;

  [...$("#log").querySelectorAll(".px")].forEach(b=>{
    b.onclick = e=>{ e.stopPropagation(); picks = picks.filter(p=>p.overall !== +b.dataset.o); reindex(); render(); };
  });
}


function render(){
  const state = evaluate();
  const pct = Math.round(state.conviction * 100);
  $("#shape").value = pct;
  $("#shapev").textContent = `${pct}%`;
  $("#shapeauto").classList.toggle("on", Math.abs(shape - DEFAULT_SHAPE) < 0.005);
  $("#shaperow").title = shapeBlurb(state);
  renderClock(state);
  renderRec(state);
  renderLedger(state);
  renderSuggest(state);
  renderBoard(state);
  renderStars(state);
  renderRoster(state);
  renderLog();
  saveState();
}

/* ============================================================
   WIRING
   ============================================================ */
$("#posfilter").innerHTML = ["ALL","PG","SG","SF","PF","C"]
  .map(p=>`<button data-p="${p}" class="${p==="ALL"?"on":""}">${p}</button>`).join("");
[...$("#posfilter").querySelectorAll("button")].forEach(b=>{
  b.onclick = ()=>{
    posFilter = b.dataset.p;
    [...$("#posfilter").querySelectorAll("button")].forEach(x=>x.classList.toggle("on", x===b));
    saveSettings(); render();
  };
});

// Column sorting — click to sort, click again to flip direction.
[...document.querySelectorAll("thead th")].forEach(th=>{
  th.onclick = ()=>{
    const k = th.dataset.s;
    if(!k) return;
    if(sortKey === k) sortDir = -sortDir;
    else { sortKey = k; sortDir = -1; }
    saveSettings();
    [...document.querySelectorAll("thead th")].forEach(x=>{
      x.classList.toggle("on", x.dataset.s === sortKey);
      const a = x.querySelector(".arw");
      if(a) a.textContent = sortDir === -1 ? "▾" : "▴";
    });
    const state = evaluate();
    renderBoard(state);
    renderStars(state);
  };
});
document.querySelector('thead th[data-s="fit"]').classList.add("on");

// Games-played weight
const GPW_LABEL = v => v < 0.15 ? "Per game" : v < 0.4 ? "Light" : v < 0.65 ? "Balanced" : v < 0.9 ? "Heavy" : "Full season";
$("#gpw").addEventListener("input", e=>{
  cfg.gpw = +e.target.value / 100;
  $("#gpwv").textContent = GPW_LABEL(cfg.gpw);
  saveSettings();
  scorePool(pool);
  const used = pool.map(p=>p.last).filter(Boolean);
  if(used.length) scoreBoth(used);
  render();
});
document.querySelector('thead th[data-s="fit"]').classList.add("on");

$("#mode").addEventListener("change", ()=>{ saveSettings(); render(); });
[...$("#ledgermode").querySelectorAll("button")].forEach(b=>{
  b.onclick = ()=>{
    ledgerMode = b.dataset.m;
    [...$("#ledgermode").querySelectorAll("button")].forEach(x=>x.classList.toggle("on", x===b));
    saveSettings(); render();
  };
});
$("#ledgerteam").addEventListener("change", e=>{
  const v = +e.target.value;
  ledgerTeam = (v === myTeamIdx()) ? null : v;
  render();
});
$("#ledgercollapse").addEventListener("click", ()=>{
  ledgerCollapsed = !ledgerCollapsed;
  $("#ledgerbody").classList.toggle("hide", ledgerCollapsed);
  $("#ledgercollapse").textContent = ledgerCollapsed ? "Expand" : "Collapse";
  $("#ledgercollapse").setAttribute("aria-expanded", String(!ledgerCollapsed));
  saveSettings();
});

// The GP slider is inert unless the loaded data actually carries a GP column.
function syncGPW(){
  const has = pool.some(p=>p.gp > 0);
  $("#gpw").disabled = !has;
  $("#gpw").style.opacity = has ? 1 : .35;
  $("#gpwlab").textContent = has ? "Games Played Weight" : "Games Played Weight — no GP column in this data";
  $("#gpw").value = Math.round(cfg.gpw * 100);   // put the handle where the value actually is
  $("#gpwv").textContent = has ? GPW_LABEL(cfg.gpw) : "Inactive";
}

$("#shape").addEventListener("input", e=>{ shape = +e.target.value / 100; render(); });
$("#shapeauto").onclick = ()=>{ shape = DEFAULT_SHAPE; render(); };

// Tuning stays folded away until asked for — two sliders is the single biggest
// source of "too much going on" before anyone has made a pick.
function setTuning(open){
  $("#tuning").classList.toggle("hide", !open);
  $("#tuningcaret").textContent = open ? "\u25be" : "\u25b8";
}
/*  Collapse state for the starred list, kept with the other view preferences. */
let starsOpen = true;
function setStarsOpen(open){
  starsOpen = open;
  const body = $("#starbody");
  if(body) body.style.display = open ? "" : "none";
  $("#starcaret").textContent = open ? "\u25be" : "\u25b8";
  $("#starbtn").setAttribute("aria-expanded", open ? "true" : "false");
}
$("#starbtn").onclick = ()=>{ setStarsOpen(!starsOpen); saveSettings(); };

$("#tuningbtn").onclick = ()=>{
  setTuning($("#tuning").classList.contains("hide"));
  saveSettings();
};


$("#q").addEventListener("input", ()=> renderBoard(evaluate()));
$("#q").addEventListener("keydown", e=>{
  if(e.key === "Enter"){
    const first = $("#board").querySelector("tr[data-id]");
    const fid = first ? +first.dataset.id : null;
    if(fid !== null){
      if(armedDraftId === fid) draft(fid);    // Enter twice to commit
      else { selectedId = fid; hoverId = fid; armedDraftId = fid; render(); }
    }
  }
});
document.addEventListener("keydown", e=>{
  if(e.key === "/" && document.activeElement !== $("#q")){ e.preventDefault(); $("#q").focus(); }
  if((e.metaKey||e.ctrlKey) && e.key === "z"){ e.preventDefault(); undo(); }
  if(e.key === "Escape" && (selectedId !== null || hoverId !== null || armedDraftId !== null)){
    selectedId = null; hoverId = null; armedDraftId = null; render();
  }
});

// Settings
function paintNameGrid(){
  // Preview the values currently entered in the modal without mutating the saved setup.
  const teams = Math.max(4, +$("#s_teams").value || cfg.teams);
  const slot = Math.min(teams, Math.max(1, +$("#s_slot").value || cfg.slot));
  $("#s_names").innerHTML = Array.from({length: teams}, (_,i)=>
    `<div class="fld"><label>${i+1 === slot ? "You \u00b7 slot " + (i+1) : "Slot " + (i+1)}</label>
     <input type="text" data-n="${i}" maxlength="18" placeholder="Team ${i+1}"
       value="${(cfg.names[i]||"").replace(/"/g,"&quot;")}"
       ${i+1===slot ? 'style="border-color:var(--ok)"' : ''}></div>`).join("");
}

function paintCatGrid(){
  $("#s_cats").innerHTML = CATS.map(c=>
    `<div class="fld"><label>${c.label}</label>
     <input type="number" data-w="${c.k}" min="0" max="3" step="0.25" value="${cw(c.k)}"></div>`).join("");
}
let firstRun = false;

function openSet(){
  $("#s_teams").value = cfg.teams; $("#s_slot").value = cfg.slot;
  $("#s_size").value = cfg.size;   $("#s_aggr").value = cfg.aggr;
  paintCatGrid();
  paintNameGrid();
  $("#s_intro").style.display = firstRun ? "" : "none";
  const catDetails = $("#catweightsdetails");
  if(catDetails) catDetails.open = false;
  $("#s_save").textContent = firstRun ? "Save and continue" : (picks.length ? "Save and reset picks" : "Save setup");
  $("#setmask").classList.add("on");
  setTimeout(()=>{ const f = $("#s_teams"); if(f && f.focus){ f.focus(); if(f.select) f.select(); } }, 30);
}
function setCatPreset(o){
  CATS.forEach(c=>{
    const el = $("#s_cats").querySelector(`input[data-w="${c.k}"]`);
    if(el) el.value = o[c.k] === undefined ? 1 : o[c.k];
  });
}
$("#s_close").onclick = ()=> $("#setmask").classList.remove("on");
$("#help_close").onclick = ()=> $("#helpmask").classList.remove("on");

/*  The table header sticks below the search bar. Measuring beats hardcoding —
    the bar's height changes with font size and zoom.                        */
function syncStickyTop(){
  const sw = document.querySelector(".searchwrap");
  if(sw) document.documentElement.style.setProperty("--stickytop", sw.offsetHeight + "px");
}
window.addEventListener("resize", syncStickyTop);
syncStickyTop();

/*  Escape closes whichever dialog is open. Every modal had a Cancel button and
    a backdrop click, but Escape is what people actually reach for.          */
/*  Arrow keys walk the board and Enter drafts, so a fast draft never needs the
    mouse. Ignored while typing, so the search box still behaves normally.   */
document.addEventListener("keydown", e=>{
  const typing = /^(INPUT|TEXTAREA|SELECT)$/.test((e.target.tagName || ""));
  if(typing || !boardIds.length) return;
  if(e.key !== "ArrowDown" && e.key !== "ArrowUp" && e.key !== "Enter") return;

  if(e.key === "Enter"){
    if(selectedId !== null){
      e.preventDefault();
      if(armedDraftId === selectedId) draft(selectedId);
      else { armedDraftId = selectedId; render(); }
    }
    return;
  }
  e.preventDefault();
  const at = boardIds.indexOf(selectedId);
  const next = e.key === "ArrowDown"
    ? (at < 0 ? 0 : Math.min(at + 1, boardIds.length - 1))
    : (at < 0 ? 0 : Math.max(at - 1, 0));
  selectedId = boardIds[next];
  hoverId = selectedId;          // keep the preview in step with the keyboard
  armedDraftId = null;           // navigation inspects; it never pre-confirms a draft
  render();
  const row = $("#board").querySelector(`tr[data-id="${selectedId}"]`);
  if(row && row.scrollIntoView) row.scrollIntoView({block:"nearest"});
});

document.addEventListener("keydown", e=>{
  if(e.key !== "Escape") return;
  const open = ["#setmask","#impmask","#helpmask"].map($).filter(m=>m && m.classList.contains("on"));
  if(open.length){ open.forEach(m=>m.classList.remove("on")); e.stopPropagation(); }
});

/*  Draggable split. Stored as a left-column fraction rather than pixels so it
    survives a window resize.                                                 */
const SPLIT_KEY = "draftboard.split.v1";
const SPLIT_DEFAULT = 1.35 / 2.35;
function applySplit(frac){
  const f = Math.max(0.28, Math.min(0.78, frac));
  document.querySelector(".shell").style.gridTemplateColumns =
    `minmax(0,${f}fr) 5px minmax(0,${1-f}fr)`;
  return f;
}
let splitFrac = SPLIT_DEFAULT;
try{ const v = parseFloat(localStorage.getItem(SPLIT_KEY)); if(!isNaN(v)) splitFrac = v; }catch(e){}
applySplit(splitFrac);

(function(){
  const grip = $("#grip");
  let dragging = false;
  const move = e=>{
    if(!dragging) return;
    const x = (e.touches ? e.touches[0].clientX : e.clientX);
    splitFrac = applySplit(x / window.innerWidth);
    e.preventDefault();
  };
  const stop = ()=>{
    if(!dragging) return;
    dragging = false;
    grip.classList.remove("drag");
    document.body.style.userSelect = "";
    try{ localStorage.setItem(SPLIT_KEY, String(splitFrac)); }catch(e){}
  };
  const start = e=>{
    dragging = true;
    grip.classList.add("drag");
    document.body.style.userSelect = "none";   // stop the drag selecting the board
    e.preventDefault();
  };
  grip.addEventListener("mousedown", start);
  grip.addEventListener("touchstart", start, {passive:false});
  window.addEventListener("mousemove", move);
  window.addEventListener("touchmove", move, {passive:false});
  window.addEventListener("mouseup", stop);
  window.addEventListener("touchend", stop);
  grip.addEventListener("dblclick", ()=>{
    splitFrac = applySplit(SPLIT_DEFAULT);
    try{ localStorage.setItem(SPLIT_KEY, String(splitFrac)); }catch(e){}
  });
})();
$("#helpmask").addEventListener("click", e=>{ if(e.target === $("#helpmask")) $("#helpmask").classList.remove("on"); });

// Close the setup modal when the user clicks the backdrop,
// but keep clicks inside the modal itself from dismissing it.
$("#setmask").addEventListener("click", e=>{
  if(e.target === e.currentTarget) e.currentTarget.classList.remove("on");
});
$("#s_teams").addEventListener("input", paintNameGrid);
$("#s_slot").addEventListener("input", paintNameGrid);
$("#s_cats_std").onclick = ()=> setCatPreset({});
$("#s_cats_8").onclick = ()=> setCatPreset({to:0});
$("#s_save").onclick = ()=>{
  const oldStructure = {teams:cfg.teams, slot:cfg.slot, size:cfg.size};
  cfg.teams = Math.max(4, +$("#s_teams").value || 12);
  cfg.slot  = Math.min(cfg.teams, Math.max(1, +$("#s_slot").value || 1));
  cfg.size  = Math.max(5, +$("#s_size").value || 13);
  cfg.aggr  = Math.max(0, Math.min(10, +$("#s_aggr").value));
  const structureChanged = oldStructure.teams !== cfg.teams || oldStructure.slot !== cfg.slot || oldStructure.size !== cfg.size;
  cfg.names = [];
  [...$("#s_names").querySelectorAll("input[data-n]")].forEach(el=>{
    cfg.names[+el.dataset.n] = el.value.trim();
  });
  [...$("#s_cats").querySelectorAll("input[data-w]")].forEach(el=>{
    const v = parseFloat(el.value);
    cfg.catW[el.dataset.w] = isNaN(v) ? 1 : Math.max(0, Math.min(3, v));
  });
  scoreBoth(pool);
  const lastRows = pool.map(p=>p.last).filter(Boolean);
  if(lastRows.length) scoreBoth(lastRows);
  cfg.k     = Math.max(1, 12 - cfg.aggr);   // higher aggression = sharper sigmoid

  /* Once a draft has started, saving League Setup is deliberately a fresh-start
     action. The button says “Save and reset picks” so there is no hidden side effect.
     Before any picks exist, setup changes simply save normally. */
  const resetDraft = picks.length > 0;
  if(resetDraft){
    picks = []; locks = {}; hoverId = null; selectedId = null; armedDraftId = null;
    rosterInspectId = null; ledgerTeam = null; recMessageCleared = false;
    ui.gapHidden = ""; ui.sugHidden.clear(); clearState(); $("#q").value = "";
  } else {
    picks = picks.filter(p => p.overall < cfg.teams * cfg.size);
    reindex();
  }
  saveSettings();                 // persist league setup the moment it's confirmed
  $("#setmask").classList.remove("on");
  render();
  if(firstRun){ firstRun = false; $("#helpmask").classList.add("on"); }
};

// Import
$("#imp_close").onclick = ()=> $("#impmask").classList.remove("on");

// Close the projections modal when the user clicks the backdrop,
// but keep clicks inside the modal itself from dismissing it.
$("#impmask").addEventListener("click", e=>{
  if(e.target === e.currentTarget) e.currentTarget.classList.remove("on");
});

$("#imp_go").onclick = ()=>{
  const rows = dedupe(parsePool($("#impbox").value));
  const msg = $("#impmsg");
  const skipped = parsePool.skipped || [];

  if(rows.length < 5){
    msg.className = "msg bad";
    msg.innerHTML = `Parsed only ${rows.length} rows. Each needs a name and a field-goal column carrying
      makes and attempts. First unreadable row:<br>
      <span class="mono" style="color:var(--dimmer)">${(skipped[0]||"—").slice(0,110).replace(/</g,"&lt;")}</span>`;
    return;
  }

  /* ---- this season's projections: replace the pool ---- */
  saveProjectionText($("#impbox").value);
  pool = rows; picks = []; locks = {};
  scoreBoth(pool);
  const withGP = pool.filter(p=>p.gp>0).length;
  const withRank = pool.filter(p=>p.srcRank!==null).length;
  const withADP = pool.filter(p=>p.adp!==null).length;

  msg.className = "msg good";
  msg.innerHTML = `Loaded <b>${pool.length}</b> players
    ${withGP ? `· ${withGP} with GP (durability slider live)` : `· no GP column, durability slider stays off`}
    ${withRank ? `· ${withRank} with source rank` : ``}
    ${withADP ? `· ${withADP} with ADP` : `· no ADP, run risk falls back to value rank`}
    ${skipped.length ? `· skipped ${skipped.length} rows` : ``}
    <table class="prev"><tr><th class="l">Sanity check</th><th>POS</th><th>FG</th><th>FT</th><th>PTS</th><th>REB</th><th>GP</th></tr>
    ${pool.slice(0,4).map(p=>`<tr><td class="l">${p.name}</td><td>${p.pos.join("/")}</td>
      <td class="mono">${p.fgm}/${p.fga}</td><td class="mono">${p.ftm}/${p.fta}</td>
      <td class="mono">${p.pts}</td><td class="mono">${p.reb}</td><td class="mono">${p.gp||"—"}</td></tr>`).join("")}</table>`;

  $("#banner").classList.add("hide");
  syncGPW();

  // A new projection pool should reset draft-specific state, but it should NOT
  // wipe league setup. At this point picks/locks are already empty and `pool`
  // is the newly imported pool, so save the new clean draft state with the
  // existing league settings before reloading.
  saveState();

  // The pasted projection text is already persisted in localStorage.
  // Reboot the page so the normal startup path reloads the pool and
  // attaches the built-in historical data before rendering.
  $("#impmask").classList.remove("on");
  setTimeout(()=> window.location.reload(), 150);
};


/* ============================================================
   YAHOO LIVE DRAFT BRIDGE — pick-number aware + fallback-line placeholders (v0.5)
   ------------------------------------------------------------
   Yahoo can render several draft-board cells out of chronological order. The
   extension therefore sends Yahoo's printed pick numbers (1.03, 1.06, etc.)
   and, when supported, an ordered snapshot. 9cat buffers/reconciles by that
   explicit order instead of treating "first DOM mutation seen" as "next pick".
   ============================================================ */
const YAHOO_PAGE_SOURCE = "9cat-page";
const YAHOO_EXT_SOURCE  = "9cat-extension";
const YAHOO_CAPABILITIES = ["yahoo-pick-number-v1", "yahoo-snapshot-v1", "yahoo-out-of-pool-v1", "yahoo-fallback-line-v1"];
const yahooPickBuffer = new Map();   // overall pick number -> Yahoo pick record

function yahooSyncPill(){
  let el = document.getElementById("yahooSyncPill");
  if(el) return el;
  const host = document.querySelector(".searchwrap");
  if(!host) return null;
  el = document.createElement("span");
  el.id = "yahooSyncPill";
  el.style.cssText = [
    "flex:0 0 auto","align-self:stretch","display:flex","align-items:center",
    "padding:0 9px","border:1px solid var(--line)","border-radius:var(--r)",
    "font-family:'Saira Condensed',sans-serif","font-weight:700","font-size:10px",
    "letter-spacing:.08em","text-transform:uppercase","color:var(--dimmer)",
    "background:var(--panel)"
  ].join(";");
  host.appendChild(el);
  return el;
}

function setYahooSyncStatus(text, tone="dim"){
  const el = yahooSyncPill();
  if(!el) return;
  el.textContent = text;
  el.style.color = tone === "ok" ? "var(--ok)" : tone === "bad" ? "var(--hot)" : tone === "warn" ? "var(--wood)" : "var(--dimmer)";
  el.style.borderColor = tone === "ok" ? "var(--okk-bd)" : tone === "bad" ? "var(--neg-bd)" : tone === "warn" ? "var(--wood)" : "var(--line)";
}

function yahooMatchPlayer(rawName){
  const name = String(rawName || "").trim();
  if(!name) return null;
  const exactKey = nameKey(name);
  let matches = pool.filter(p => nameKey(p.name) === exactKey);
  if(matches.length === 1) return matches[0];

  const parts = nameParts(name);
  const fallback = parts.length ? (parts[parts.length-1] + (parts[0]?.[0] || "")) : "";
  if(!fallback) return null;
  matches = pool.filter(p => {
    const pp = nameParts(p.name);
    return pp.length && (pp[pp.length-1] + (pp[0]?.[0] || "")) === fallback;
  });
  return matches.length === 1 ? matches[0] : null;
}

function pickName(pk){ return playerForPick(pk)?.name || pk?.unknownName || ""; }

function announceNinecatState(){
  const ordered = [...picks].sort((a,b)=>a.overall-b.overall);
  window.postMessage({
    source: YAHOO_PAGE_SOURCE,
    type: "NINECAT_READY",
    playerNames: pool.map(p=>p.name),
    /*  Teams disambiguate abbreviated names. Yahoo renders "A. THOMPSON" for
        both Amen (HOU) and Ausar (DET); without the team there is no way to
        tell them apart, and guessing wrong corrupts the draft.              */
    playerTeams: pool.map(p=>p.team || ""),
    /* Stable identity fallback for Yahoo abbreviations that remain ambiguous
       even after NBA-team matching (e.g. Jalen/Jaylin Williams are both OKC).
       This is the board's fixed Value Rank (#), not the dynamic Fit rank. */
    playerRanks: pool.map(p=>Number.isFinite(p.valRank) ? p.valRank : null),
    pickNames: ordered.map(pickName).filter(Boolean),
    teamCount: cfg.teams,
    rosterSize: cfg.size,
    capabilities: YAHOO_CAPABILITIES
  }, "*");
}

function yahooAck(status, name, detail=""){
  window.postMessage({ source:YAHOO_PAGE_SOURCE, type:"YAHOO_ACK", status, name, detail }, "*");
}

/*  A pick used to be thrown away unless Yahoo's payload carried an integer
    overall-pick number. The DOM path doesn't need one — position in the draft
    results list IS the pick number — so a missing pickNo now means "append",
    not "discard". This was the reason nothing ever synced.                  */
function normalizeYahooPick(raw, fallbackNo){
  const name = String(raw?.name || "").trim();
  if(!name) return null;
  let pickNo = +raw?.pickNo;
  if(!Number.isInteger(pickNo) || pickNo < 1){
    pickNo = Number.isInteger(fallbackNo) && fallbackNo > 0 ? fallbackNo : null;
  }
  if(pickNo === null) return null;
  return { ...raw, name, pickNo };
}

function bufferYahooPicks(rawPicks){
  let n = 0;
  for(const raw of rawPicks || []){
    n++;
    const p = normalizeYahooPick(raw, n);
    if(!p) continue;
    yahooPickBuffer.set(p.pickNo, p);
  }
}

function contiguousYahooBuffer(){
  const out = [];
  for(let n=1;;n++){
    const p = yahooPickBuffer.get(n);
    if(!p) break;
    out.push(p);
  }
  return out;
}

/* Reconcile only from a contiguous 1..N Yahoo prefix. This guarantees a later
   tile such as 1.06 can never be mistaken for pick 1.03. If the current 9cat
   log is already longer than the available Yahoo prefix, wait for the rescan to
   catch up before correcting it so a partial render never truncates the draft. */
function reconcileYahooBuffer(){
  const ordered = contiguousYahooBuffer();
  const bufferedCount = yahooPickBuffer.size;
  const gapPickNo = bufferedCount > ordered.length ? ordered.length + 1 : null;
  if(!ordered.length){
    setYahooSyncStatus("Yahoo · buffering", "warn");
    yahooAck("buffering", "", "Waiting for Yahoo pick #1 / earlier picks");
    return false;
  }

  const resolved = [];
  const used = new Set();
  for(const yp of ordered){
    if(yp.outOfPool){
      const ukey = `u:${nameKey(yp.name)}:${String(yp.team||"").toUpperCase()}`;
      if(used.has(ukey)){
        setYahooSyncStatus(`Yahoo · duplicate #${yp.pickNo}`, "bad");
        yahooAck("unmatched", yp.name, `Same out-of-pool Yahoo player appeared twice by pick #${yp.pickNo}`);
        break;
      }
      used.add(ukey);
      resolved.push({ yahoo:yp, player:null, outOfPool:true });
      continue;
    }
    const p = yahooMatchPlayer(yp.name);
    if(!p){
      setYahooSyncStatus(`Yahoo · unmatched #${yp.pickNo}`, "bad");
      yahooAck("unmatched", yp.name, `Could not uniquely match Yahoo pick #${yp.pickNo}`);
      break;
    }
    const pkey=`p:${p.id}`;
    if(used.has(pkey)){
      setYahooSyncStatus(`Yahoo · duplicate #${yp.pickNo}`, "bad");
      yahooAck("unmatched", yp.name, `Same player appeared twice in Yahoo snapshot by pick #${yp.pickNo}`);
      break;
    }
    used.add(pkey);
    resolved.push({ yahoo:yp, player:p, outOfPool:false });
  }

  if(!resolved.length) return false;

  const current = [...picks].sort((a,b)=>a.overall-b.overall);
  const currentKey = pk => pk?.outOfPool
    ? `u:${nameKey(pk.unknownName||"")}:${String(pk.unknownTeam||"").toUpperCase()}`
    : `p:${pk?.playerId}`;
  const resolvedKey = x => x?.outOfPool
    ? `u:${nameKey(x.yahoo?.name||"")}:${String(x.yahoo?.team||"").toUpperCase()}`
    : `p:${x?.player?.id}`;
  const compareN = Math.min(current.length, resolved.length);
  let mismatch = false;
  for(let i=0;i<compareN;i++){
    if(currentKey(current[i]) !== resolvedKey(resolved[i])){ mismatch = true; break; }
  }

  /*  Yahoo is the source of truth.

      This used to refuse any snapshot shorter than the existing log, so a stale
      9cat draft left over from a previous session deadlocked syncing forever —
      "waiting for Yahoo snapshot to cover 43 picks" while Yahoo only had 8, and
      it never would. Now: if the overlapping picks DISAGREE, the old log is
      wrong and gets rebuilt from Yahoo. Only a clean prefix waits, since that's
      the genuine partial-render case worth protecting.                        */
  if(resolved.length < current.length && !mismatch){
    setYahooSyncStatus(`Yahoo · ${resolved.length}/${current.length}`, "warn");
    yahooAck("buffering", "", `Yahoo snapshot covers ${resolved.length} of ${current.length} logged picks`);
    return false;
  }

  const changed = mismatch || resolved.length !== current.length;
  if(!changed){
    if(gapPickNo){
      setYahooSyncStatus(`Yahoo · waiting for #${gapPickNo} · ${bufferedCount} seen`, "warn");
      yahooAck("buffering", "", `Captured ${bufferedCount} Yahoo picks but pick #${gapPickNo} is missing`);
      return false;
    }
    setYahooSyncStatus(`Yahoo · synced ${resolved.length}`, "ok");
    return true;
  }

  const wasCorrection = current.length > 0 && mismatch;
  picks = resolved.map((x,i)=> x.outOfPool ? ({
    playerId:null,
    unknownName:x.yahoo.name,
    unknownTeam:String(x.yahoo.team||"").toUpperCase(),
    unknownPos:Array.isArray(x.yahoo.pos) ? x.yahoo.pos : [],
    outOfPool:true,
    teamIdx:teamOnClock(i),
    overall:i
  }) : ({
    playerId:x.player.id,
    teamIdx:teamOnClock(i),
    overall:i
  }));

  selectedId = null;
  hoverId = null;
  armedDraftId = null;
  flashPick = picks.length ? picks.length - 1 : null;
  clearTimeout(reconcileYahooBuffer._flash);
  reconcileYahooBuffer._flash = setTimeout(()=>{ flashPick = null; renderLog(); }, 1500);
  render();

  const last = resolved.at(-1);
  if(gapPickNo) setYahooSyncStatus(`Yahoo · waiting for #${gapPickNo} · ${bufferedCount} seen`, "warn");
  else setYahooSyncStatus(`Yahoo · synced ${picks.length}`, "ok");
  yahooAck(wasCorrection ? "corrected" : "applied", last?.player?.name || last?.yahoo?.name || "",
    `${wasCorrection ? "Rebuilt" : "Synced"} picks 1-${picks.length} in Yahoo order`);
  if(gapPickNo) yahooAck("buffering", "", `Captured ${bufferedCount} Yahoo picks but pick #${gapPickNo} is missing`);
  announceNinecatState();
  return !gapPickNo;
}

function applyYahooDraftSnapshot(rawPicks){
  bufferYahooPicks(Array.isArray(rawPicks) ? rawPicks : []);
  return reconcileYahooBuffer();
}

function applyYahooDraftPick(raw){
  const obj = (raw && typeof raw === "object") ? raw : {name:raw};
  const explicit = normalizeYahooPick(obj);
  if(explicit){
    yahooPickBuffer.set(explicit.pickNo, explicit);
    return reconcileYahooBuffer();
  }

  // Compatibility with the original v0.1 extension only. v0.3 always sends an
  // explicit pick number and never uses this sequential fallback.
  const name = String(obj?.name || "").trim();
  const p = yahooMatchPlayer(name);
  if(!p){
    setYahooSyncStatus("Yahoo · unmatched", "bad");
    yahooAck("unmatched", name, "No unique player match in the current 9cat pool");
    return false;
  }
  if(takenIds().has(p.id)){
    yahooAck("duplicate", p.name, "Player is already in the 9cat pick log");
    return true;
  }
  draft(p.id);
  setYahooSyncStatus(`Yahoo · synced ${picks.length}`, "ok");
  yahooAck("applied", p.name, `Logged as pick ${picks.length}`);
  announceNinecatState();
  return true;
}

window.addEventListener("message", e=>{
  if(e.source !== window || !e.data || e.data.source !== YAHOO_EXT_SOURCE) return;
  const d = e.data;
  if(d.type === "REQUEST_STATE") announceNinecatState();
  if(d.type === "YAHOO_PICK") applyYahooDraftPick(d.pick || {name:d.name});
  if(d.type === "YAHOO_SNAPSHOT") applyYahooDraftSnapshot(d.picks || []);
  if(d.type === "YAHOO_RESET_CAPTURE") yahooPickBuffer.clear();
  if(d.type === "YAHOO_STATUS"){
    if(d.enabled === false) setYahooSyncStatus("Yahoo · paused", "warn");
    else if(d.yahooConnected){
      const contiguous = +d.contiguousCount || 0;
      const captured = +d.capturedCount || 0;
      if(captured > contiguous){
        setYahooSyncStatus(`Yahoo · waiting for #${contiguous + 1} · ${captured} seen`, "warn");
      } else {
        const n = contiguous || captured || 0;
        setYahooSyncStatus(`Yahoo · live${n ? ` ${n}` : ""}`, "ok");
      }
    } else setYahooSyncStatus("Yahoo · waiting", "dim");
  }
});

document.addEventListener("visibilitychange", ()=>{
  if(!document.hidden) setTimeout(announceNinecatState, 80);
});
setTimeout(announceNinecatState, 0);

// Boot
const savedProjectionText = loadProjectionText();
/*  Settings must load BEFORE scoring. cfg.gpw feeds straight into scoreBoth, so
    scoring first meant every refresh silently used the default games-played
    weight and threw away the saved one — the slider snapping back was the
    visible half of it; the z-scores being wrong was the half you couldn't see. */
loadSettings();
pool = dedupe(parsePool(savedProjectionText || RAW));
scoreBoth(pool);
const matched = pool.length ? attachLast(dedupe(parsePool(RAW_LAST))) : 0;
const resumed = loadState();
syncGPW();

if(!pool.length){
  $("#banner").innerHTML =
    `<span>No projections loaded yet — open Projections and paste a table to begin.</span>`
    + `<button id="b_banner2">Load projections</button>`;
} else {
  $("#banner").innerHTML =
    `<span>${pool.length} players loaded with projections, ADP and games played. `
    + `${matched} matched to last season's actuals.`
    + `${resumed ? ` Resumed your draft at pick ${picks.length + 1}.` : ``}</span>`
    + `<button id="b_banner2">Load different data</button>`
    + `<button id="b_dismiss" class="x" title="Dismiss">&times;</button>`;
}
$("#banner").classList.remove("hide");
$("#b_banner2").onclick = ()=> $("#impmask").classList.add("on");
$("#i_photoauto").onclick = ()=> autoPhotos(false);
$("#i_photogo").onclick = ()=>{
  const n = importPhotoIds($("#i_photos").value);
  $("#i_photomsg").textContent = n ? `${n} ids saved` : "Nothing recognised \u2014 each line needs a name and a numeric id";
  $("#i_photomsg").style.color = n ? "var(--ok)" : "var(--hot)";
  syncPhotoStat();
  if(n) render();
};

/*  Tells you how many players on the CURRENT board actually resolved to a photo.
    "12 ids saved" is useless on its own — what matters is whether they matched
    the names in your projection pool.                                        */
function syncPhotoStat(){
  const el = $("#photostat"); if(!el) return;
  if(!pool.length){ el.textContent = "load projections first"; return; }
  const hit = pool.filter(p => photoIdFor(p)).length;
  el.textContent = hit ? `${hit} of ${pool.length} players matched` : "not set up";
  el.style.color = hit ? "var(--ok)" : "var(--dimmer)";
  const d = $("#photodetails");
  if(d && hit) d.open = false;
}
const bd = $("#b_dismiss"); if(bd) bd.onclick = ()=> $("#banner").classList.add("hide");
/*  First visit opens Setup, not the importer. Teams and draft slot decide the
    snake order, which decides "next turn in N picks", which decides scarcity on
    every player — wrong there and nothing looks broken, the numbers are just
    quietly wrong all draft. Saving chains straight into Projections.         */
const SEEN_KEY = "draftboard.seen.v1";
let seenBefore = false;
try{ seenBefore = localStorage.getItem(SEEN_KEY) === "1"; }catch(e){}

if(!seenBefore){
  firstRun = true;
  openSet();
  try{ localStorage.setItem(SEEN_KEY, "1"); }catch(e){}
} else if(!pool.length){
  $("#impmask").classList.add("on");
}
syncPhotoStat();
applyUIPrefs();     // restore view preferences before the first paint
render();

/*  First run with no photos cached: try once, quietly, after the board is up.
    A failure is remembered so it doesn't retry on every load — but it will try
    again after a day in case the network was simply down.                    */
(function primePhotos(){
  if(Object.keys(photoIds).length) return;
  let last = null;
  try{ last = localStorage.getItem(PHOTO_TRY); }catch(e){}
  if(last === "ok") return;
  if(last && Date.now() - Number(last) < 86400000) return;
  setTimeout(()=> autoPhotos(true), 400);
})();

/* ============================================================
   YAHOO SYNC — stub, deliberately not wired up.

   Yahoo now gates Fantasy API access behind an approval application
   (sports.yahoo.com/developer/access). If you get approved, the shape
   is: OAuth2 with an https://localhost callback, then poll

     GET /fantasy/v2/league/{game_key}.l.{league_id}/draftresults?format=json

   every ~5s while league draft_status is "drafting". Map each result's
   {round, pick, team_key, player_key} onto the picks[] array below and
   call render(). Test it in a mock draft BEFORE you rely on it — verify
   picks actually appear mid-draft rather than only at postdraft.

   async function syncYahoo(leagueKey, token){
     const r = await fetch(`/api/yahoo/league/${leagueKey}/draftresults`,
                           {headers:{Authorization:`Bearer ${token}`}});
     const data = await r.json();
     // ...map to picks[], dedupe on overall, then render()
   }
   Note: the fetch must go through your own small proxy — Yahoo does not
   send CORS headers, so a browser-only call will be blocked.
   ============================================================ */
